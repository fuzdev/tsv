# Prettier Conformance: TypeScript Comments

Comment handling is tsv's single largest category of deliberate Prettier divergence, so
its catalog carries its own doc. The framework it answers to — §Comment Position Philosophy,
§Uniform Forced-Continuation Indent, §Authored breaks in value position — lives in
[conformance_prettier.md](./conformance_prettier.md); the rest of the TypeScript catalog is
in [conformance_prettier_ts.md](./conformance_prettier_ts.md).

## TypeScript: Comments

Prettier relocates certain comments during formatting. tsv preserves comments where the user placed them. This is the single largest category of divergence. See [Comment Position Philosophy](./conformance_prettier.md#comment-position-philosophy) for the design principles.

### Comment relocation

Prettier moves comments between syntactic boundaries into adjacent blocks, parens, or other positions. tsv preserves them where the user placed them. **Unless an entry notes otherwise, both the authored position and Prettier's relocated one are stable under tsv** ("dual-stable in our formatter" — the author's choice is preserved without trading away stability). The exceptions an entry calls out are where a form is stable under only one formatter, where Prettier is non-idempotent, or where not preserving would drop the comment (content loss).

- Conditional type after `:` → Trailing on true branch (changing association from false to true branch) — [comment_after_colon](../tests/fixtures/typescript/types/conditional/comment_after_colon_prettier_divergence/)
- Conditional branch breaking-union leading block comment → Into the first member, across the `|` (`: | /* c */ A`); tsv keeps it before the union on the `:` line (`: /* c */ | A`) — [branch_comment_long](../tests/fixtures/typescript/types/conditional/branch_comment_long_prettier_divergence/)
- Ternary operand to operator (≥2 line comments; test→`?`, consequent→`:`) → Every comment after the first relocated across the operator (`cond // c1⏎ ? // c2`); tsv keeps each before the operator on its own line — [consecutive_operand_comment](../tests/fixtures/typescript/expressions/ternary/consecutive_operand_comment_prettier_divergence/)
- Ternary operator→branch **block** comment, authored trailing the operand's line (`cond ? x : /* c */⏎y`) → Across the operator, onto the preceding operand (`cond ? x /* c */ : y`), changing its association from the branch it precedes to that operand; tsv keeps it after the operator and collapses inline. Prettier's attachment claims the comment as leading of the branch only when it is *not* on the preceding node's line, so sharing that line makes it trailing of the operand instead. Both slots (`?` and `:`) behave alike, and the relocated form is dual-stable — pinned `variant_trailing`. The **own-line** authoring is a *pass-count* gap, not a relocation: prettier reaches tsv's fixed point non-idempotently (first pass breaks the whole conditional on the authored newline), pinned `prettier_intermediate_own_line` reconverging to input — [operand_block_comment_relocation](../tests/fixtures/typescript/expressions/ternary/operand_block_comment_relocation_prettier_divergence/)
- Conditional-type branch block comment (same shape, type level; `B extends C ? /* c */ D : E`) → Across the operator (`B extends C /* c */ ? D : E`); tsv preserves. Same `variant_trailing` + `prettier_intermediate_own_line` pinning as its expression twin — [branch_block_comment_relocation](../tests/fixtures/typescript/types/conditional/branch_block_comment_relocation_prettier_divergence/)
- Decorator expression→shell-close, optional chain only (`@(a?.b /* c */)`) → **the one entry where tsv relocates and prettier preserves**, and it is not a taste call on either side. tsv prints a decorator's trailing run *outside* the shell, so both authorings reach one fixed point (`@(a?.b) /* c */`); the parens are the printer's, added or omitted from the expression's shape rather than the source's spelling, so a position defined against them carries no authorial signal — and outside means a `//` needs no forced-open shell. Prettier agrees on **every other shape** (`@(a + b) /* c */`, `@(a[b]) /* c */`, `@(x!) /* c */`, `@(a ? b : c) /* c */`, `@fn() /* c */`), pinned as matches by [expression_trailing_comment](../tests/fixtures/typescript/typescript_specific/decorators/expression_trailing_comment/). The chain is an *attachment artifact*: `a?.b` is a `ChainExpression` wrapping the member, so the comment attaches inside. Two tells that no rule is meant — prettier is stable on **both** spellings (hence `prettier_variant_inside_parens`, not `output_prettier`), and the same chain with a **line** comment goes back outside, matching tsv — [optional_chain_trailing_comment](../tests/fixtures/typescript/typescript_specific/decorators/optional_chain_trailing_comment_prettier_divergence/)
- Negative literal type, sign→numeral (`-/* c */ 1`) → ◆prettier_bug ◆content_preservation — into parens around the numeral (`-(/* c */ 1)`), a type that does not exist: tsc reads `-` as a negative literal type only when the next token is a numeric/bigint literal, so the output fails to re-parse. tsv preserves in place, which stays valid because the lookahead's `nextToken()` skips trivia. Not preserving drops the comment — the position has no other emitter — [negative_literal_sign_comment](../tests/fixtures/typescript/types/negative_literal_sign_comment_prettier_divergence/)
- Switch empty body → Discriminant parens — [empty_comment](../tests/fixtures/typescript/statements/switch/empty_comment_prettier_divergence/)
- Switch case before `{` → After opening brace (prettier non-idempotent, 2-3 passes) — [case_block_comment](../tests/fixtures/typescript/statements/switch/case_block_comment_prettier_divergence/)
- Switch case before `{`, **comment run** (block comments, mixed with a label or leading line comment) → The whole run pulled onto the label line and the block de-indented to the label's level; tsv keeps each comment on the line the author gave it — a glued pair stays glued, own-line ones keep their own lines, author blank lines survive — with the block below the run at consequent indent. Both halves come from the shared leading-comment rule (prettier's `printLeadingComment`, keyed on the source right after *each comment*), so the run is neither split nor merged, and whether the block still hugs falls out of it rather than being a second policy: anything on the label's *rendered* line was claimed by the label's trailing emitter — which walks a multiline block to its closing line, so a comment glued past the `*/` trails the label too — leaving any run that reaches the leading position to claim a line of its own. The block-comment face of the row above, decided by that one rule rather than by comment kind; the hug survives wherever the author left the label's line to the comment (`case 7: /* block */ {`, `case 8: /* a⏎b */ /* c */ {`). Prettier is non-idempotent over four passes and its fixed point relocates the run **across the `:`** (`case 1 /* block1 */ /* block2 */: {`), re-binding it from the block to the case test, and pulls a label line comment inside the block body — `audit_signature.txt` pins the chain. It also **reorders**: the label's own line comment ends up behind the run, and at a `default:` label the run is emitted backwards (`/* block2 */ /* block1 */`) — [case_block_leading_comment_run](../tests/fixtures/typescript/statements/switch/case_block_leading_comment_run_prettier_divergence/)
- Switch discriminant trailing → Switch body. Both authorings of the gap reach one tsv fixed point — the comment kept before the `)` on its own line — but prettier's *landing* differs by authoring: from the own-line form it leads the first case on a line of its own, while from the **closing-paren line** form (`switch (y⏎/* c */) {`, the only spelling a block comment has there — a `//` would swallow the `) {`) it lands **glued** to the label (`/* trailing */ case 1:`), a dual-stable form pinned `variant_close_paren_line`. The whole gap is shared with `if`/`while`/`do…while`/`catch`, where prettier preserves in place and tsv matches ([condition_paren_comment_on_close_line](../tests/fixtures/typescript/syntax/comments/condition_paren_comment_on_close_line/)); switch is the one member whose comment prettier moves — [discriminant_trailing_comment](../tests/fixtures/typescript/statements/switch/discriminant_trailing_comment_prettier_divergence/)
- For empty clauses (line comments) → Outside parentheses (broken); tsv keeps each inside the parens, breaking only where a `//` forces a line end so `;;` stay together — [empty_clauses_comment](../tests/fixtures/typescript/statements/for/empty_clauses_comment_prettier_divergence/)
- For empty clauses (block comments) → Outside parentheses; tsv keeps them inline in place and the trivially-short header on one line (`for (/* a */ ;;)`) — [empty_clauses_block_comment](../tests/fixtures/typescript/statements/for/empty_clauses_block_comment_prettier_divergence/)
- For clause **leading own-line block** comment (`for (⏎/* a */⏎x = 0; …)`, and the same gap before the test / update clauses) → The whole header pulled onto one line; tsv keeps the authored line and expands around it, the same rule its own-line siblings follow. A block written *on* the `(`/`;` line takes the other outcome and keeps the header on one line, matching prettier ([clause_leading_comment_run](../tests/fixtures/typescript/statements/for/clause_leading_comment_run/)); a glued run stays on one line in both — [clause_leading_own_line_run](../tests/fixtures/typescript/statements/for/clause_leading_own_line_run_prettier_divergence/)
- For update clause→`)` **own-line** comment → Out of the header entirely, stranded between `)` and the body `{`; tsv keeps it inside the parens on its own line before `)`, the same treatment an [empty update slot](#comment-relocation) gets, and for the same reason — `)` is all that closes the region. Not preserving drops it: the gap had no emitter but the same-line trail — [update_trailing_comment](../tests/fixtures/typescript/statements/for/update_trailing_comment_prettier_divergence/)
- For **partially**-empty clause slot (one or two clauses present) → Into the next clause across the `;` (empty init / empty test slot) or out of the header entirely, stranded between `)` and the body `{` (empty update slot); tsv keeps the comment in the slot it was written in, the partially-empty header's form of the fully-empty rule above. The slot is what the comment is about — one in the empty test slot documents the missing condition, not the update prettier binds it to. Not preserving drops it: an empty slot has no other emitter — [empty_slot_comment](../tests/fixtures/typescript/statements/for/empty_slot_comment_prettier_divergence/)
- For partially-empty clause slot, **a run of comments** → The whole run relocated the same way; tsv keeps it in the slot, in order, separated by the slot's own `line` — a space while the header fits, a fresh line once a `//` forces the header open. The run is where that separator becomes observable, since a single comment has nothing between it and could be held as one glued blob unnoticed — [empty_slot_comment_run](../tests/fixtures/typescript/statements/for/empty_slot_comment_run_prettier_divergence/)
- For-of broken header line comments → Header collapsed inline, `// before const` kept trailing `(`, the rest scattered into the body (two-pass); tsv keeps each inside the broken header — [of_line_comment](../tests/fixtures/typescript/statements/for/of_line_comment_prettier_divergence/)
- For-of/for-in keyword→binding line comment → Comment kept trailing `const`, but `x of y` pulled back onto one indented line; tsv keeps each inside the broken header (a same-gap *block* comment matches — `decl_keyword_binding_comment`; C-style `for`/standalone agree, only for-of/for-in diverges) — [of_in_keyword_binding_line_comment](../tests/fixtures/typescript/statements/for/of_in_keyword_binding_line_comment_prettier_divergence/)
- For-in/of interior line comment → Header collapsed inline, the comment relocated to after `)`; tsv keeps the header broken with the comment between the operands (inline block comments match) — [in_of_own_line_comment](../tests/fixtures/typescript/statements/for/in_of_own_line_comment_prettier_divergence/)
- For-in/of pre-paren comment / `for await` (breaking layout) → Pre-paren comment inside the parens, left-trailing line comment after `)` — [in_of_break_pre_paren_comment](../tests/fixtures/typescript/statements/for/in_of_break_pre_paren_comment_prettier_divergence/)
- For-in/of `)`→body line comment → Dropped to its own line, indented with the body; tsv keeps it trailing `)` (block comments stay inline in both) — [in_of_close_paren_comment](../tests/fixtures/typescript/statements/for/in_of_close_paren_comment_prettier_divergence/)
- Do-while after `(` → After semicolon (unique to do-while; other constructs keep comments inside parens) — [open_paren_comment](../tests/fixtures/typescript/statements/do_while/open_paren_comment_prettier_divergence/)
- If/while/switch keyword before `(` → Inside the condition parens — [keyword_paren_comment](../tests/fixtures/typescript/statements/if/keyword_paren_comment_prettier_divergence/)
- Between `}` and catch/finally → Into subsequent block body; tsv preserves the authored position, trailing or own-line, exactly as it does in the `}`→`else` gap (Prettier relocates only here, not at `else` — internally inconsistent on the same question, so no oracle) — [catch_between_comment](../tests/fixtures/typescript/statements/try/catch_between_comment_prettier_divergence/)
- Switch `)`→`{` → Into the switch body (own-line comment) or into the **discriminant parens** (one trailing `)`, `switch (⏎↹a // c⏎) {`, which re-binds it to the discriminant); tsv keeps the author's placement and drops `{` to the next line after a `//`. Emitting this gap inline and then appending a bare `" {"` let the comment **swallow the opening brace** (`switch (a) // c {`), which does not reparse — content corruption, which is why tsv holds a position here rather than following either relocation. A block comment keeps its authored line like every other kind: authored trailing `)` it stays there with `{` still hugged (both formatters), authored on its own line it keeps that line and `{` drops below, where prettier absorbs it into the body — [head_body_line_comment](../tests/fixtures/typescript/statements/switch/head_body_line_comment_prettier_divergence/)
- Try/catch/finally before `{` → Into block body or catch parens, from **either** authoring; tsv keeps the line comment where the author wrote it — trailing the keyword/`)` stays trailing (`try // c⏎{`), on its own line keeps its own line (`try⏎// c⏎{`) — across `try`, `catch (e)`, bare `catch`, and `finally`, matching what the `if`/`while` `)`→`{` gap already does. An own-line **block** comment normalizes to trailing in both formatters (as it does at `if`/`while`), so it is not a divergence — [line_comment_absorbed](../tests/fixtures/typescript/statements/try/line_comment_absorbed_prettier_divergence/)
- Label after `:` → Before entire labeled statement — [comment](../tests/fixtures/typescript/statements/labeled/comment_prettier_divergence/)
- Between `}` and `else` (block comment, same-line `/* b */ else`) → Cuddled on the `else` line by prettier; tsv splits it onto its own line before `else` (an own-line leading comment matches in both — no oracle) — [else_leading_block_comment](../tests/fixtures/typescript/statements/if/else_leading_block_comment_prettier_divergence/)
- Multiple inline block comments between `}` and `else` → Prettier splits every comment after the first onto its own line; tsv keeps them all inline on the `} … else` line — [else_block_inline_comment](../tests/fixtures/typescript/statements/if/else_block_inline_comment_prettier_divergence/)
- While `)`→`{` line comment, blank line *after* it → Prettier keeps the blank between the comment and `{`; tsv drops it (a blank *before* the comment is dropped by both). Trailing/own-line comments stay put in both — [line_before_body_comment](../tests/fixtures/typescript/statements/while/line_before_body_comment_prettier_divergence/)
- Do-while between `}` and `while` → Into while condition — [line_before_while_comment](../tests/fixtures/typescript/statements/do_while/line_before_while_comment_prettier_divergence/), [while_leading_block_comment](../tests/fixtures/typescript/statements/do_while/while_leading_block_comment_prettier_divergence/)
- Trailing member chain → After `=` — [trailing_member_comment](../tests/fixtures/typescript/expressions/calls/chained/trailing_member_comment_prettier_divergence/)
- Member-only chain interior line comment (no calls) → Own-line comment hoisted onto its own line after `=`, same-line comment kept trailing its member, chain partially broken (single own-line comment → chain stays inline); tsv breaks the chain and keeps each comment in place — [member_only_interior_line_comment](../tests/fixtures/typescript/expressions/calls/chained/member_only_interior_line_comment_prettier_divergence/)
- Member-only chain interior line comment, **parenthesized head** (no calls) → Prettier keeps the comment in the gap but expands the head's grouping parens over three lines (`const a = (⏎\tb as T⏎) // c⏎.d;`), breaking the paren group rather than the chain; tsv applies the bare-head rule above — the chain breaks at the member, the head's parens stay inline. Both keep the comment where it was written, so the divergence is about which group pays for the break. Covers every head that takes parens (a cast, an `await`, an arrow), the shapes the bare-head entry's `!has_calls && !first_has_parens` route never reached — without the chain break the comment defers to end-of-line and merges with the statement's own trailing comment (`.g; // c1 // c2`, the second `//` becoming text of the first) — [member_only_paren_head_interior_line_comment](../tests/fixtures/typescript/expressions/calls/chained/member_only_paren_head_interior_line_comment_prettier_divergence/)
- Member-only chain interior line comment, trailing non-null `!` (no calls) → Same relocation as above; both formatters keep the `!` glued to its member (`.bar!` / `?.bar!`) — a break before a non-null `!` is a syntax error (`[no LineTerminator here]`), so it never lands on its own line. The non-null variant of [member_only_interior_line_comment](#comment-relocation) — [member_only_non_null_line_comment](../tests/fixtures/typescript/expressions/calls/chained/member_only_non_null_line_comment_prettier_divergence/)
- Member-only chain interior line comment, in assignment-expression / object-property position → Same relocation as above, but on the RHS of an assignment expression (`x = foo // c⏎.bar!`) or an object-property value rather than a `const` declaration; tsv keeps the chain on the operator line (comment where authored), prettier breaks after `=` (the object-property value stays on the `:` line for both). The `const`-declaration seed prints through a different layout path, so this position is pinned separately — [member_chain_line_comment_assignment](../tests/fixtures/typescript/expressions/calls/chained/member_chain_line_comment_assignment_prettier_divergence/)
- Callback **last-parameter** break-forcing comment, `function` expression with a non-bare parameter → A comment after the last parameter that forces the list multiline invalidates the expand-last-argument hug — that hug renders the callee and the signature's head on one line — so the call expands. tsv applies that to every callback; prettier applies it to a `function` expression **only while its last parameter is a bare identifier**, and goes back to hugging once that parameter carries a type annotation, a default, or is a pattern. tsv is uniform because prettier's split is not a layout decision: `handle-comments.js`'s last-function-argument handler fires only when the node preceding the comment is an `Identifier`/pattern/rest, and an annotation puts the *type* node there instead, so the handler declines and the expansion flips. Nothing about `y: T` versus `y` changes what the layout can render, and a formatter whose **arrow** and `function` callbacks answered this oppositely would be the alternative — the arrow twin already expands in every one of these shapes, prettier included. Both prettier forms are stable fixed points, so this is a divergence, not a convergence bug. The bare-identifier half converges and is pinned in [function_callback_trailing_param_comment](../tests/fixtures/typescript/expressions/calls/function_callback_trailing_param_comment/), together with the single-line-block control and the **`new`** control — prettier hugs a `function` argument under `new` uniformly, so there is no incoherence there and tsv matches it on both callback kinds — [function_annotated_param_comment](../tests/fixtures/typescript/expressions/calls/function_annotated_param_comment_prettier_divergence/)
- Type-alias `=`→RHS same-line line comment → Onto its own line after `=` (`type T =⏎// c⏎Ref`); tsv keeps it trailing `=` (`type T = // c⏎Ref`), independent of whether the RHS breaks. The type-alias analog of the attribute-list [comment_same_line](./conformance_prettier.md#comment-position-philosophy): a same-line `//` is a deliberate authoring choice tsv preserves. A variable declarator `=` keeps it trailing in **both** formatters — prettier relocates only for the type alias, so tsv is the self-consistent side. Block-after-`=` and own-line-after-`=` comments match prettier in both constructs (see the plain `rhs_leading_comment` / `rhs_break_leading_comment_long` fixtures) — [rhs_line_comment_same_line](../tests/fixtures/typescript/types/aliases/rhs_line_comment_same_line_prettier_divergence/)
- Return/throw grouping `(`→argument same-line line comment → Onto its own line below `(` (`return (⏎// c⏎a`); tsv keeps it trailing the `(` (`return ( // c⏎a`). A leading comment forces the parens to break either way (the argument is a restricted-production operand), so the only question is whether the comment leads the argument line or trails `(` — a same-line `//` is a deliberate authoring choice tsv preserves, the open-paren analog of the type-alias `=` [rhs_line_comment_same_line](#comment-relocation) above. Assignment / sequence / plain-expression arguments behave alike, in both `return` and `throw`. The **own-line** authoring matches prettier (not a divergence — sibling `value_leading_comment_parens`) — [keyword_open_paren_line_comment](../tests/fixtures/typescript/syntax/comments/keyword_open_paren_line_comment_prettier_divergence/)
- `yield`/`yield*` grouping `(`→argument same-line line comment → the third restricted production, sharing the hanging-paren layout (`build_restricted_production_paren_doc`): tsv keeps the comment trailing the `(` (`yield ( // c⏎a`), and renders a sequence operand bare (`yield ( // c⏎a, b`). Prettier relocates it differently than the return/throw sibling above — it trails the comment on the **keyword** with the argument below (`yield // c⏎(a = b)`) and strips a now-redundant grouping pair around a plain identifier (`yield // c⏎a`), so `yield` has its own fixture rather than folding into the return/throw one — [yield_open_paren_line_comment](../tests/fixtures/typescript/syntax/comments/yield_open_paren_line_comment_prettier_divergence/)
- Array element end-of-line block comment → Across the element's comma, from leading the next element to trailing the previous one (`['aaaa', /* c */⏎'bbbb']` → `['aaaa' /* c */, 'bbbb']`, flipping the binding from `'bbbb'` to `'aaaa'`); prettier classifies on newlines alone (`endOfLine`), so the comma — which carries the association — plays no part. tsv keeps the comment after the comma. **Not** the sanctioned pure-separator trail: that covers a same-line *line* comment (`A // c⏎, B` → `A, // c`), where the binding never changes and a `//` running to end-of-line leaves no other rendering; a block comment flips its binding and renders fine either side, so the move is unforced. One rule across every comma list — the array literal, both destructuring patterns, the type-parameter **declaration** list, the import/export specifier list, the **object literal**, a call's **argument** list and a **parameter** list all read own-line-ness on the **source**, so the comma the author pushed onto its own line (`[a⏎, /* c */⏎b]`) is the same authoring one notch further and takes the same normalization; reading the item BOUNDARY instead expanded the list, a third fixed point neither side produces. Own-line-ness is **two**-sided and the trailing half is the same rule: a comma the author wrote right after the `*/` (`[a⏎/* c */,⏎b]`) ends that line, so the comment is glued and the list still flattens — an item-boundary reading there was *non-idempotent* rather than merely divergent, since the reprint's `/* c */ b` finally put an item on the comment's line and the second pass collapsed. The glue question is asked **per comment** (`Printer::comment_hugs_next`), which is what leaves a pair the author glued and gave its own line (`<A,⏎/* c1 */ /* c2 */⏎B>`) unforced: its head hugs its neighbour, the neighbour has content before it, and neither is isolated — so the run takes the emitter's soft `line` and the list still flattens when it fits, as prettier's does. Four gates ask that one question — `Printer::has_own_line_block_comments_in_bracket_list` (bracketed lists), `Printer::has_standalone_block_comment` (object literal), `should_force_expansion_for_comments` + `PartitionedComments::for_item_gap` (call arguments) and `Printer::has_own_line_comment_between` (parameter lists) — and the seam they sit over (`Printer::trailing_comment_run`) reads the same source, since an item-boundary gate's blindness *cancels* the seam's and tightening either alone loses a comment — [array](../tests/fixtures/typescript/expressions/arrays/end_of_line_block_comment_prettier_divergence/), [destructuring](../tests/fixtures/typescript/expressions/destructuring/end_of_line_block_comment_prettier_divergence/), [type params](../tests/fixtures/typescript/types/type_params/end_of_line_block_comment_prettier_divergence/), [specifier](../tests/fixtures/typescript/modules/imports/specifier_end_of_line_block_comment_prettier_divergence/), [object](../tests/fixtures/typescript/expressions/objects/property_end_of_line_block_comment_prettier_divergence/), [call argument](../tests/fixtures/typescript/expressions/calls/argument_end_of_line_block_comment_prettier_divergence/), [param](../tests/fixtures/typescript/declarations/function/param_end_of_line_block_comment_prettier_divergence/)
- Specifier glued PAIR given its own line → prettier's relocation stops being position-only here: it SPLITS the run across the comma and **reorders** it (`{ a,⏎/* c1 */ /* c2 */⏎b }` → `{ a /* c2 */, /* c1 */ b }`, dual-stable), so the authored order comes out reversed and the relation between the two comments is lost. tsv keeps the run whole, in source order, after the comma — the same normalization the single-comment entry above describes, which for a pair is also the only rendering that preserves what the author wrote. The glued run is not an own-line comment in either formatter's model: only its head has a newline before it and only its tail one after, so prettier's own `printLeadingComment` gives it the soft `line` that collapses when the list fits — [specifier_glued_pair_own_line](../tests/fixtures/typescript/modules/imports/specifier_glued_pair_own_line_prettier_divergence/)
- Array element end-of-line block comment, **across an elision** → The rule above with a hole between the comment and the element it leads. Prettier pulls a comment written after the element's comma back across it (`[x, /* c */ , y]` → `[x /* c */, , y]`), flipping the binding from `y` to `x`; tsv slides it *forward* past the anonymous elision commas to the element it leads (`[x, , /* c */ y]`). An elision is a **slot**, not an element — it prints nothing and its comma is re-emitted structure outside every element span, the same status the base rule gives a comma the author pushed onto its own line — so the forward slide is lossless and leaves the binding alone; the backward move is the one refused, direction rather than distance being the rule. Dual-stable both sides (`variant_before_comma`), so the divergence is in normalization exactly as above. Order survives the slide: prettier's per-comment classification splits a two-comment run across the comma and returns it **reversed** (`[x⏎/* c1 */, /* c2 */ , y]` → `[x /* c2 */, , /* c1 */ y]`) — [sparse_hole_after_comma_comment](../tests/fixtures/typescript/expressions/arrays/sparse_hole_after_comma_comment_prettier_divergence/)
- Spread stripped-paren interior comment **before an elision** → A **normalization-path** divergence, the fixed point shared: the array's share of a spread's redundant grouping parens (`[...(b⏎/* c */⏎), , d]`) slides past the following elision commas like every other hole-region comment, so tsv reaches `[...b,⏎,⏎/* c */⏎d]` in ONE pass; prettier emits the share against the spread's own comma and moves it on the next pass, converging on the same form in two. tsv used to print prettier's first form, which made its own output disagree with its reprint of it — two fixed points for one document. An authored blank *inside* the parens is dropped when the share is deferred, since a hole carries no blank line after it (`has_blank_line_after_slot`, prettier's `node &&`) — the position the blank named is gone — [spread_paren_comment_before_elision](../tests/fixtures/typescript/expressions/arrays/spread_paren_comment_before_elision_prettier_divergence/)
- Array **trailing-elision** block comment → The same rule where a trailing hole leaves no next element to lead, so the run slides to the array's own trailing position (`[x, ,/* c */]`); prettier pulls it back before the last real element's comma (`[x /* c */, ,]`). That position is the one `sparse_block_comment_inline` already pins for an array holding no real element at all (`[, , , ,/* c */]`, where prettier agrees) — an earlier real element does not change where the comment was written. An own-line comment there takes its own line and expands the array in both formatters, which differ only in ordering it against the elision comma — [sparse_trailing_hole_comment](../tests/fixtures/typescript/expressions/arrays/sparse_trailing_hole_comment_prettier_divergence/), [array_trailing_elision_comment](../tests/fixtures/typescript/expressions/destructuring/array_trailing_elision_comment_prettier_divergence/) (the array-PATTERN face, one rule: a comment authored either side of the elision comma reaches the same trailing position, and no blank is measured across the hole — the elision's own line break is structure, and reading it as authorship made the form non-idempotent)
- Type-list item block comment glued to a **stripped paren shell**'s `(` → The same relocation across the comma, reached without an authored newline: the shell is redundant and both formatters erase it, so `Foo<T,⏎(/* c */⏎U)>` leaves the comment in the list's own gap and prettier moves it to `Foo<T /* c */, U>` where tsv keeps it after the comma (`Foo<T, /* c */ U>`). Dual-stable both sides, exactly as the array rule above (`variant_before_comma`). One rule across a tuple element and a type argument in both type and call position — all three ask own-line-ness on the **source** rather than on the item boundary, since their item spans no longer cover the `(` that occupied the comment's line (`Printer::has_own_line_block_comments_in_bracket_list`, `crates/tsv_ts/src/printer/mod.rs`); on the boundary reading the list would *expand* instead, a third fixed point neither the bare authoring nor prettier produces — [type_position_parens_glued_block_comment](../tests/fixtures/typescript/types/type_position_parens_glued_block_comment_prettier_divergence/)
- Binding-default **stripped paren shell** interior, own-line block (`a = (1⏎/* c */)`) → The same relocation across the comma, at the tail of the shell rather than its `(`: both formatters erase the shell, so the comment lands in the list's own gap, and tsv slides it **forward** to lead the next element (`const { a1 = 1, /* c */ b1 } = x`) where prettier hoists it **backward** across the `=` and the binding name to lead the element it was written in (`const { /* c */ a1 = 1, b1 } = x`). Dual-stable both sides — a normalization-path divergence, like the entries above. One rule across the object pattern, the array pattern and a parameter list; prettier changes its answer with what the shell wraps, agreeing with tsv's forward slide wherever the shell wraps the **whole** element (`[(1⏎/* c */), 2]`, `fn((1⏎/* c */), 2)`) and reversing only for the `AssignmentPattern` shape, where it attaches the comment to that node and prints it at its front. The **same-line** spelling of the same shell is a plain match ([stripped_paren_interior_comment](../tests/fixtures/typescript/expressions/destructuring/stripped_paren_interior_comment/), [param_default_stripped_paren_comment](../tests/fixtures/typescript/declarations/function/param_default_stripped_paren_comment/)) — the anchor that claims it is the element's PRINTED end (`docs/comments.md` §The element-comma seam) — [stripped_paren_interior_own_line_comment](../tests/fixtures/typescript/expressions/destructuring/stripped_paren_interior_own_line_comment_prettier_divergence/). An author **blank line ahead of** that comment (`a = (1⏎⏎/* c */)`) rides along with it under the same rule, because the blank is authorship rather than shell structure: it separated the comment from the value in the authored form and separates it from the item above in the erased one. The scan that decides this is the seam's *distance* half (`Printer::element_shell_end`), which steps over the erased `)` and whitespace but **stops at the first comment** — so a blank with nothing but shell behind it is erased with the shell, a plain match with prettier ([stripped_paren_interior_blank](../tests/fixtures/typescript/expressions/destructuring/stripped_paren_interior_blank/)), while one written ahead of a comment survives. It needs a line to survive *on*, though, and the comment supplies none: the comment's own line ends with the erased `)` and the comma, so it is glued and forces nothing open, in every family alike — the list must already be open for a reason of its own — [stripped_paren_interior_blank_before_comment](../tests/fixtures/typescript/expressions/destructuring/stripped_paren_interior_blank_before_comment_prettier_divergence/)
- Indexed-access `[`→index **multiline** block → Out before `[` (`A /* c⏎d */[K]`), which **changes what the code means**: a type's index suffix may not follow a line break (TypeScript's `parsePostfixTypeOrHigher` stops at `scanner.hasPrecedingLineBreak()`), and the comment's *interior* newline supplies one — so Prettier's relocated form parses as `type X = A;` plus a separate `ArrayExpression` statement, not an indexed access. The canonical parser agrees: `A /* c */[K]` → `TSIndexedAccessType`, `A /* c⏎d */[K]` → `TSTypeReference` + `ExpressionStatement`. Prettier reaches it in two passes and then re-prints the changed tree as a third form, so it has **no** stable rendering of this construct — unlike the single-line sibling below, whose relocation carries no newline and is therefore safe and dual-stable. tsv keeps the comment inside the brackets and hangs the index. Content preservation, not a position preference — [indexed_access_own_line_multiline_block_comment](../tests/fixtures/typescript/types/indexed_access_own_line_multiline_block_comment_prettier_divergence/)
- **Type-suffix trailing line comment / run**, at the end of an indexed access's brackets (`T[K // c1⏎]`) or of a redundant paren shell around a type (`(A // c2⏎)`) → Carried **out of the construct**, past the `=` onto the initializer's line (`const a: T[K] = // c1⏎↹y;`) or past the `;` (`type A = T[K]; // c1⏎// c2`), the shell stripped on the way; tsv keeps the comment inside the region the author wrote it in and drops the closer to its own line.
- **Type-suffix trailing line comment, member position** → the member-position face of the entry above. An indexed access that is a **union member** keeps the comment inside its brackets (`| T[⏎↹↹K // c1⏎↹]`); prettier trails it on the member (`| T[K] // c1`), re-binding it from the index to the whole member. A redundant paren shell around the **last** (or sole) member of a union / intersection is **retained** (`| B⏎| (⏎↹A // c2⏎↹)`); prettier strips it and carries the comment past the `;` — and its form there is **non-idempotent**: the escaped comment is all that held the union broken, so prettier's second pass collapses `type A2 =⏎↹| B⏎↹| A; // c2` to `type A2 = B | A; // c2` (pinned by the fixture's `audit_signature.txt`). A **non-last** member's shell still strips and defers, matching prettier (`(a // c⏎) | b` → `| a // c⏎| b`, [union_intersection_parens_line_comment](../tests/fixtures/typescript/types/union_intersection_parens_line_comment/)): that carve-out's argument is exactly that the next separator's per-member break ends the output line where the shell ends, flushing the deferred comment on the member it was written in — so it is keyed on a following `|`/`&` in source (`Printer::type_member_separator_follows`) and stops at the last member, where only the statement's tail follows — [type_suffix_trailing_comment_union_member](../tests/fixtures/typescript/types/type_suffix_trailing_comment_union_member_prettier_divergence/) **The four sibling bracketed type regions already answer this tsv's way in both formatters** — a type literal's `}`, a type-argument list's `>`, a tuple's `]`, a function type's `)` all keep a trailing comment inside and break the closer onto its own line, byte-identically — so prettier answers one question two ways and tsv answers it once; a **value**-position redundant paren is already retained for the same reason (`const e = (⏎x // c⏎);`), which makes the two sides of the paren rule agree too. Carrying the comment out is not lossless: it re-binds the comment from the index (or the parenthesized type) to the whole statement, and lands it on a line that may already hold one, where the run renders back to back and the second `//` becomes text of the first (`const a: T[K] = 1; // c1 // c2`) — irreversibly, since the merged form is a fixed point in both formatters. Keeping the comment inside is what makes that collision unreachable rather than something the renderer must defuse — [type_suffix_trailing_comment](../tests/fixtures/typescript/declarations/variable/type_suffix_trailing_comment_prettier_divergence/), [type_suffix_trailing_comment_run](../tests/fixtures/typescript/types/type_suffix_trailing_comment_run_prettier_divergence/)
- **Type-suffix trailing line comment, nested-composite member position** → the sanctioned non-last-member strip one composite deeper: the shell ends an **intersection inside a union member** (`B & (A // c1⏎) | C`), directly or through further redundant layers. Both formatters converge on the same fixed point — the comment trails the whole member and the intermediate composite prints flat (`| (B & A) // c1⏎| C`) — but prettier's first pass also breaks that intersection (its `breakParent` forces every enclosing group), a break its own reparse cannot reproduce once the comment sits in the union's member gap, so prettier needs **two passes**; tsv emits the fixed point in one, the deferred comment forcing only the group it actually flushes in. A divergence in the normalization path, not the form — [type_suffix_trailing_comment_nested_composite](../tests/fixtures/typescript/types/type_suffix_trailing_comment_nested_composite_prettier_divergence/). The **retained-paren** neighbour (`B & (A | (C // c1⏎)) & D` — the shell on the last member of a parenthesized union inside an intersection) converges the same way onto tsv's already-sanctioned union-fit form: the shell strips, the deferred comment flushes before the retained `)`, and the inner union re-fits inline where prettier keeps it exploded — the [union_intersection_retained_paren_line_comment](../tests/fixtures/typescript/types/union_intersection_retained_paren_line_comment_prettier_divergence/) divergence, whose fixture carries the intersection-position case and this authoring
- Array-type suffix `[`→`]` → Out in front of the brackets (`string[/* c */]` → `string /* c */[]`), re-binding the comment from the suffix to the element type; for a line comment, out of the declaration entirely (`type B = string[];⏎// c`), where it reads as leading whatever statement follows. tsv keeps it inside the brackets the author wrote it in, routing the pair through the same empty-brackets emitter the **empty tuple type** uses — `type A = [/* c */]` keeps its comment inside and a `//` there breaks the brackets open, and both formatters already agree on that, so preserving here makes the two bracket forms answer one question one way. The suffix's own `[]` becoming a break point also makes the array hug the type-alias `=` like every other empty container (`type B = string[⏎↹// c⏎]`, as `= [` / `= {` already do). The gap **before** the brackets can hold only a single-line block comment, which both formatters keep in place (`string /* c */[]`): a `//` or a multiline block puts a line break in front of the `[`, and by the same `hasPrecedingLineBreak()` rule as the indexed-access entry above the construct is then no longer an array type at all. A chain of suffixes keeps each comment with its own pair. Not preserving drops it — the region had no emitter — [array_bracket_comment](../tests/fixtures/typescript/types/array_bracket_comment_prettier_divergence/)
- Array-type suffix `[`→`]`, **parenthesized** element → The same hoist one construct over (`(X & Y)[/* c */]` → `(X & Y) /* c */[]`, a `//` out of the declaration entirely); tsv keeps it inside the brackets, through the same empty-brackets emitter. The parens change what *precedes* the suffix, not what the suffix is. The `)`→`[` gap is a **match** — a single-line block comment written there stays there in both (`(X & Y) /* c */[]`), including after the `)` tsv synthesizes for an element the author left bare (`typeof x /* c */[]` → `(typeof x) /* c */[]`), and a `//` or multiline block cannot occur in that gap at all by the same `hasPrecedingLineBreak()` rule (the plain fixtures [array_paren_before_bracket_comment](../tests/fixtures/typescript/types/array_paren_before_bracket_comment/) and, for the layout boundary, [array_paren_before_bracket_comment_long](../tests/fixtures/typescript/types/array_paren_before_bracket_comment_long/)). A **hugged** union element takes this route; an expanding one takes the entry below — [array_paren_bracket_comment](../tests/fixtures/typescript/types/array_paren_bracket_comment_prettier_divergence/). At the width boundary the suffix's own `[]` is what breaks under tsv, the element staying put, while prettier — out of in-declaration room for its relocated form — strands the **block** comment on its own line after the `;`, the same escape its line comment already makes — [array_paren_bracket_comment_long](../tests/fixtures/typescript/types/array_paren_bracket_comment_long_prettier_divergence/)
- Array-type suffix `[`→`]`, **expanding parenthesized union** element → Prettier's destination flips to the other side of the `)`: into the parens, trailing the last union member (`('a' | 'b')[/* c */]` → `('a' | 'b' /* c */)[]`), re-binding it from the suffix to that member — and a comment authored in the `)`→`[` gap lands there too, so two authorings collapse onto one form. The choice is keyed on the element's **layout**, not its kind: a union that prints hugged (`(T | null)[]`) takes the entry above instead, so prettier answers one gap two ways. tsv keeps each comment where it was written in both — [array_paren_union_bracket_comment](../tests/fixtures/typescript/types/array_paren_union_bracket_comment_prettier_divergence/)
- Block comment in computed `[]` → Before member chain (hoisted) — [block_comment_computed_member_long](../tests/fixtures/typescript/syntax/comments/block_comment_computed_member_long_prettier_divergence/)
- Computed member-access `[]` leading comment run (bracket-break path, incl. `?.[`) → Prettier empties the brackets: the whole run is hoisted out to before the object and the `[`-line comment is stranded between the object and its index (`/* c1 */ /* c2 */⏎obj // force⏎[idx]`), so each comment changes what it reads as being about; tsv keeps all of them inside the brackets the author wrote them in — [computed_index_glued_block_comment](../tests/fixtures/typescript/expressions/member/computed_index_glued_block_comment_prettier_divergence/)
- Switch case colon comment → Before colon or into body — [case_colon_comment](../tests/fixtures/typescript/statements/switch/case_colon_comment_prettier_divergence/)
- Switch case/`default` colon, comment contains a colon (scan robustness) → `case` preserved (plain); `default` into the body — [case_colon_in_comment](../tests/fixtures/typescript/statements/switch/case_colon_in_comment_prettier_divergence/)
- Switch `case`→test line comment → In place flat; tsv indents. **Placement matches** — prettier keeps every comment in this gap where the author wrote it too, so the divergence is the continuation's indent alone: the test drops one level in under tsv ([§Uniform Forced-Continuation Indent](./conformance_prettier.md#uniform-forced-continuation-indent)) and stays flush at the case's own indent under prettier. The break is not a layout choice on either side — a `//` runs to end-of-line, so inlining the gap would swallow the test *and* its `:` (`case // c x:`, which does not reparse), the head→`:` entry's argument one step earlier in the same label. Covers a test whose parens are the **printer's** to re-synthesize (`case // c⏎(y, z):`), where the gap's emitter is the only thing that prints the comment at all; a **run**, which stays in order with each comment on its own line and distinct; and a **block ahead of the line comment** (`case /* c5 */ // c6⏎v:`), which keeps its place in the run rather than reordering across it — a block prints inline where a line comment defers, so emitting the two apart would put the block behind the `//` it was written in front of. The same gap's **block** comment forces no break, collapses inline, and matches ([case_test_gap_block_comment](../tests/fixtures/typescript/statements/switch/case_test_gap_block_comment/)) — [case_test_gap_line_comment](../tests/fixtures/typescript/statements/switch/case_test_gap_line_comment_prettier_divergence/)
- Switch case head→`:` line comment → Across the `:`, trailing the label line (`case x: // c`); a **run** split across the boundary — first comment trailing the label, the rest re-bound into the body leading the first consequent statement, so one authored run lands in two syntactic positions. tsv keeps the comment after the head and drops the `:` to a continuation line indented one level, the run in place, in order, each comment distinct (emitting the gap inline would swallow the colon into the comment — `case x // c:` does not reparse; the same content-preservation argument as the switch `)`→`{` gap above). The own-line authoring goes into the body under prettier (dual-stable `variant_own_line`), while tsv pulls it up to the same continuation form — [case_before_colon_line_comment](../tests/fixtures/typescript/statements/switch/case_before_colon_line_comment_prettier_divergence/)
- Switch case head→`:` **own-line block** comment → Into the body, re-bound to the first consequent statement (glued to it from the inline authoring — the already-cataloged `default /* c */:` move above; on its own line leading it from the own-line authoring, dual-stable `variant_own_line`); tsv collapses the unforced breaks in one pass, keeping the comment trailing the head in its authored gap (`case x /* c */:` — for a tested `case`, the inline form prettier itself produces from the after-`:` authoring). A **multiline** block in this gap stays glued to the `:` in both formatters — the broke-after continuation rule is scoped to value-separator gaps, and prettier does not distinguish the broke-after authoring here either. A labeled statement's name→`:` gap is outside all of this: both formatters hoist a line or own-line-block comment there to lead the whole statement (a match, like the mapped-type modifier→`[` hoist) — [case_before_colon_own_line_block_comment](../tests/fixtures/typescript/statements/switch/case_before_colon_own_line_block_comment_prettier_divergence/)
- Class property definite `!`, no type annotation (`d! /* c */ = 1`) → Prettier **rejects** the input (TS1264: a definite-assignment assertion requires a type annotation — 3.9.6 enforces it at parse time; ≤3.9.5 relocated the comment before `!`). tsv defers the early error (permissive parser, tsc is the oracle) and preserves the comment where authored — a `prettier_rejects` fixture, [property_definite_comment](../tests/fixtures/typescript/statements/class/property_definite_comment_prettier_divergence/)
- Class property definite `!`, **with** type annotation + initializer (`e!: number /* c */ = 1`) → Prettier **rejects** the input for the complementary reason (TS1263: an initializer cannot accompany a definite-assignment assertion — 3.9.6 enforces it at parse time; ≤3.9.5 relocated the comment before `!`). tsv defers the early error and preserves the value-gap comment where authored — a `prettier_rejects` fixture, [property_definite_typed_comment](../tests/fixtures/typescript/statements/class/property_definite_typed_comment_prettier_divergence/)
- Class property `?` modifier, no type annotation (`a? /* c1 */ = 1`) → Before `?` modifier. An optional property with an initializer and no annotation is valid TS, so this relocation stays live in 3.9.6; the parallel definite `!` form is the TS1264 early error above (`property_definite_comment`), not a relocation — [property_modifier_comment](../tests/fixtures/typescript/statements/class/property_modifier_comment_prettier_divergence/)
- Between member modifiers → After the last modifier — [modifier_pair_comment](../tests/fixtures/typescript/declarations/class/modifier_pair_comment_prettier_divergence/)
- Between parameter-property modifiers → After the last modifier (same rule as class members) — [param_property_modifier_pair_comment](../tests/fixtures/typescript/declarations/class/param_property_modifier_pair_comment_prettier_divergence/)
- Between class-*member* decorators, block comment on the next decorator's line → Prettier pushes it to its own line (non-idempotent on the all-inline `@fn1 /* c */ @fn2`: leads inline on pass 1, own-line on pass 2); tsv leads the next decorator inline (`@fn1⏎/* c */ @fn2`), stable and consistent with class-*level* decorators (`@a⏎/* c */ @b`, which prettier keeps inline too) — [member_decorator_lead_next_comment](../tests/fixtures/typescript/statements/class/member_decorator_lead_next_comment_prettier_divergence/)
- Generator method `async`→`*` (class + object shorthand) → After the `*`, before the name (`async */* c */ m()`) — [async_star_comment (class)](../tests/fixtures/typescript/statements/class/async_star_comment_prettier_divergence/), [(object)](../tests/fixtures/typescript/expressions/objects/async_star_comment_prettier_divergence/)
- Async arrow `async`→parameters block comment → Into the arrow **body** when the parameter list is empty (`async () => /* c */ {}`), into the **parameter list** when it has one (`async (/* c */ x) => x`), and left **in place** before a type-parameter list (`async /* c */ <T extends A>(x: T) => x`) — one gap, three destinations keyed on what follows `async`, so prettier's placement is no oracle here; tsv preserves the authored position in all three, a run in order. Only a **single-line block** comment can occupy this gap: every other kind puts a line terminator between `async` and the parameters, which `AsyncArrowFunction : async [no LineTerminator here] ArrowFormalParameters` (ecma262) forbids — those rejections are pinned by the plain [async_line_break](../tests/fixtures/typescript/expressions/arrow/async_line_break/), whose single-parameter case is the one shape a line break makes *valid* rather than invalid (ASI splits it into an `async` identifier reference and a separate arrow statement, as prettier prints it too). Not preserving drops the comment — the gap has no other emitter — [async_before_params_comment](../tests/fixtures/typescript/expressions/arrow/async_before_params_comment_prettier_divergence/)
- Function-head modifier (`async` / `declare`)→`function` keyword gap → After the keyword, before the name (`async function /* a */ a()`) when the function is named, into the **body** (`async function () /* c */ {}`) when it is not — two destinations keyed on whether a name follows, so again no oracle; tsv preserves between the two keywords, the answer its `abstract`→`class` sibling above already gives. The declaration, the expression and the bodiless overload signature are printed by three different builders and used to disagree: the declaration preserved while the other two emitted a bare `async ` and let the gap fall through to the next emitter (the keyword→name gap for a named function, the parameter list for an anonymous one), landing the comment on prettier's side of `function`. All three share one head emitter now (`Printer::push_function_keyword_head`), which owns the modifier itself — a caller that printed it and left the gap behind it to the next emitter is what orphaned the region — [async_function_keyword_comment](../tests/fixtures/typescript/syntax/comments/async_function_keyword_comment_prettier_divergence/)
- `function`→generator `*` gap → Into the **body** for a function *expression* (`function* () /* b */ {}`); a *declaration* keeps it after the `*`, where tsv normalizes both authorings (spaced and glued) and where prettier agrees. Preserving it *before* the `*` — what the `async`→`*` method gap above does — would make the declaration diverge too, so it is filed rather than taken. The glued spelling used to **drop** the comment: the `*` was stepped over as `cursor + 1` rather than found, landing the cursor inside the comment so the next emitter's range began past its start — [function_generator_star_comment](../tests/fixtures/typescript/syntax/comments/function_generator_star_comment_prettier_divergence/)
- Interface member after `?` → Before `?` or inside parens — [modifier_after_comment](../tests/fixtures/typescript/types/type_members/modifier_after_comment_prettier_divergence/)
- Type-literal member after `?` → Before `?` or inside parens — [optional_marker_comment](../tests/fixtures/typescript/types/type_literal/optional_marker_comment_prettier_divergence/)
- Member after `?`, no annotation (iface + type-literal) → Before `?` (`a /* c */?;`) — [property_signature_no_annotation_optional_comment](../tests/fixtures/typescript/types/type_members/property_signature_no_annotation_optional_comment_prettier_divergence/)
- Class method after `?` → Before `?` modifier — [optional_marker_comment](../tests/fixtures/typescript/declarations/class/optional_marker_comment_prettier_divergence/)
- Optional `?` to `:` line comment (all contexts) → Trailing the member `;` — [optional_marker_line_comment](../tests/fixtures/typescript/syntax/comments/optional_marker_line_comment_prettier_divergence/). An own-line `//` authoring pulls up to the same continuation form under tsv, while prettier reaches the trailing form via an unstable pass across the `:` (pinned `prettier_intermediate_to_variant_own_line` → `variant_own_line`)
- Optional `?` to `:` **own-line block** comment (iface/type-literal/class) → Prettier converges to its cataloged before-`?` relocation (`a /* c1 */?: number` — the after-`?` entries above) in two passes — including the class property it leaves alone when the comment is authored inline — via an unstable first pass across the `:` (`a?: /* c1 */⏎number`); tsv collapses the unforced breaks in one pass, keeping the comment trailing the marker in its authored gap (`a? /* c1 */ : number`). The landing is pinned dual-stable as `variant_own_line` (a comment *authored* before `?` is a match in both — `optional_marker_before_comment`) — [optional_marker_own_line_block_comment](../tests/fixtures/typescript/syntax/comments/optional_marker_own_line_block_comment_prettier_divergence/)
- Member key to `:` line comment (non-optional) → Trailing the member `;` — [key_colon_line_comment](../tests/fixtures/typescript/syntax/comments/key_colon_line_comment_prettier_divergence/). An own-line `//` authoring pulls up to the same continuation form under tsv, while prettier reaches the trailing form via an unstable pass across the `:` (pinned `prettier_intermediate_to_variant_own_line` → `variant_own_line`)
- Member key to optional `?` line comment (iface/type-literal/class/method) → Trailing the member `;`; tsv keeps the comment after the key and drops `?` + the rest of the member to a continuation line — [key_optional_marker_line_comment](../tests/fixtures/typescript/syntax/comments/key_optional_marker_line_comment_prettier_divergence/)
- Member key to `:` **own-line block** comment (iface/type-literal/class) → Prettier reaches tsv's fixed point non-idempotently over three passes (across the `:` leading the type — `a: /* c1 */⏎number` — then re-glued inline, then the class property's spacing); tsv collapses the unforced breaks in one pass, keeping the comment trailing the key in its authored gap (`a /* c1 */: number`, class `c /* c3 */ : number = 1` — the inline-authored form both formatters hold stable). A pass-count gap, not an end-state divergence; the chain (two intermediates) is README-recorded, one past what a `prettier_intermediate_*` pin can carry — [key_colon_own_line_block_comment](../tests/fixtures/typescript/syntax/comments/key_colon_own_line_block_comment_prettier_divergence/)
- Binding/param/index-signature-key to `:` **own-line block** comment → The annotation-colon siblings of the member-key entry above, spaced per the sites' inline parity (`let x /* c */ : string`). tsv collapses in one pass; prettier's chain splits by count: pass 1 pulls the first comment up but keeps the break before `:` (parameter lists and index-signature brackets stay expanded), then a **single** comment collapses to tsv's form on pass 2 while a **run** parks stable with its tail comments still own-line — pinned as `prettier_variant_own_line` plus the unstable first pass as `prettier_intermediate_to_variant_own_line` — [binding_key_colon_own_line_block_comment](../tests/fixtures/typescript/declarations/variable/binding_key_colon_own_line_block_comment_prettier_divergence/), [param_key_colon_own_line_block_comment](../tests/fixtures/typescript/declarations/function/param_key_colon_own_line_block_comment_prettier_divergence/), [index_signature_key_colon_own_line_block_comment](../tests/fixtures/typescript/types/type_members/index_signature_key_colon_own_line_block_comment_prettier_divergence/)
- Initializer/value gap block comment with an **authored break** (`A = /* c */⏎1`) → Both formatters reflow the value onto the operator's line (the [§Authored breaks in value position](./conformance_prettier.md#authored-breaks-in-value-position) rule; the already-inline form is byte-identical in both, so the divergence rides the `unformatted_ours_break` variant), but prettier also **relocates** the comment — before the `=` for an enum member ([member_init_block_comment_break](../tests/fixtures/typescript/declarations/enum/member_init_block_comment_break_prettier_divergence/)), a class property ([property_init_block_comment_break](../tests/fixtures/typescript/declarations/class/property_init_block_comment_break_prettier_divergence/)), and a parameter default ([param_default_block_comment_break](../tests/fixtures/typescript/declarations/function/param_default_block_comment_break_prettier_divergence/)); hoisted onto its own line above the property for an object value ([value_block_comment_break](../tests/fixtures/typescript/expressions/objects/value_block_comment_break_prettier_divergence/)). tsv preserves the authored position in every case. A variable declarator agrees with prettier outright (no relocation there)
- Value gap block comment with an authored **blank line** (`A = /* c */⏎⏎1`) → Prettier keeps the blank (its own fixed point); tsv reflows it, because the blank sits inside a break already judged unforced and a blank line is a property of a line break (the [§Authored breaks in value position](./conformance_prettier.md#authored-breaks-in-value-position) rule). The three initializer fixtures above carry it as their `unformatted_ours_blank` variant; the object value additionally pins prettier's blank-preserving hoist as `variant_hoisted_blank`. A *list* gap is excluded and keeps its blank — [arrays/end_of_line_block_comment](../tests/fixtures/typescript/expressions/arrays/end_of_line_block_comment_prettier_divergence/)
- Enum member name to `=` line comment → After the value (`A = 1 // c`); tsv keeps it after the name + continuation indent. With a second trailing comment prettier merges both onto one line (info loss); tsv keeps them distinct — [member_before_eq_line_comment](../tests/fixtures/typescript/declarations/enum/member_before_eq_line_comment_prettier_divergence/). An own-line `//` authoring pulls up to the same form under tsv, while prettier reaches its trailing form via an unstable glued pass (`A = // c⏎1` — pinned `prettier_intermediate_to_variant_own_line` → `variant_own_line`)
- Class property name to `=` line comment → Trailing the member `;` after the value (`a = 1; // c`); tsv keeps it in place + continuation indent. Two comments → prettier merges (info loss), tsv keeps distinct — [property_before_eq_line_comment](../tests/fixtures/typescript/declarations/class/property_before_eq_line_comment_prettier_divergence/). An own-line `//` authoring pulls up to the same form under tsv, while prettier hangs the comment leading the value (dual-stable `variant_own_line`)
- Variable binding to `=` line comment → Trailing the statement `;` after the value (`const a = 1; // c`); tsv keeps it in place + continuation indent. Two comments → prettier merges (info loss), tsv keeps distinct — [declarator_before_eq_line_comment](../tests/fixtures/typescript/declarations/variable/declarator_before_eq_line_comment_prettier_divergence/). An own-line `//` authoring pulls up to the same form under tsv, while prettier hangs the comment leading the value (dual-stable `variant_own_line`). A **for-init** declarator's gap answers identically — prettier floats to the end of the init clause's line and merges a second comment there the same way (`let b = 0; // c1 // c2`) — [init_binding_equals_line_comment](../tests/fixtures/typescript/statements/for/init_binding_equals_line_comment_prettier_divergence/). The gap's **block** comment stays in place in both formatters at either site (the non-divergent [init_binding_equals_comment](../tests/fixtures/typescript/statements/for/init_binding_equals_comment/)); the for-init gap had **no emitter at all** and dropped every comment kind in it. An **assignment expression**'s target→operator gap is the same rule with the author's operator carried onto the continuation (`a // c⏎\t= 1`, `c // c⏎\t+= 4`), member targets included — the one member of this family that used to relocate, printing the comment *after* the operator — [before_operator_line_comment](../tests/fixtures/typescript/expressions/assignment/before_operator_line_comment_prettier_divergence/)
- Type alias name to `=` line comment (non-union values) → Across the `=`, hung leading the RHS (`type A =⏎\t// c⏎\tnumber;`), reached non-idempotently from the forced-break authoring via an unstable glued pass (`type A = // c⏎\tnumber;` — the `output_prettier` + `audit_signature` chain from input, and `prettier_intermediate_to_variant_spaces` → `variant_own_line` from the flush authoring); tsv keeps it after the name + continuation indent. Unlike the declarator/enum/property faces above, prettier's landing here hangs the RHS rather than floating the comment to end-of-line, so a second trailing comment stays distinct in both formatters — a comment-position divergence, not a merge. With a **union** value prettier floats to end-of-statement instead (the merge-prone destination — the Type-alias-head-to-`=`-union entry below, [type_alias_line_pre_equals](../tests/fixtures/typescript/types/comments/type_alias_line_pre_equals_prettier_divergence/)) — [name_before_eq_line_comment](../tests/fixtures/typescript/types/aliases/name_before_eq_line_comment_prettier_divergence/). An own-line `//` authoring pulls up to the continuation form under tsv, while prettier reaches its hang in one pass (dual-stable `variant_own_line`)
- Name to `=` **own-line block** comment (enum member / class property / declarator / for-init declarator) → tsv collapses the unforced breaks in one pass, keeping the comment trailing the name (`A /* c */ = 1` — the inline-authored form both formatters hold stable, a plain match in `member_value_comment` / `property_equals_comment`), runs in order and each comment distinct. Prettier relocates across the `=` and hangs the comment leading the value (`a =⏎\t/* c */⏎\t1;`) — stable in one pass for the class property and declarator, pinned dual-stable as `variant_own_line` — while for the enum member that first pass is unstable and prettier walks back to tsv's fixed point, one comment per pass (2 passes single, 3 for a run — a pass-count gap, README-recorded like the member key→`:` entry above) — [member_before_eq_own_line_block_comment](../tests/fixtures/typescript/declarations/enum/member_before_eq_own_line_block_comment_prettier_divergence/), [property_before_eq_own_line_block_comment](../tests/fixtures/typescript/declarations/class/property_before_eq_own_line_block_comment_prettier_divergence/), [declarator_before_eq_own_line_block_comment](../tests/fixtures/typescript/declarations/variable/declarator_before_eq_own_line_block_comment_prettier_divergence/). A type alias's name→`=` gap takes the declarator's outcome exactly (`type A /* c */ = number` vs prettier's `type A =⏎\t/* c */⏎\tnumber`) — [name_before_eq_own_line_block_comment](../tests/fixtures/typescript/types/aliases/name_before_eq_own_line_block_comment_prettier_divergence/). The **for-init** declarator takes the declarator's outcome exactly, prettier breaking the header open around its hang — [init_binding_equals_own_line_block_comment](../tests/fixtures/typescript/statements/for/init_binding_equals_own_line_block_comment_prettier_divergence/).
- Pre-separator **multiline block, authored break kept** (key→`:`, name→`=` — declaration and for-init alike, name→annotation-`:`) → A multiline block the author **broke after** keeps its break — the comment trails the head and the separator + tail drop to a continuation line indented one level (the same landing as the gap's line-comment entry; the value-gap rule's pre-separator mirror — a break after a multiline block is authoring signal on both sides of the separator, unlike a single-line block's unforced breaks, which still collapse). One whose separator shares its closing line stays glued in both formatters. Prettier's broke-after destination varies by site: hoisted to the property's leading line (object property), relocated across the `=` and hung leading the value (declarator), kept trailing with a **flush** continuation (binding annotation — an indent-only divergence), or simply **glued**, with no distinction drawn between the two authorings at all (every binding default — object shorthand and non-shorthand, array element, standalone parameter) — [property_key_colon_multiline_block_break](../tests/fixtures/typescript/expressions/objects/property_key_colon_multiline_block_break_prettier_divergence/), [declarator_before_eq_multiline_block_break](../tests/fixtures/typescript/declarations/variable/declarator_before_eq_multiline_block_break_prettier_divergence/), [binding_key_colon_multiline_block_break](../tests/fixtures/typescript/declarations/variable/binding_key_colon_multiline_block_break_prettier_divergence/), [default_equals_multiline_block_break](../tests/fixtures/typescript/expressions/destructuring/default_equals_multiline_block_break_prettier_divergence/). Where prettier draws no distinction the rule is tsv's own rather than a mirror of prettier's, and it is what keeps the authored break honored on **both** sides of the separator: the binding default's value side already hangs a broke-after multiline block (`d = /* x⏎y */⏎\t\tv`), which prettier answers by relocating the comment back before the `=`. The rule rides the shared continuation seams (`build_marker_colon_line_continuation`, `build_initializer_line_continuation`, and the object-property / pattern / index-signature / mapped-type gap gates), so enum members, class properties, parameters, property signatures, optional markers, destructuring renames, every binding default, computed keys, and the type-alias head take it uniformly. The for-init declarator's gap answers as its statement-level sibling does — [init_binding_equals_multiline_block_break](../tests/fixtures/typescript/statements/for/init_binding_equals_multiline_block_break_prettier_divergence/) — and so does an **assignment expression**'s target→operator gap, which used to glue the broke-after authoring exactly as it relocated the line comment — [before_operator_multiline_block_break](../tests/fixtures/typescript/expressions/assignment/before_operator_multiline_block_break_prettier_divergence/).
- Binding default name to `=` **own-line block** comment (object/array/param destructuring + standalone param default) → Prettier **hoists** the comment to its own line leading the whole binding, expanding the pattern/list (`{⏎\t/* c1 */⏎\ta = 1⏎}` — a leading position, dual-stable `variant_own_line`); tsv collapses the unforced breaks in one pass, keeping the comment trailing the name (`{ a /* c1 */ = 1 }` — the inline-authored form both formatters hold stable, a plain match in `default_equals_comment`). A third destination in the same family: the `=`→value gap collapses to *after* `=` under tsv, *before* `=` under prettier (the `param_default_own_line_block_comment` entry under §Comment normalization) — [default_equals_own_line_block_comment](../tests/fixtures/typescript/expressions/destructuring/default_equals_own_line_block_comment_prettier_divergence/)
- Type parameter name to `extends` line comment → After `extends`, leading the constraint (`T extends // c⏎A`), re-binding the comment from the name to the constraint; tsv keeps it trailing the name and drops the `extends A` tail to a continuation line at one indent level (inlining would swallow the tail — the constraint becomes comment text). Identical in Svelte `{#snippet}` generics (shared type-parameter printer) — [type_param_before_extends_line_comment](../tests/fixtures/typescript/types/comments/type_param_before_extends_line_comment_prettier_divergence/), [snippet/ts_generic_constraint_gap_line_comment](../tests/fixtures/svelte/blocks/snippet/ts_generic_constraint_gap_line_comment_prettier_divergence/)
- Type parameter before-`=` default line comment → After `=`, leading the default (`T extends A = // c⏎B`); tsv keeps it in place + continuation indent — the type-parameter face of the before-`=` initializer family above (declarators, enum members, class properties) — [type_param_before_eq_line_comment](../tests/fixtures/typescript/types/comments/type_param_before_eq_line_comment_prettier_divergence/)
- Object property key to `:` line comment → Hoisted to its own line before the key (`// c⏎a: 1`); tsv keeps it after the key and drops `: value` to a continuation line — [property_key_colon_line_comment](../tests/fixtures/typescript/expressions/objects/property_key_colon_line_comment_prettier_divergence/). An own-line `//` authoring pulls up under tsv, while prettier hangs it leading the value (dual-stable `variant_own_line`) — two prettier destinations from one gap, keyed on the authoring
- Object-property / import-attribute key to `:` **own-line block** comment → Across the `:`, hung leading the value (`a:⏎\t/* c */⏎\t1`), re-binding it from the key's gap to the value; tsv collapses the unforced breaks and keeps the comment inline in its authored gap (`a /* c */: 1`, `type /* c */: 'json'` — the inline-authored form both formatters hold stable), runs in order and each comment distinct. Own-line-ness is authoring signal for a *leading* position, not a *trailing* one (see [Comment Position Philosophy](./conformance_prettier.md#comment-position-philosophy)): the pre-`:` comment trails the key, while a comment authored after the `:` leads the value and keeps its own line in both formatters — prettier's landing form, pinned dual-stable as `variant_own_line` — [property_key_colon_own_line_block_comment](../tests/fixtures/typescript/expressions/objects/property_key_colon_own_line_block_comment_prettier_divergence/), [attributes_key_colon_own_line_block_comment](../tests/fixtures/typescript/modules/imports/attributes_key_colon_own_line_block_comment_prettier_divergence/)
- Destructuring rename key to `:` line comment → The pattern face of the object-property entry above: from the forced-break authoring prettier **hoists** the comment to the property's own leading line, expanding the pattern (`{⏎\t// c⏎\ta: b⏎}`); tsv keeps it after the key and drops `: local` to a continuation line indented one level (inlining would swallow `: b } = o;` — content loss). The own-line authoring takes prettier's second destination — across the `:`, hung leading the local name — whose landing is **not** tsv-stable (a pattern has no multiline preservation): tsv re-collapses it keeping the comment in its relocated after-`:` slot, trailing the `:`, the local on a continuation line indented one level (`{ a: // c⏎\t\tb }`, a third stable form — pinned `divergent_variant_own_line`), mirroring its block sibling below — [rename_key_colon_line_comment](../tests/fixtures/typescript/expressions/destructuring/rename_key_colon_line_comment_prettier_divergence/)
- Destructuring rename key to `:` **own-line block** comment → The pattern face of the entry above, with a twist: prettier relocates across the `:` and hangs the comment leading the local name, but its landing is **not** dual-stable — a destructuring pattern has no multiline preservation (unlike an object literal), so tsv re-collapses prettier's hang keeping the comment in its relocated after-`:` slot (`{ a: /* c */ b }`, a third stable form — pinned `divergent_variant_own_line`). tsv collapses the own-line authoring in place (`{ a /* c */: b }`, glued per the object-property parity); three stable forms coexist, keyed on which side of the `:` the comment was authored — [rename_key_colon_own_line_block_comment](../tests/fixtures/typescript/expressions/destructuring/rename_key_colon_own_line_block_comment_prettier_divergence/)
- Variable definite `!` → After `!` modifier — [definite_comment](../tests/fixtures/typescript/declarations/variable/definite_comment_prettier_divergence/)
- Function param optional `?` → After `?` modifier — [param_optional_comment](../tests/fixtures/typescript/declarations/function/param_optional_comment_prettier_divergence/)
- Computed key after `]` (object) → Inside brackets `[x /* c */]` — [computed_key_bracket_colon_comment](../tests/fixtures/typescript/expressions/objects/computed_key_bracket_colon_comment_prettier_divergence/). An **own-line** authoring in the `]`→`:` gap takes a second destination: prettier crosses the `:` and hangs the comment leading the value (dual-stable `variant_own_line`), while tsv collapses it in place to the inline form — [computed_key_bracket_colon_own_line_block_comment](../tests/fixtures/typescript/expressions/objects/computed_key_bracket_colon_own_line_block_comment_prettier_divergence/)
- Computed key `]`→`:` line comment (object) → Hoisted to the property's own leading line (`{⏎\t// c⏎\t[x]: 1⏎}`); tsv keeps it after `]` and drops `: value` to a continuation line indented one level (the index-signature layout; inlining would swallow `: 1` — content loss). The own-line authoring takes prettier's second destination — across the `:`, hung leading the value (dual-stable `variant_own_line`) — while tsv pulls it up to the same continuation form — [computed_key_bracket_colon_line_comment](../tests/fixtures/typescript/expressions/objects/computed_key_bracket_colon_line_comment_prettier_divergence/)
- Computed key after `]` (class) → Inside brackets `[x /* c */]` — [computed_key_bracket_comment](../tests/fixtures/typescript/statements/class/computed_key_bracket_comment_prettier_divergence/). The `]`→`=` own-line authoring likewise splits: prettier hangs the comment leading the value (dual-stable `variant_own_line`), tsv collapses in place — [computed_key_bracket_own_line_block_comment](../tests/fixtures/typescript/statements/class/computed_key_bracket_own_line_block_comment_prettier_divergence/)
- Computed key `]`→`=` line comment (class) → Trailing the member `;` after the value (`[y] = 2; // c`); tsv keeps it after `]` + continuation indent. Two comments → prettier merges (info loss), tsv keeps distinct — the computed-key face of the class-property before-`=` entry above. An own-line `//` authoring pulls up under tsv, while prettier hangs the comment leading the value (dual-stable `variant_own_line`) — [computed_key_bracket_line_comment](../tests/fixtures/typescript/statements/class/computed_key_bracket_line_comment_prettier_divergence/)
- Computed key after `]` (iface) → Inside brackets (set: into params) — [computed_key_bracket_comment](../tests/fixtures/typescript/types/type_members/computed_key_bracket_comment_prettier_divergence/), [paren_in_comment](../tests/fixtures/typescript/types/type_members/computed_key_bracket_paren_in_comment_prettier_divergence/). The `]`→`:` **own-line block** authoring (iface + type literal) converges on the same in-bracket landing — unlike the object/class/destructuring hosts, whose own-line authoring prettier hangs across the separator — reached non-idempotently: pass 1 crosses the `:` and hangs the value (`[x]: /* c */⏎number;`), pass 2 moves the comment inside the brackets (one comment per pass, so a run takes 3 passes; the single-comment chain pinned `prettier_intermediate_to_variant_own_line` → dual-stable `variant_own_line`, the run README-recorded). tsv collapses the unforced breaks in one pass, keeping the comment inline in its authored gap — [computed_key_bracket_colon_own_line_block_comment](../tests/fixtures/typescript/types/type_members/computed_key_bracket_colon_own_line_block_comment_prettier_divergence/)
- Computed key after `]`, no annotation (iface) → Inside brackets `[k /* c */]` — [computed_key_no_annotation_comment](../tests/fixtures/typescript/types/type_members/computed_key_no_annotation_comment_prettier_divergence/)
- Computed key after `]` (destr) → Inside brackets `[x /* c */]` — [computed_key_bracket_comment](../tests/fixtures/typescript/expressions/destructuring/computed_key_bracket_comment_prettier_divergence/). The `]`→`:` **own-line block** authoring takes prettier's second destination — across the `:`, expanding the pattern and hanging the comment leading the local name — whose landing is **not** tsv-stable (a pattern has no multiline preservation): tsv re-collapses it keeping the comment in its relocated after-`:` slot, glued leading the local (`{ [x]: /* c */ a }`, a third stable form — pinned `divergent_variant_own_line`), while collapsing the own-line authoring in place to the inline form; the computed-key face of the rename entry above — [computed_key_bracket_colon_own_line_block_comment](../tests/fixtures/typescript/expressions/destructuring/computed_key_bracket_colon_own_line_block_comment_prettier_divergence/)
- Destructuring computed key `]`→`:` line comment → Hoisted to the property's own leading line (`{⏎\t// c⏎\t[x]: a⏎}`); tsv keeps it after `]` and drops `: local` to a continuation line indented one level (inlining would swallow `: a } = o;` — content loss). The own-line authoring takes prettier's second destination — across the `:`, hung leading the local name — whose landing is **not** tsv-stable (a pattern has no multiline preservation): tsv re-collapses it keeping the comment in its relocated after-`:` slot, trailing the `:`, the local on a continuation line indented one level (`{ [x]: // c⏎\t\ta }`, a third stable form — pinned `divergent_variant_own_line`), mirroring the rename sibling above — [computed_key_bracket_colon_line_comment](../tests/fixtures/typescript/expressions/destructuring/computed_key_bracket_colon_line_comment_prettier_divergence/)
- `readonly` keyword to `[` (index sig) → Inside brackets before the key `[/* c */ k` — [index_signature_readonly_comment](../tests/fixtures/typescript/types/type_members/index_signature_readonly_comment_prettier_divergence/)
- Accessor keyword (`get`/`set`) to `[` (computed key) → Inside brackets before the key `[/* c */ a` — [accessor_keyword_bracket_comment](../tests/fixtures/typescript/expressions/objects/accessor_keyword_bracket_comment_prettier_divergence/)
- Generator `*` to `[` (computed key, incl. `async *`) → Inside brackets before the key `[/* c */ a` — [generator_star_bracket_comment](../tests/fixtures/typescript/expressions/objects/generator_star_bracket_comment_prettier_divergence/)
- Computed member-access index, leading line comment (element access) → Inside brackets before the index (`arr[// c⏎i]`); tsv breaks the bracket keeping the comment before the index (index + `]` on their own lines so the `//` can't swallow them), prettier relocates it to lead the whole expression. Optional chains (`obj?.[…]`) too — [computed_leading_line_comment](../tests/fixtures/typescript/expressions/member/computed_leading_line_comment_prettier_divergence/)
- Computed member-access object to `[` line comment (element access) → Hoisted out to lead the whole expression, before the assignment RHS (`arr // c⏎[i]`); tsv trails the object and drops the bracket to its own line. Two comments in the gap → prettier **reorders** them (the own-line one hoisted past the same-line one); tsv keeps them distinct and in order. The computed-member counterpart of [member_only_interior_line_comment](#comment-relocation) — [computed_pre_bracket_line_comment](../tests/fixtures/typescript/expressions/member/computed_pre_bracket_line_comment_prettier_divergence/)
- Computed member-access object to `[` line comment, **numeric index** (`foo.bar().baz()⏎// c⏎[0]`) → prettier has **no fixed point**: it relocates the comment *inside* the brackets (`…[// c` with `0];` on the next line) and moves it again on the next pass. tsv treats the gap exactly as for a non-numeric index (comment in place, bracket drops to its own line, multiples distinct and in order). The index kind matters because prettier's member-chain grouping glues a numeric-index access into the preceding call's group instead of starting a new one, so no chain group begins at its `[` — the one gap where a chain line comment could still be deferred to end of line (which would relocate it past the `;` and merge multiples into a swallow) — [computed_numeric_index_pre_bracket_line_comment](../tests/fixtures/typescript/expressions/member/computed_numeric_index_pre_bracket_line_comment_prettier_divergence/)
- Type params to `(` (signatures) → Inside parens as leading on param — [signature_paren_in_comment](../tests/fixtures/typescript/types/type_members/signature_paren_in_comment_prettier_divergence/)
- `new` to `(` (construct signatures) → Inside parens as leading on param (after `)` when empty) — [construct_signature_paren_in_comment](../tests/fixtures/typescript/types/interfaces/construct_signature_paren_in_comment_prettier_divergence/)
- `new` to `(` (constructor types, incl. `abstract`) → Inside parens as leading on param (after `)` when empty) — [constructor_paren_comment](../tests/fixtures/typescript/types/function_type/constructor_paren_comment_prettier_divergence/)
- Type params to `(` (func types) → Inside parens as leading on param — [paren_in_comment](../tests/fixtures/typescript/types/function_type/paren_in_comment_prettier_divergence/)
- Type params to `(` (declare fn) → Inside parens as leading on param — [declare_paren_in_comment](../tests/fixtures/typescript/declarations/function/declare_paren_in_comment_prettier_divergence/)
- Type params to `(` (arrows) → Inside parens as leading on param — [arrow_type_params_paren_comment](../tests/fixtures/typescript/declarations/function/arrow_type_params_paren_comment_prettier_divergence/)
- Type params to `(` (overloads) → Inside parens as leading on param — [overload_type_params_paren_comment](../tests/fixtures/typescript/declarations/function/overload_type_params_paren_comment_prettier_divergence/)
- Type params to `(` (iface/type) → Inside parens as leading on param — [method_type_params_paren_comment](../tests/fixtures/typescript/types/type_members/method_type_params_paren_comment_prettier_divergence/)
- Anon func keyword to `(` → After `)` or inside parens — [expr_anon_keyword_comment](../tests/fixtures/typescript/declarations/function/expr_anon_keyword_comment_prettier_divergence/)
- Anon func keyword to `(` (line) → After `)` or inside parens — [expr_anon_line_comment](../tests/fixtures/typescript/declarations/function/expr_anon_line_comment_prettier_divergence/)
- Anon class keyword to `{` (line) → Into class body — [expr_anon_line_comment](../tests/fixtures/typescript/declarations/class/expr_anon_line_comment_prettier_divergence/)
- Bare parenthesized decorated class expr, cmt after `(` → Out of parens, onto its own line before `(`; tsv keeps it inside the parens after `(`. The decorated-class form of the required-paren statement case below — [decorated_expr_open_paren_comment](../tests/fixtures/typescript/expressions/class/decorated_expr_open_paren_comment_prettier_divergence/)
- Expression statement whose parens are **required** (object / class / function expression at statement start, or a bare string that would read as a directive), cmt after `(` → Out of parens, onto its own line before `(`; tsv breaks the parens open and keeps the comment inside them, all spellings alike (a block glued to the *expression* is owned by it and stays inline — not a divergence). Not preserving drops it: the flat `(`/`)` wrap has nowhere to put a comment the expression doesn't own. Where the paren is *redundant* tsv drops it and the comment leads the statement, matching prettier (`expression_statement_paren_comment`) — [expression_statement_paren_kept_comment](../tests/fixtures/typescript/statements/expression_statement_paren_kept_comment_prettier_divergence/)
- Constructor type `new` to `(` → After `)`, before param, or place — [constructor_type_new_comment](../tests/fixtures/typescript/types/constructor_type_new_comment_prettier_divergence/)
- Constructor type `abstract` to `new` → After `new` (mirrors the `new`-to-params relocation) — [constructor_type_abstract_comment](../tests/fixtures/typescript/types/constructor_type_abstract_comment_prettier_divergence/)
- Name to type params (line) → End of declaration line — [name_type_params_line_comment](../tests/fixtures/typescript/declarations/class/name_type_params_line_comment_prettier_divergence/)
- Anon class keyword to type params (line) → **In place, welded to the keyword, continuation flush** (`class// c⏎<T> {}`) — the one class-head gap prettier does not relocate, and the space it deletes is authored. tsv keeps that space, the separator every sibling header gap emits (`class A // c`, `const a = class // c⏎{}`), and indents the continuation: this is a declaration **header** gap, so it takes the [Uniform Forced-Continuation Indent](./conformance_prettier.md#uniform-forced-continuation-indent) rather than a rule of its own. The gap had **no emitter at all** on the anonymous arm and dropped every comment kind in it — after a name it is the name→`<T>` seam's, anonymous it belongs to the `class` keyword and nothing read it, which is the asymmetry `Printer::push_class_type_params` carried as an `Option`. The block spelling is glued in both formatters and is the non-divergent [anonymous_type_params_comment](../tests/fixtures/typescript/declarations/class/anonymous_type_params_comment/) — [anonymous_type_params_line_comment](../tests/fixtures/typescript/declarations/class/anonymous_type_params_line_comment_prettier_divergence/)
- Method name to type params (line) → End of method line — [method_name_type_params_line_comment](../tests/fixtures/typescript/declarations/class/method_name_type_params_line_comment_prettier_divergence/)
- Heritage last item before `{` → Into class/interface body — [heritage_last_item_line_comment](../tests/fixtures/typescript/declarations/class/heritage/heritage_last_item_line_comment_prettier_divergence/)
- Enum / namespace header before `{` (line) → Past the `{`: into the body when it is populated, trailing the collapsed `{}` when it is empty. From a **run** prettier emits `enum G // c4 // c3` — the two comments **reordered** and **merged** onto one line, where `// c3` stops being a comment and becomes text inside `// c4`; a line followed by a block reorders the same way (`enum H /* c6 */ // c5`). tsv keeps each on its authored line, in order, and drops `{` below — the class/interface rule of the row above, which these two headers share through `Printer::build_header_pre_body_doc`. The break is forced either way: emitting the gap inline and appending a bare `" {"` let the comment **swallow the brace** (`enum E // c1 {`), output that does not reparse, the same content-preservation argument as the switch `)`→`{` gap. Uniform across `enum` / `const enum` and `namespace` / `module` / `declare global` / a dotted name; a single-line **block** in the gap carries no such hazard and collapses onto the header line with `{` hugging it, matching prettier — [enum/header_brace_line_comment](../tests/fixtures/typescript/declarations/enum/header_brace_line_comment_prettier_divergence/), [namespace/header_brace_line_comment](../tests/fixtures/typescript/declarations/namespace/header_brace_line_comment_prettier_divergence/)
- Value-level function definition, signature→body `{` (line) → **Into the body**, leading its first statement; tsv keeps the comment trailing the head and drops `{` to its own line, flush with it (`function a() // c⏎{`) — the class/interface/enum/namespace answer to the same gap, which the value-level definitions (function declaration and expression, class method, getter/setter, constructor, object method) now share through the one gate (`Printer::comments_force_own_line_between`) and the one per-comment separator (`Printer::comment_hangs_next`). The brace **must** leave the line: emitted inline the `//` swallows it (`function a() // c {`), output that does not reparse — the same content-preservation argument the heritage row above makes. This family used to make the relocation itself, the one move across `{` tsv declines everywhere else — [declaration_head_body_line_comment](../tests/fixtures/typescript/syntax/comments/declaration_head_body_line_comment_prettier_divergence/)
- Declaration head→body `{`, **multiline block, authored break kept** → A multiline block the author broke after keeps its break here too, the rule every pre-separator and value gap applies: the comment trails the head and `{` drops to its own line, **flush** with it (not continuation-indented — the brace owns the indent level beneath it and must stay aligned with its `}`). Prettier answers it three ways, none of them preservation: relocated before the parameter list for a function declaration and a class method (two passes), absorbed into the body for a class and an interface, and **glued back** for an `enum` / `namespace`, where its first pass keeps the break and its second collapses it. Prettier gluing is not a reason to glue — tsv already keeps the break where prettier glues at the class-property `=`, enum-member `=`, property-signature key→`:` and all three binding defaults. A multiline block whose `{` shares its closing line stays glued in both. The gap used to re-derive the answer from line-vs-block (`pre_body_gap_breaks` asking `has_line_comments_between`, `append_body_with_sig_comments` hardcoding a space), which made it the one site collapsing an authored break — [declaration_head_body_multiline_block_break](../tests/fixtures/typescript/syntax/comments/declaration_head_body_multiline_block_break_prettier_divergence/); a function expression and an object method agree with tsv and are the non-divergent [declaration_head_body_multiline_block_break](../tests/fixtures/typescript/syntax/comments/declaration_head_body_multiline_block_break/)
- Declaration head→body `{`, **own-line single-line block** → tsv collapses the unforced breaks in one pass and keeps the comment in its authored slot (`function a() /* c */ {}` — the inline form both formatters hold stable, [declaration_head_body_comment](../tests/fixtures/typescript/syntax/comments/declaration_head_body_comment/)); own-line-ness is authoring signal for a *leading* position, not a trailing one. Prettier splits three ways over the same authoring: it **relocates** — out before the parameter list for a function declaration and class method, across the `{` for a class and interface (dual-stable, two passes) — [relocated](../tests/fixtures/typescript/syntax/comments/declaration_head_body_own_line_block_relocated_prettier_divergence/); it **keeps the break** for a function expression and an object method, its own fixed point, which tsv normalizes — [break_kept](../tests/fixtures/typescript/syntax/comments/declaration_head_body_own_line_block_break_kept_prettier_divergence/); and for an `enum` / `namespace` it reaches **tsv's own form** in two passes, so the divergence there is normalization only — [two_pass](../tests/fixtures/typescript/syntax/comments/declaration_head_body_own_line_block_two_pass_prettier_divergence/)
- Class **expression** head→body `{` → **Absorbed into the class body**, leading its first member, for every comment kind; tsv keeps each where the author wrote it, on the declaration's rule above. A class expression reaches its brace by four routes — bare name, anonymous, heritage, type parameters — and only the last two went through the shared header→body seam, so the bare-name and anonymous forms collapsed a broke-after multiline block their siblings kept, and the bare-name form emitted a **stray space** before the brace (`class A // c⏎␣{`). That space was a stable fixed point, so idempotency, the ratchets and the gate were all blind to it. The `emit_pre_body_comments` flag that routed those two around the seam is gone; a flag meaning "another emitter already claimed this range" is the [§3 hazard](./comments.md), not a knob — [class/head_body_comment](../tests/fixtures/typescript/expressions/class/head_body_comment_prettier_divergence/)
- Arrow body stripped parens → Parens stripped, block comment trailing the body / line comment floated past `;` (non-idempotent — at prettier's fixed point the comment relocates past `;` in every case but the call arg); tsv preserves the parens to keep the comment in place — [body_paren_comment](../tests/fixtures/typescript/expressions/arrows/body_paren_comment_prettier_divergence/)
- Await grouped operand → Comment relocated outside the required parens (`await (x + y) /* c */`, then floated past `;` on the next pass; leading → `await /* c */ (x + y)`); tsv keeps it inside where authored. `yield` matches *when the comment doesn't force a break* — there its parens are redundant and both strip (a break after `yield` is a different question, see the next entry) — [grouped_operand_comment](../tests/fixtures/typescript/expressions/await_yield/grouped_operand_comment_prettier_divergence/)
- Yield hanging comment → A comment that forces a break between `yield`/`yield*` and its operand takes the **parenthesized** form in tsv (`yield (⏎/* c */⏎x ?? y⏎)`), because `yield` is a restricted production — `yield [no LineTerminator here] AssignmentExpression` (ECMA-262 §15.5) — so the parens are what keep the author's break legal. Prettier drops them and emits `yield /* c */⏎x ?? y;`, which **no longer parses as the input did**: ASI ends the `yield` at the newline, the operand becomes a separate expression statement, and the `yield` loses its argument (prettier's own second pass makes the split explicit, `yield; /* c */` — pinned in `audit_signature.txt`). Prettier applies exactly this retention to `return`/`throw`, the other two restricted productions, but scopes the check to `ReturnStatement`/`ThrowStatement` (`parent-needs-parentheses.js`) so it never reaches `YieldExpression`. tsv uses one rule and one gate for all three. A comment that does *not* force a break still collapses inline (`yield /* c */ x ?? y`) with the redundant parens dropped. The delegate `yield*` diverges in layout only — a bare `yield*` is a syntax error, so ASI cannot silently split it — [yield_hanging_comment_parens](../tests/fixtures/typescript/expressions/await_yield/yield_hanging_comment_parens_prettier_divergence/)
- Non-null grouped operand → Comment relocated outside the required parens, between `)` and `!` (`(x + y) /* c */!`); tsv keeps it inside where authored — [grouped_operand_comment](../tests/fixtures/typescript/expressions/non_null/grouped_operand_comment_prettier_divergence/)
- Non-null grouped operand, leading comment → Comment relocated outside the required parens, before `(` (`/* b */ (x + y)!`); tsv keeps it inside where authored, before the operand — [grouped_operand_leading_comment](../tests/fixtures/typescript/expressions/non_null/grouped_operand_leading_comment_prettier_divergence/)
- Non-null grouped operand then member access → Same relocation outside the parens, through the member-chain printer (`(x + y) /* c */!.foo`); tsv keeps it inside where authored — [grouped_operand_member_comment](../tests/fixtures/typescript/expressions/non_null/grouped_operand_member_comment_prettier_divergence/)
- Non-null grouped operand then member access (line comment) → tsv keeps the line comment inside the parens, forcing the multiline operand layout (`(\n\tx + y // c\n)!.foo`); prettier relocates it after `)!` and breaks the chain — [grouped_operand_member_line_comment](../tests/fixtures/typescript/expressions/non_null/grouped_operand_member_line_comment_prettier_divergence/)
- Non-null operand (line comment), the general rule → The entry above holds at **every** non-null operand, not only where a member access follows, and the shell is **retained** when the parens were otherwise redundant (`(\n\taaa // c\n)!`). Prettier strips and defers to end of line. Deferring is what forces the divergence rather than taste: a `//` carried past its own statement merges with whatever already trails that line — `(x + y // c1⏎)!; // c2` → `(x + y)!; // c1 // c2`, one comment where there were two, which the comment census measures as `MISSING "c1"` · `MISSING "c2"` · `EXTRA "c1 // c2"`. A **block** comment in the same gap needs no shell and matches prettier. ⚠️ One cell is still unfixed and loses the comment outright — a `//` in a *stripped* shell whose `!` is **mid-chain** (`(aaa // c⏎)!.bbb`), where retaining the shell is a linearizer decision rather than a printer one — [operand_line_comment](../tests/fixtures/typescript/expressions/non_null/operand_line_comment_prettier_divergence/)
- Sealed optional chain, line comment before the `!` → The same rule at the two positions a sealed chain never reaches through the member-chain printer, where the parens are *required* rather than merely authored: a `new` callee and a template tag. tsv keeps the comment inside the parens on the multiline layout; prettier relocates it after `)!`, stranding the arguments (`new (a?.b)! // c⏎();`) or the template on the next line. The **block**-comment spelling matches prettier at both positions (inline inside the parens, `new (a?.b /* c */)!()`) — [optional_paren_non_null_sealed_line_comment](../tests/fixtures/typescript/expressions/chain/optional_paren_non_null_sealed_line_comment_prettier_divergence/)
- Spread grouped operand → Comment relocated outside the canonical parens, before `]`/`)` (`[...(x + y) /* c */]`); tsv keeps it inside where authored — [grouped_operand_comment](../tests/fixtures/typescript/expressions/spread/grouped_operand_comment_prettier_divergence/)
- Spread stripped-paren line comment → Prettier puts a `//` written after the `)` on the same output line as the one the parens held, which **merges** them into a single comment — `// i // t` reparses as one `//` whose text is ` i // t`, so the second stops existing. tsv gives it its own line, where a reparse keeps it (`...b // i⏎// t`). One of the information-losing merges §Comment Position Philosophy names. The rule is about what a `//` does to its line, so it holds in every list and at every position in one: on an element the list continues past, the second comment's line lands ahead of the next element (`...b, // i⏎// t⏎c`) — [last_arg_paren_comment_line](../tests/fixtures/typescript/expressions/spread/last_arg_paren_comment_line_prettier_divergence/), [inter_arg_paren_comment_line](../tests/fixtures/typescript/expressions/spread/inter_arg_paren_comment_line_prettier_divergence/), [element_paren_comment_line](../tests/fixtures/typescript/expressions/spread/element_paren_comment_line_prettier_divergence/) (array + object literals)
- Spread stripped-paren interior then a gap line comment → A spread's parens hold an own-line comment and a `//` is written after the `)`, before the separator comma (`[...(b⏎/* i */⏎)// c⏎, d]`). Prettier keeps the authored order by gluing the `//` onto the inside comment's line (`...b,⏎/* i */ // c`); tsv places each by its own rule — the `//` trails the element past the pure separator (the sanctioned trail, the only rendering it has there), the interior comment is the *list's* share and takes the line below (`...b, // c⏎/* i */`), so their output order is a consequence of the two placements rather than a relocation of either. Uniform across the argument list, object literal and array literal — [interior_then_gap_line_comment](../tests/fixtures/typescript/expressions/spread/interior_then_gap_line_comment_prettier_divergence/)
- Spread stripped-paren comment then an outside block, as the LAST argument → Prettier hoists the outside block onto the argument's line and drops the inside one below it, so the pair comes back in the **reverse** of the order it was written (`fn(a, ...(b⏎/* i */⏎) /* t */)` → `...b /* t */⏎/* i */`); tsv keeps the authored order, the inside comment first (`...b⏎/* i */ /* t */`). Both forms are stable in both formatters — the divergence is which one the parenthesized authoring normalizes to — [last_arg_paren_comment_order](../tests/fixtures/typescript/expressions/spread/last_arg_paren_comment_order_prettier_divergence/)
- Ternary operand leading comment → Relocated out of the operand's grouping parens, before `(` (`(/* c */ x ?? y) ? …` → `/* c */ (x ?? y) ? …`), in the test position and both branches; tsv keeps it glued inside. Both authorings are dual-stable in tsv (a glued block comment binds to the operand it leads, `Comment::owned_by_node`) — [test_paren_leading_comment](../tests/fixtures/typescript/expressions/ternary/test_paren_leading_comment_prettier_divergence/)
- Sequence operand leading comment → Relocated out of the sequence parens, before `(` (`fn((/* c */ x, y))` → `fn(/* c */ (x, y))`); tsv keeps it glued inside. The trailing edge floats after `)` in both (a match), and interior operand comments match too — [operand_edge_comment](../tests/fixtures/typescript/expressions/sequence/operand_edge_comment_prettier_divergence/)
- Unary assignment-operand leading comment → Relocated out of the operand's required parens (`!(/* c */ x = y)` → `!(/* c */ (x = y))`); tsv keeps it glued inside the single pair (no double-wrap). Same leading-comment rule as the ternary/sequence operands above — [assignment_operand_leading_comment](../tests/fixtures/typescript/expressions/unary/assignment_operand_leading_comment_prettier_divergence/)
- Unary conditional/arrow-operand leading comment → Relocated out of the operand's required parens (`!(/* c */ cond ? b : c)` → `!(/* c */ (cond ? b : c))`, `!(/* c */ () => x)` → `!(/* c */ (() => x))`); tsv keeps it glued inside the single pair (no double-wrap). The same rule as the unary assignment operand above — every operand that prints its own value-position pair encloses the owned comment in that one pair. A **type-assertion** operand is the non-divergent counterpart: prettier keeps the single pair there too, so tsv matches ([type_assertion_operand_leading_comment](../tests/fixtures/typescript/expressions/unary/type_assertion_operand_leading_comment/)) — [paren_operand_leading_comment](../tests/fixtures/typescript/expressions/unary/paren_operand_leading_comment_prettier_divergence/)
- Bundler annotation (`/* @__PURE__ */`) before an expression that takes parens → The paren emitted *between* the annotation and the call it marks, so the annotation leads a paren and the call stops being side-effect-free; tsv keeps it glued to its token — [pure_annotation_enclosing_parens](../tests/fixtures/typescript/syntax/comments/pure_annotation_enclosing_parens_prettier_divergence/)
- Between keyword and `(` → Inside parens — [keyword_paren_comment](../tests/fixtures/typescript/syntax/comments/keyword_paren_comment_prettier_divergence/)
- Between keyword and `(` (line) → Inside the condition parens (`for` past the header to before the body); tsv keeps it trailing the keyword with `(` forced onto the next line (a `//` there would otherwise swallow the `(`). Uniform across `if`/`while`/`for`/`switch`/`catch` — [keyword_paren_line_comment](../tests/fixtures/typescript/syntax/comments/keyword_paren_line_comment_prettier_divergence/)
- `for await` keyword gaps → Inside parens — [for_await_keyword_comment](../tests/fixtures/typescript/statements/for/for_await_keyword_comment_prettier_divergence/)
- `for await` keyword gaps (line) → Inside the condition parens, before the binding; tsv keeps it trailing the preceding keyword with the next token (`await`, then `(`) forced onto the next line (a `//` there would otherwise swallow it). Covers both the `for`→`await` and `await`→`(` gaps — the line counterpart of the row above, and the two extra gaps `for await` adds to the uniform keyword→`(` rule — [for_await_keyword_line_comment](../tests/fixtures/typescript/statements/for/for_await_keyword_line_comment_prettier_divergence/)
- Between `)` and `{` (switch) → Inside condition parens — [condition_absorbed_comment](../tests/fixtures/typescript/syntax/comments/condition_absorbed_comment_prettier_divergence/)
- Between `)` and `{` (switch), comment contains a `{` (scan robustness) → Inside condition parens — [condition_absorbed_brace_in_comment](../tests/fixtures/typescript/syntax/comments/condition_absorbed_brace_in_comment_prettier_divergence/)
- Between modifier keywords → Before declaration name — [declaration_keyword_name](../tests/fixtures/typescript/syntax/comments/declaration_keyword_name_prettier_divergence/)
- Between `async` and `function` → Before function name — [comments_between_keywords](../tests/fixtures/typescript/declarations/function/async/comments_between_keywords_prettier_divergence/)
- Import keyword to empty `{}` → After `from` — [empty_keyword_comment](../tests/fixtures/typescript/modules/imports/empty_keyword_comment_prettier_divergence/)
- Export keyword to empty `{}` → After `from` — [empty_keyword_comment](../tests/fixtures/typescript/modules/exports/empty_keyword_comment_prettier_divergence/)
- Import `type` keyword to empty `{}` → After `from` — [empty_type_keyword_comment](../tests/fixtures/typescript/modules/imports/empty_type_keyword_comment_prettier_divergence/)
- Export `type` keyword to empty `{}` → After `from` — [empty_type_keyword_comment](../tests/fixtures/typescript/modules/exports/empty_type_keyword_comment_prettier_divergence/)
- Import `type` keyword to specifiers → Into the specifier braces — [type_keyword_comment](../tests/fixtures/typescript/modules/imports/type_keyword_comment_prettier_divergence/)
- Export `type` keyword to specifiers → Into the specifier braces — [type_keyword_comment](../tests/fixtures/typescript/modules/exports/type_keyword_comment_prettier_divergence/)
- Import/export `type` keyword to specifiers, at print width → Same relocation, plus the width axis: because Prettier's relocation makes a header-gap comment the lone specifier's *leading* comment, it satisfies the `canBreak` rule that otherwise pins a single braced specifier to one line ([§Print Width Philosophy](./conformance_prettier.md#print-width-philosophy)), so the braces expand at 101. tsv mirrors the break while keeping the comment in its authored gap; a comment-free lone specifier at the same width stays inline in both — [imports/type_keyword_comment_long](../tests/fixtures/typescript/modules/imports/type_keyword_comment_long_prettier_divergence/), [exports/type_keyword_comment_long](../tests/fixtures/typescript/modules/exports/type_keyword_comment_long_prettier_divergence/)
- Import keyword to the `defer` phase keyword → After `defer` — [phase_keyword_comment](../tests/fixtures/typescript/modules/imports/phase_keyword_comment_svelte_prettier_divergence/). The import-phase proposal's sibling of the `type`-keyword rows above, and preserved for the same reason: the gap is a position the author chose, and `type` and the phase keywords are mutually exclusive occupants of one header slot. Only `defer` is cataloged — Prettier's parser does not accept `import source`, so that phase has no oracle to diverge from. Also a `_svelte_divergence`: acorn-typescript rejects `import defer` outright — the syntax itself is cataloged in [conformance_prettier_ts.md §Import-phase proposals](./conformance_prettier_ts.md#import-phase-proposals) and [conformance_svelte.md §Import-phase proposals](./conformance_svelte.md#import-phase-proposals); only the comment *position* rule lives here.
- Default import header comments → Binding side of `type` — [default_keyword_comment](../tests/fixtures/typescript/modules/imports/default_keyword_comment_prettier_divergence/)
- Namespace import header comments → Binding side of `type` — [namespace_keyword_comment](../tests/fixtures/typescript/modules/imports/namespace_keyword_comment_prettier_divergence/)
- Export-all header comments → After `from` — [all_keyword_comment](../tests/fixtures/typescript/modules/exports/all_keyword_comment_prettier_divergence/)
- Export-all namespace `*` to `as` → After `as` (before binding) — [all_namespace_keyword_comment](../tests/fixtures/typescript/modules/exports/all_namespace_keyword_comment_prettier_divergence/)
- Named-import-specifier rename `as` gaps (line) → Every `imported`→`as` and `as`→`local` line comment relocated to lead the whole specifier and stacked flush before it (`{ //c⏎a as b }`), collapsing the two authored positions into one; tsv keeps each on its authored side and continues `as <local>` (or the `local` after `as`) at **one** indent level — the same preserve-in-place rule as the namespace `*`→`as` gap. A before-`as` `//` would otherwise swallow the `as` and the renamed binding (content loss); a same-line block comment trails inline in both. — [imports/specifier_as_gap_line_comment](../tests/fixtures/typescript/modules/imports/specifier_as_gap_line_comment_prettier_divergence/)
- Named-export-specifier rename `as` gaps (line) → The export-side twin (`local`→`as`→`exported`), same shared renamed-specifier printer — [exports/specifier_as_gap_line_comment](../tests/fixtures/typescript/modules/exports/specifier_as_gap_line_comment_prettier_divergence/)
- Named-specifier `type` modifier gap (line) → Relocated to lead the whole specifier (`{ //c⏎type A }`), the same move as the rename `as` gaps above; tsv keeps the comment after the modifier and continues the name — plus any `as <binding>` after it — at **one** indent level. The gap the modifier opens is the same shape as the header `type`→`{` gap one level out, so it routes through the same continuation helper: a `//` would otherwise swallow the name (the gap had **no emitter at all**, so every comment in it was dropped). A same-line block trails inline in both (`type /* c */ A`); a multiline block follows the shared header-gap rule and keeps its own line. Import and export specifiers share one printer, so the two sides answer alike — [imports/specifier_type_gap_line_comment](../tests/fixtures/typescript/modules/imports/specifier_type_gap_line_comment_prettier_divergence/), [exports/specifier_type_gap_line_comment](../tests/fixtures/typescript/modules/exports/specifier_type_gap_line_comment_prettier_divergence/)
- `export`→`default` (block) → After `default`, leading the value (`export default /* c */ 1`); tsv keeps it after `export`. The interior gap of a multi-word keyword — see [Comments inside a multi-word keyword](#comments-inside-a-multi-word-keyword) — [default_keyword_comment](../tests/fixtures/typescript/modules/exports/default_keyword_comment_prettier_divergence/)
- `export`→`default` (line) → After `default`, with the value pulled back flush (`export default // c⏎1;`); tsv keeps it after `export` and indents the `default 1;` continuation one level (the uniform forced-continuation indent) — [default_keyword_line_comment](../tests/fixtures/typescript/modules/exports/default_keyword_line_comment_prettier_divergence/)
- `export`→`default`, decorated class expression → After `default` (`export default /* c */⏎@dec⏎class {}`); tsv keeps it after `export`. The decorated-class path of the same gap — [default_keyword_decorator_comment](../tests/fixtures/typescript/modules/exports/default_keyword_decorator_comment_prettier_divergence/)
- `export default` keyword gaps, decorators-*first* class → Both the `export`→`default` and `default`→`class` gaps relocated onto the decorator line and stacked (`@dec /* c1 */ /* c2 */⏎export default class A {}`), collapsing two authored positions into one; tsv keeps each where it was written. Decorators before `export` keep the class a *declaration*, so it is a third path of the same keyword — [decorator_first_default_keyword_comment](../tests/fixtures/typescript/modules/exports/decorator_first_default_keyword_comment_prettier_divergence/)
- `declare`→kind keyword (`const`/`let`/`var`) → After the kind keyword, leading the binding (`declare const /* c */ a`); tsv keeps it after `declare` — [declare_keyword_comment](../tests/fixtures/typescript/declarations/variable/declare_keyword_comment_prettier_divergence/)
- `await`→`using` → After `using`, leading the binding (`await using /* c */ a`); tsv keeps it after `await`. Comments on *both* sides of `using` collapse to one position under Prettier (`await using /* c2 */ /* c3 */ b`), losing the before/after distinction; tsv keeps each on its authored side — [await_keyword_comment](../tests/fixtures/typescript/typescript_specific/using/await_keyword_comment_svelte_prettier_divergence/)
- `export`→`=` (export assignment) → After the `=`, leading the value (`export = /* c */ value`); tsv keeps it after `export`, matching the `export`→`const`/`function` gap both formatters preserve. The operand-side gap agrees in both — [export_equals_keyword_comment](../tests/fixtures/typescript/modules/exports/export_equals_keyword_comment_prettier_divergence/)
- `export as namespace` keyword-interior gaps (`export`→`as`, `as`→`namespace`) → Both relocated past the whole keyword and stacked before the name (`export as namespace /* c1 */ /* c2 */ Foo`), collapsing two distinct positions into one; tsv keeps each on its authored side. The `namespace`→name gap agrees in both (regular [export_as_namespace_name_comment](../tests/fixtures/typescript/modules/exports/export_as_namespace_name_comment/)) — [export_as_namespace_keyword_comment](../tests/fixtures/typescript/modules/exports/export_as_namespace_keyword_comment_prettier_divergence/)
- Meta-property dot gaps (line) → Out of the meta property entirely: past the `;` for `new.target` (`return new.target; // c`), or after `.meta` for `import.meta`; tsv keeps it in the gap and continues `.property` one level down. Block comments in the same gaps agree in both (regular [dot_gap_comments](../tests/fixtures/typescript/expressions/misc/meta_property/dot_gap_comments/)) — [dot_gap_line_comment](../tests/fixtures/typescript/expressions/misc/meta_property/dot_gap_line_comment_prettier_divergence/)
- Qualified-name dot gaps (line) → Past the `;` (`let a: ns.Type; // c`); tsv keeps it in the gap and continues the rest of the name one level down. The meta property's twin — same `name` `.` `name` shape, one shared printer — so it reaches type references, interface heritage, and import-equals module references alike. Block comments in the same gaps agree in both (regular [qualified_name_dot_gap_comments](../tests/fixtures/typescript/types/qualified_name_dot_gap_comments/)) — [qualified_name_dot_gap_line_comment](../tests/fixtures/typescript/types/qualified_name_dot_gap_line_comment_prettier_divergence/)
- `export as namespace` keyword gaps (line) → All relocated past the whole keyword and stacked flush before the name (`export as namespace // c1⏎// c2⏎// c3⏎Foo`); tsv keeps each on its authored side, continuing the header at **one** indent level rather than a staircase — [export_as_namespace_line_comment](../tests/fixtures/typescript/modules/exports/export_as_namespace_line_comment_prettier_divergence/)
- Import-equals `import`→`type` → Binding side of `type` (`import type /* c */ C = require('./c')`, the same rule as a default import); tsv preserves. The **only** diverging gap of that header — the other four agree in both and are pinned by the regular [equals_header_comment](../tests/fixtures/typescript/modules/imports/equals_header_comment/) — [equals_type_keyword_comment](../tests/fixtures/typescript/modules/imports/equals_type_keyword_comment_prettier_divergence/)
- Import-equals header gaps (line) → Relocated past the `=` to trail the whole statement (`A = require('./a'); // c3`) with the rest stacked flush below it, and the `=`→reference gap's comment onto the `require(` line; tsv keeps each on its authored side, continuing the header at **one** indent level. The module reference is the only such tail that can break *internally*, so this also pins that its own breaks nest **inside** that level — its contents one deeper than `require(`, its `)` back at the reference's level. Prettier is non-idempotent on its own output here (a second pass moves the `require(` comment again; `audit_signature.txt` pins the chain) — [equals_header_line_comment](../tests/fixtures/typescript/modules/imports/equals_header_line_comment_prettier_divergence/)
- `export default`→first decorator, comment **glued** to `@dec` → Relocated back past `default` (`export default /* c */`), then one blank line added per pass, saturating at two (Prettier non-idempotent; `output_prettier.*` is its first pass, `audit_signature.txt` pins the chain); tsv keeps it glued to `@dec`, where the parser bound it. The complementary own-line authoring is *not* glued, so it is emitted by the keyword→decorator gap instead — both authorings preserved — [default_decorator_lead_comment](../tests/fixtures/typescript/modules/exports/default_decorator_lead_comment_prettier_divergence/)
- Import binding to `from` (line) → After `;` — [from_comment](../tests/fixtures/typescript/modules/imports/from_comment_prettier_divergence/)
- Import specifiers to `from` → Into the specifier braces — [from_comment](../tests/fixtures/typescript/modules/imports/from_comment_prettier_divergence/)
- Export specifiers to `from` → Into the specifier braces — [from_comment](../tests/fixtures/typescript/modules/exports/from_comment_prettier_divergence/)
- Import source to `with` (line) → After `;` — [with_keyword_comment](../tests/fixtures/typescript/modules/imports/with_keyword_comment_prettier_divergence/)
- Import `with` to attributes `{` → Before `with` (block) / `;` (line) — [with_keyword_comment](../tests/fixtures/typescript/modules/imports/with_keyword_comment_prettier_divergence/)
- Re-export attributes header (`with`→`{`) → Before `with` (block); the after-`}` comment trails past `;` in both — [exports/attributes_comment](../tests/fixtures/typescript/modules/exports/attributes_comment_prettier_divergence/)
- Empty `with {}` comment (`with`→`{`, inside `{}`) → Before `with` (a run, out past the `;`); the after-`}` comment trails past `;` in both. tsv keeps each inside the braces it was written in, through the same empty-container emitter the **empty tuple type** uses: a lone fitting block stays inline and delimiter-tight (`with {/* c */}`, as `type A = [/* c */]` does), while a `//` — which the `}` and `;` cannot follow — breaks the braces open, and a run takes one line per comment in source order. Open-coding that run instead separated it with a plain `" "`, which WELDED two blocks into one comment and, behind a `//`, swallowed the `}` and the `;` — output that does not reparse from input canonical Svelte accepts ([comments.md](./comments.md) §Trailing and dangling runs, whose kind-test trap this was) — [attributes_empty_comment](../tests/fixtures/typescript/modules/imports/attributes_empty_comment_prettier_divergence/)
- Import-attribute value's leading line comment authored on the `:` line (`with { type: // c⏎'json' }`) → Prettier floats it past the value to trail it (`type: 'json' // c`, dual-stable); tsv keeps it leading the value, breaking after `:` and indenting (an own-line line comment matches — plain `attributes_value_comment`; a same-gap block stays inline — plain `attributes_comma_comment`) — [attributes_value_colon_line_comment](../tests/fixtures/typescript/modules/imports/attributes_value_colon_line_comment_prettier_divergence/)
- Import-attribute key→`:` gap line comment (`with { type // c⏎: 'json' }`) → Prettier floats it past the value to trail it (`type: 'json' // c`); tsv keeps it trailing the key and drops `: value` to a continuation line (`type // c⏎\t: 'json'`, the uniform forced-continuation indent — same as the object-property / type-member key→`:` line comment; a same-gap block stays inline — plain `attributes_comma_comment`) — [attributes_key_colon_line_comment](../tests/fixtures/typescript/modules/imports/attributes_key_colon_line_comment_prettier_divergence/). An own-line `//` authoring pulls up under tsv, while prettier hangs it leading the value (dual-stable `variant_own_line`) — distinct from its trailing float on the forced-break authoring
- Import-attribute comma-gap **block** sharing the comma's line (`with { type: 'json'⏎, /* c */⏎attr: 'value' }`, and the same comment ending the comma's own line) → Prettier slides it **backward** across the comma onto the previous attribute (`type: 'json' /* c */, attr: 'value'`, dual-stable); tsv keeps it after the comma, where the author put it, and the clause stays inline — the comma is re-emitted structure, so a comment sharing its line does not own that line ([comments.md](./comments.md) §The element-comma seam). The mirror authoring, a comment with the comma **glued** behind it (`'json'⏎/* c */,⏎attr:`), collapses to the same inline form in both formatters. An **isolated** block (a line of its own on both sides) still expands the clause in both — plain [attributes_comma_comment](../tests/fixtures/typescript/modules/imports/attributes_comma_comment/) — [attributes_comma_gap_comment](../tests/fixtures/typescript/modules/imports/attributes_comma_gap_comment_prettier_divergence/)
- Own-line line comment before a statement's `;`, blank line after → Prettier floats the blank above the comment (promoting it to lead the next statement); tsv keeps the blank below (comment stays with its statement). General — imports are the example, the `; // 1` is incidental — [source_trailing_comment](../tests/fixtures/typescript/modules/imports/source_trailing_comment_prettier_divergence/)
- **Member `;` that deferred a line comment past itself, a second comment on the `;`'s line** (`a = 1 // c1⏎; // c2`) → ◆content_preservation — Prettier **merges** the pair onto one output line (`a = 1; // c1 // c2`, the second `//` becoming text of the first, so a comment stops existing) and **reorders** a block ahead of the deferred one (`a = 1; /* c2 */ // c1`); tsv gives the second comment the line below, both distinct and in source order. What the member deferred may equally be an own-line **block** rather than the trailing `//` (`a: A // c1⏎/* c2 */; /* c3 */`), and the rule is the same: the block takes the line, the trailing comment follows it there. A member body only — at statement level prettier keeps them distinct too, which is what the statement / block walks already do. Class, interface and type-literal bodies alike, on a **last** member (where the body's own end-of-run emitter claims the comment) as much as a middle one, and for a type member's `,` separator as much as its `;` — tsv normalizes either spelling to the `;` the member prints, so both authorings defer the same way. `input.svelte` is dual-stable, so the `unformatted_ours_*` variants carry the split — [member_line_before_semicolon_run](../tests/fixtures/typescript/syntax/comments/member_line_before_semicolon_run_prettier_divergence/)
- **JSDoc cast in value position, comment mid-line, `(` authored on the next line** (`const a = /** @type {A} */⏎(b)`) → The break is unforced (a block comment ends nothing), so tsv reflows the `(` back onto the comment's line per [§Authored breaks in value position](./conformance_prettier.md#authored-breaks-in-value-position), landing on the comment-glued-to-`(` layout every wide cast already takes (`= /** @type {A} */ (⏎\tinner⏎)`); prettier instead breaks after the operator and hangs the comment on its own line above the `(`. Uniform across the value positions — declarator, assignment expression, class-property initializer, arrow body, and a for-header's init declarator (an object-literal value takes the same reflow, but its `:`→value gap has a hang rule of its own that keeps the two authorings apart; see the fixture's README). `input.svelte` is dual-stable, so the divergence rides `unformatted_ours_break`, and prettier's hang form is dual-stable in turn (an own-line cast comment hangs under tsv too), pinned as `variant_hung` — [jsdoc_type_cast_value_gap_break](../tests/fixtures/typescript/syntax/comments/jsdoc_type_cast_value_gap_break_prettier_divergence/). The complement, where the cast is **not** in a value gap (statement, label, call argument) and the authored break is kept because those lists keep lines, is the non-divergent [jsdoc_type_cast_leading_run_break](../tests/fixtures/typescript/syntax/comments/jsdoc_type_cast_leading_run_break/)
- **JSDoc cast in a BINDING DEFAULT, same authoring** (`const { a = /** @type {A} */⏎(b) } = obj`) → tsv reflows exactly as above; prettier **keeps the break mid-line** here instead of hanging the comment above the `(`, so its form is one tsv still normalizes (a `prettier_variant_break`, where the value-gap sites above reach a dual-stable `variant_hung`) — which is why these sites are a fixture of their own. One gap and one emitter for all three spellings: an object-pattern property, an array-pattern element, and a parameter default. The **array** one is not a divergence: prettier collapses the run back onto the element's line even while the pattern breaks, per the per-element group of [comments.md](./comments.md) §Array family vs params family — [jsdoc_type_cast_binding_default_break](../tests/fixtures/typescript/syntax/comments/jsdoc_type_cast_binding_default_break_prettier_divergence/)
- **JSDoc cast in a binding default, comment on a line of ITS OWN** (`objDefault =⏎/** @type {A} */⏎(b)`) → ◆content_preservation — the one cast authoring that does NOT reflow: the cast prints a hardline between the comment and its `(`, so tsv hangs the value with the `=` ending its line, as at every other value position. Prettier **relocates the comment across the `=`** to lead the *binding* (`/** @type {A} */⏎objDefault = (b)`), and its own second pass then **strips the parens** — the comment no longer adjoins a `(`, so the type assertion stops existing. No prettier-stable form exists to pin as a variant, so the chain rides `output_prettier` + `audit_signature`. The hang and the hardline come off one predicate and must agree; it is deliberately narrower than `owned_leading_comment_effect`, which also hangs an indentable owned block these sites keep inline — [jsdoc_type_cast_binding_default_own_line](../tests/fixtures/typescript/syntax/comments/jsdoc_type_cast_binding_default_own_line_prettier_divergence/). The enum-member spelling rides its sibling fixture's `C` member
- **JSDoc cast at an ENUM MEMBER `=`, same authoring** → the same reflow at the one value gap that is TypeScript-only, so prettier's cast-unaware TS parser [strips the parens](#jsdoc--paren-semantics) on top of keeping the break. The two compose: `input.svelte` is prettier-unstable (hence an `output_prettier`), and prettier's break form is stable while tsv rewrites it to a THIRD form — a paren-less cast is just a block comment, so width decides its break — pinned as `divergent_variant_break` — [jsdoc_type_cast_enum_member_break](../tests/fixtures/typescript/syntax/comments/jsdoc_type_cast_enum_member_break_prettier_divergence/)
- **JSDoc cast in a COMPUTED KEY, comment own-line or mid-line** (`{ [⏎/** @type {A} */⏎(k)]: 1 }`) → ◆prettier_bug — the `[`→key gap is a **cannot-hang** value gap: a `[` starts the key's line, so there is no operator line to end and the cast's own-line hardline would strand the `(` at the bracket's own column — the same shape as a Svelte braced head ([conformance_prettier_svelte.md §Svelte: Own-line JSDoc cast at a braced head](./conformance_prettier_svelte.md#svelte-own-line-jsdoc-cast-at-a-braced-head)), answered the same way: tsv **reflows** both authorings onto the `[`'s line (`[/** @type {A} */ (k)]`) in one pass, matching what a plain block comment in the same gap already does. Prettier (JS path — it preserves the cast) is **non-idempotent** here and splits the family: pass 1 emits the broken mid-line form for every member kind, pass 2 reflows a **property** key to the glued form while holding a **method**/accessor key broken — a mixed second stable form tsv normalizes whole. Uniform across the object literal (property, method, `get` accessor — [computed_key_jsdoc_cast_own_line](../tests/fixtures/typescript/expressions/objects/computed_key_jsdoc_cast_own_line_prettier_divergence/)), class members ([computed_key_jsdoc_cast_own_line](../tests/fixtures/typescript/statements/class/computed_key_jsdoc_cast_own_line_prettier_divergence/)), destructuring patterns (where prettier's chain does converge to the glued form, in two passes — [computed_key_jsdoc_cast_own_line](../tests/fixtures/typescript/expressions/destructuring/computed_key_jsdoc_cast_own_line_prettier_divergence/)), and the Svelte `{#each … as}` pattern spelling ([each/destructure_computed_key_jsdoc_cast](../tests/fixtures/svelte/blocks/each/destructure_computed_key_jsdoc_cast_prettier_divergence/), where prettier-plugin-svelte instead drops the comment outright). A **type-literal member's** computed key is deliberately not converged yet (stable today at the stranded column; a cast there is TS-context, where prettier strips cast parens anyway)
- **Own-line comment in a MULTI-DECLARATOR's value gap** (`const a =⏎// c⏎1,⏎b = 2`) → Both formatters hang the value and indent the sibling declarators one level; tsv gives the hung value that same one level ([§Uniform Forced-Continuation Indent](./conformance_prettier.md#uniform-forced-continuation-indent)) where prettier gives it two, to keep it distinguishable from the next declarator. An indent-only difference — the comment does not move in either. The three spellings answer alike: the gap-emitted `//`, an indentable owned block, and an own-line JSDoc cast, whose hang is not optional (its hardline with no hang left the `(` at the list's own indent, an authoring the next pass collapsed) — [value_own_line_comment_hang](../tests/fixtures/typescript/declarations/variable/multiple/value_own_line_comment_hang_prettier_divergence/)
- Import keyword/`from`→source (line) → In place flat (bare/empty), into braces (named), after `;` (default); tsv indents — [source_line_comment](../tests/fixtures/typescript/modules/imports/source_line_comment_prettier_divergence/)
- Re-export `from`→source (line) → In place flat (empty/export-all), into braces (named); tsv indents — [source_line_comment](../tests/fixtures/typescript/modules/exports/source_line_comment_prettier_divergence/)
- Module header gap, **multiline block** (`import { a } from /* x⏎y */⏎'./a'`) → The author broke after a comment that cannot be reflowed (inlining would swallow the `*/` line into the header), so the break is forced and tsv indents the continuation one level — [§Uniform Forced-Continuation Indent](./conformance_prettier.md#uniform-forced-continuation-indent), uniformly with the same gap's *line* comment and with every value gap's multiline block (`const x =⏎\t/* x⏎y */⏎\t1;`). The comment's own interior lines stay flush; tsv never re-indents a comment body. Prettier relocates the comment into the braces (imports) or past `from` (export-all) and leaves the continuation flat — [imports/header_multiline_block_comment](../tests/fixtures/typescript/modules/imports/header_multiline_block_comment_prettier_divergence/), [exports/header_multiline_block_comment](../tests/fixtures/typescript/modules/exports/header_multiline_block_comment_prettier_divergence/)
- Module header gap, **own-line block** (`import { a }⏎/* c */⏎from './a'`) → Prettier preserves the break and relocates the comment per gap (into the braces as a specifier-trailing comment for specifiers→`from`, as a leading comment for keyword→`{`, flat for default→`from`); tsv trails it inline and reflows the value onto the header line. A module header is a keyword→value gap, so a single-line block collapses from any authored position — the shared `comment_hangs_next` rule its `export default` / `export =` siblings use ([§Authored breaks in value position](./conformance_prettier.md#authored-breaks-in-value-position)); an authored blank line in the gap yields with the break. Prettier's first pass is unstable here, so the chain is pinned `prettier_intermediate_to_variant_*` → `variant_*` — [header_own_line_block_comment](../tests/fixtures/typescript/modules/imports/header_own_line_block_comment_prettier_divergence/)
- No-`from` empty export keyword→`{}` (line) → In place flat; tsv indents — [empty_no_from_line_comment](../tests/fixtures/typescript/modules/exports/empty_no_from_line_comment_prettier_divergence/)
- `export`/`export default`→declaration (line) → In place flat; tsv indents — [export_declaration_line_comment](../tests/fixtures/typescript/syntax/comments/export_declaration_line_comment_prettier_divergence/)
- `export default`→value / `export =`→value, **same-line** comment + value on the next line → comment position matches (both trail the keyword/`=`); prettier preserves the author's line break, tsv **reflows the value inline**. The author's break carries no meaning here — a block comment does not run to end-of-line, so nothing forces the tail off the line — and tsv's TS printer reflows this gap at every other value position (`const`/`let` `=`, `type =`, class property, object value, parameter default, arrow body, `:` annotation, return type, `satisfies`, `as`). See [§Authored breaks in value position](./conformance_prettier.md#authored-breaks-in-value-position) — [default_value_same_line_comment](../tests/fixtures/typescript/modules/exports/default_value_same_line_comment_prettier_divergence/), [export_equals_value_same_line_comment](../tests/fixtures/typescript/modules/exports/export_equals_value_same_line_comment_prettier_divergence/)
- Declaration keyword→name (line; `function`/`class`/`enum`/`declare function`) → In place flat; tsv indents — [keyword_name_line_comment](../tests/fixtures/typescript/syntax/comments/keyword_name_line_comment_prettier_divergence/)
- Between `else` and non-block body (line) → Prettier keeps it after `else` on its own line (indented); tsv keeps it trailing `else` on the same line — [else_line_comment_nonblock](../tests/fixtures/typescript/statements/if/else_line_comment_nonblock_prettier_divergence/)
- Union infix `\|` line comment → Trailing on previous member — [union_infix_pipe_line_comment](../tests/fixtures/typescript/types/comments/union_infix_pipe_line_comment_prettier_divergence/)
- Retained paren union member comment → A trailing block comment hoisted after `)`; a leading one stays inside — [union_intersection_retained_paren_comment](../tests/fixtures/typescript/types/union_intersection_retained_paren_comment_prettier_divergence/)
- Retained paren union member line cmt → Comment stays inside parens (both); tsv keeps inner union inline (union-fit), prettier explodes it one-per-line — [union_intersection_retained_paren_line_comment](../tests/fixtures/typescript/types/union_intersection_retained_paren_line_comment_prettier_divergence/)
- Retained paren union leading line cmt → Before the 1st member (out of parens); tsv keeps it inside the parens. Covers a paren-union member, a paren-intersection-with-trailing-object member (whose aligned layout is built from the already-unwrapped inner type — so the paren's own gap must be threaded in, or the comment is silently dropped), and later paren-function / -conditional / -intersection members (for which prettier trails the comment on the previous member) — for every member, not just the first — [union_intersection_retained_paren_leading_line_comment](../tests/fixtures/typescript/types/union_intersection_retained_paren_leading_line_comment_prettier_divergence/)
- Redundant paren member leading line cmt → tsv leads the member with it on its own line before the `| ` (the parens are stripped, so it can't stay inside); prettier trails it on the previous member (`| a // c`). Dual-stable — the stripped-paren counterpart of the retained-paren keep-inside rule; a comment never changes which parens are retained, only where it renders once they are — [union_redundant_paren_member_line_comment](../tests/fixtures/typescript/types/union_redundant_paren_member_line_comment_prettier_divergence/)
- Redundant paren member mixed / trailing cmt (union) → the mixed / trailing extension of the entry above. A redundant paren shell around a **later** union member holds a leading **line** comment with a **leading block** (mixed, `A | (/* b */ // c⏎ B)`) or a **trailing** comment (`A | (// c⏎ B /* t */)`), and the double-nested forms. tsv renders the run between the `| ` members — block and line each on their own line before the member's `| `, a trailing block staying inline after the member (`| A⏎ /* b */⏎ // c⏎ | B`) — keeping every comment where the author wrote it, idempotent in one pass (the pass-1 member-hang reflowed otherwise). Prettier floats the leading run to trail the previous member (`| A /* b */ // c`), dual-stable, so it is a `variant_trailing` exactly as the pure-line sibling — [union_redundant_paren_member_mixed_trailing_line_comment](../tests/fixtures/typescript/types/union_redundant_paren_member_mixed_trailing_line_comment_prettier_divergence/)
- Redundant paren **first** member mixed / trailing cmt (intersection) → the intersection-first-member analog of the union entry above, and the intersection extension of the pure-line [intersection_leading_line_comment](../tests/fixtures/typescript/types/intersection_leading_line_comment_prettier_divergence/) below. A redundant paren shell around the **first** member of an intersection holds a leading **line** comment with a **leading block** (mixed, `(/* b */ // c⏎ A) & B`) or a **trailing** comment (`(// c⏎ A /* t */) & B`), and the double-nested forms. tsv strips the shell and hangs the run at the same fixed point the bare authoring settles on — the leading block trails `=` inline, the line comment forces the value onto its own line, a trailing block trails the member inline, and the intersection stays inline (`type T = /* b */⏎ // c⏎ A & B`) — the keyword→value hang seam (bug188), routed through the first-member hoist. Prettier breaks after `=` and drops the run onto its own line(s). The two cases differ only in prettier's *paren-shell* path: both pass through an unstable member-hang first pass (`A &⏎ B` broken), but the **trailing** case's terminal is the dual-stable own-line form (a `variant_own_line`, `prettier_intermediate_to_variant`) — [intersection_redundant_paren_first_member_trailing_line_comment](../tests/fixtures/typescript/types/intersection_redundant_paren_first_member_trailing_line_comment_prettier_divergence/) — and the **mixed** case's terminal, which keeps prettier's `/* b */ // c` *glued* on one line, is dual-stable too — tsv holds the glue wherever a comment run is emitted ([comments.md](./comments.md) §Trailing and dangling runs), so it is a `variant_glued` reached through the same `prettier_intermediate_to_variant` marker (rule N7c). The two cases differed there only until that seam was stated once — [intersection_redundant_paren_first_member_mixed_line_comment](../tests/fixtures/typescript/types/intersection_redundant_paren_first_member_mixed_line_comment_prettier_divergence/)
- Redundant paren **first** member mixed / trailing cmt (intersection **in a tuple element**) → the tuple-element analog of the entry above: the same `(/* b */ // c A) & B` / `(// c A /* t */) & B` shells, but the intersection is a **tuple element** (`[(…) & B]`) rather than a type-alias RHS. A tuple element is an **own-line** context — the caller already places it on its own indented line — so the first-member hoist keeps the run at the element indent instead of adding the trailing-prefix continuation level (`own_line` in `build_intersection_type_doc`; without it the reparsed bare form was one level shallower, a non-idempotency). The canonical is stable under both formatters, so unlike the type-alias RHS the divergence is only in the shell's normalization: the **mixed** shell's terminal is the dual-stable *glued* form (a `variant`, `prettier_intermediate_to_variant`) — [intersection_redundant_paren_first_member_tuple_mixed_line_comment](../tests/fixtures/typescript/types/intersection_redundant_paren_first_member_tuple_mixed_line_comment_prettier_divergence/) — while the **trailing** shell converges (through the same unstable member-hang first pass) all the way back to `input` (the element is on its own line for both formatters, so no own-line `variant` splits off), pinned by `prettier_intermediate_*` — [intersection_redundant_paren_first_member_tuple_trailing_line_comment](../tests/fixtures/typescript/types/intersection_redundant_paren_first_member_tuple_trailing_line_comment_prettier_divergence/)
- Retained paren member **own-line leading** line cmt (collapsing intersection) → Out of parens, onto its own line before `(`, intersection broken; tsv keeps it inside the parens, forcing the member to break open — [union_intersection_paren_member_own_line_comment](../tests/fixtures/typescript/types/union_intersection_paren_member_own_line_comment_prettier_divergence/)
- Retained paren intersection member cmt → Outside the parens (after `)`/`(`). Covers the plain intersection member and the **trailing-object** one (`(a & { x: X } /* c */)`), whose aligned layout prints its own `(`…`)` — so, like the leading gap, the trailing gap must be emitted there or the comment is silently dropped; a line comment in it keeps its line and drops the `)` to the next — [retained_paren_intersection_member_comment](../tests/fixtures/typescript/types/retained_paren_intersection_member_comment_prettier_divergence/)
- Intersection operator gap, comment **run** straddling the `&` → the multi-comment extension of the member-gap entry below. With comments on BOTH sides of the operator (`A⏎// x⏎& // c⏎B`, and the `A // t⏎& // c⏎B` / own-line-block variants), the `&` is pulled back onto the previous member's line and every comment in the gap follows it. tsv keeps them in the **authored order**, each on its own line; prettier keeps the operator-trailing one on the `&`'s line and drops the earlier ones below it, which **reorders** them. Both are lossless and dual-stable — `input.svelte` is a fixed point for prettier too — so the divergence is only in how the straddling authoring normalizes (an `unformatted_ours_*` + the `audit_signature_<suffix>` chain pin, prettier being non-idempotent from that source). ⚠️ Emitted back-to-back these comments **welded** (`// x // c`, one comment whose text holds the second) — the separator-before rule, at this gap — [intersection_operator_gap_comment_run](../tests/fixtures/typescript/types/intersection_operator_gap_comment_run_prettier_divergence/)
- Retained paren intersection **operator** gap cmt → the member→member (`&`) gaps of the same trailing-object shell as the entry above, which prints them as fused text (`" & "`, `" & {"`) — so, like that shell's leading and trailing gaps, they must be emitted there or the comment is silently dropped. A **block** matches prettier: both keep it on the side of the `&` the author chose (`(a /* c */ & { x: X }) | c`, `(a & /* c */ { x: X }) | c`), the shell's own answer to the bare rule below — plain [retained_paren_intersection_operator_gap_comment](../tests/fixtures/typescript/types/retained_paren_intersection_operator_gap_comment/). A **line** comment sharing the `&`'s line diverges: prettier relocates it out of the parens to trail the whole member (`| (a & { x: X }) // c`), tsv keeps its line and drops the object to the next one (`| (a & // c⏎{ x: X })`). An **own-line** comment between the `&` and the object matches — [retained_paren_intersection_operator_gap_line_comment](../tests/fixtures/typescript/types/retained_paren_intersection_operator_gap_line_comment_prettier_divergence/)
- Retained paren shell trailing gap, comment **run** → the ≥2-comment extension of the two entries above (union shell and intersection-trailing-object shell alike). Placement is theirs — prettier hoists the run out, tsv keeps every comment inside the shell — and what the run adds is that the comments stay **distinct**: emitted with nothing between them a `//` welds the next comment into its own text (`// c1 // c2`), and a *block* after a line comment additionally **reorders** ahead of it (block inline vs `line_suffix`). Each comment following a line comment takes a break before it; two **glued blocks** keep sharing their line, so the separator is conditional on what precedes it. The run continues at the column of the region its shell emits it into — the `)`'s `align(2)` offset for the intersection shell, the interior indent for the union one — [retained_paren_shell_trailing_comment_run](../tests/fixtures/typescript/types/retained_paren_shell_trailing_comment_run_prettier_divergence/). The rule is the shared emitter's, not the shell's: the same run in the **prettier-ignore-frozen** indexed-access route welds identically, pinned by case `B` of [indexed_access_prettier_ignore_index](../tests/fixtures/typescript/types/indexed_access_prettier_ignore_index_prettier_divergence/), and a **stripped** paren or a plain indexed access takes the same in-place treatment as the retained shell, rather than letting the run escape to trail the statement the way prettier does — the Type-suffix trailing entry above, [type_suffix_trailing_comment_run](../tests/fixtures/typescript/types/type_suffix_trailing_comment_run_prettier_divergence/)
- Type alias head to `=` (line, union values) → With an **inline union** value prettier relocates to end-of-statement (`type A<X> = B | C; // c` — the merge-prone declarator destination); with a **break-forced non-hugging union** it crosses the `=` non-idempotently (glued pass, then own-line leading the first member — dual-stable `variant_own_line`). tsv keeps the comment trailing the head + continuation indent in both; on the broken union the `=` drops onto its own line and members hang at the `=` level (not one deeper). Own-line authorings pull up under tsv, to the hang under prettier — [type_alias_line_pre_equals](../tests/fixtures/typescript/types/comments/type_alias_line_pre_equals_prettier_divergence/), [type_alias_line_pre_equals_break](../tests/fixtures/typescript/types/comments/type_alias_line_pre_equals_break_prettier_divergence/). The non-union face (prettier hangs, no float) is the [name_before_eq_line_comment](../tests/fixtures/typescript/types/aliases/name_before_eq_line_comment_prettier_divergence/) entry above
- Type param keyword to value (own-line) → Up onto the keyword line — [type_param_keyword_own_line_comment](../tests/fixtures/typescript/types/comments/type_param_keyword_own_line_comment_prettier_divergence/)
- Function param default to value (line) → Floated out to trail the whole parameter (after the value); tsv keeps it after `=` and drops the value to a continuation line indented one level (the uniform forced-continuation indent, matching the declarator's value hang) — [param_default_line_comment](../tests/fixtures/typescript/declarations/function/param_default_line_comment_prettier_divergence/)
- Name/key to default `=` (line) → Floated out to trail the whole binding (after the value); the *before*-`=` counterpart of the row above, at a parameter default and every destructuring default (object/array/param binding). tsv keeps it trailing the name with `= value` on the next line (a `//` there would otherwise swallow the `=`) — [default_equals_line_comment](../tests/fixtures/typescript/expressions/destructuring/default_equals_line_comment_prettier_divergence/)
- `as`/`satisfies` keyword to type (line) → Statement-trailing (after `;`) — [as_satisfies_value_line_comment](../tests/fixtures/typescript/expressions/as_satisfies_value_line_comment_prettier_divergence/)
- **Redundant paren shell at a keyword→value hang, mixed / trailing comment** → the mixed / trailing extension of the pure-line seam above. A redundant paren shell around the value of a keyword→value hang site whose leading gap holds a **block before the line comment** (mixed, `(/* b */ // c⏎ X)`), or whose trailing gap holds a **comment after the value** (trailing, `(// c⏎ X /* t */)`), and the double-nested `((…))` forms. tsv strips the shell and hangs the whole run at the same fixed point the bare (paren-free) authoring settles on — the leading block trails the keyword inline, the line comment forces the value onto its own line, and the trailing comment trails the value (a cast, being a value position, defers a trailing block past the statement `;` via `line_suffix`; type positions keep it inline). Prettier floats or reorders the comments per site. Across every hang site: the `as`/`satisfies` cast — [as_satisfies_value_mixed_comment](../tests/fixtures/typescript/expressions/as_satisfies_value_mixed_comment_prettier_divergence/), [as_satisfies_value_trailing_comment](../tests/fixtures/typescript/expressions/as_satisfies_value_trailing_comment_prettier_divergence/) — the prefix type-operator (`keyof`/`typeof`/`readonly`) — [type_operator_keyword_mixed_trailing_comment](../tests/fixtures/typescript/types/type_operator_keyword_mixed_trailing_comment_prettier_divergence/) — the type-predicate `is` — [predicate_is_mixed_trailing_comment](../tests/fixtures/typescript/types/predicate_is_mixed_trailing_comment_prettier_divergence/) — the mapped-type value — [mapped_value_mixed_trailing_comment](../tests/fixtures/typescript/types/mapped_value_mixed_trailing_comment_prettier_divergence/) — the `: T` annotation — [annotation_mixed_trailing_comment](../tests/fixtures/typescript/types/comments/annotation_mixed_trailing_comment_prettier_divergence/) — the type-parameter `= default` / `extends` constraint — [constraint_default_mixed_trailing_comment](../tests/fixtures/typescript/types/type_params/constraint_default_mixed_trailing_comment_prettier_divergence/) — and the type-alias `= T` RHS (its own `build_type_alias_eq_value_doc`, routed through the same seam) — [type_alias_rhs_mixed_trailing_comment](../tests/fixtures/typescript/types/comments/type_alias_rhs_mixed_trailing_comment_prettier_divergence/)
- **Redundant paren shell trailing block at a value-position cast, no line comment** → the no-line-comment sibling of the hang entry above, and a distinct mechanism (bug188's line-comment seam never fires). A redundant paren shell around an `as`/`satisfies` cast type whose trailing gap holds a **block** comment with **no leading line comment** (`(Z /* t */)`, `(/* b */ Z /* t */)`, and the double-nested forms). Because a cast is a **value** position, tsv strips the shell and defers the trailing block past the statement `;` (via `line_suffix`) — a leading block trails the keyword inline — reaching `x as Z; /* t */` in one pass where prettier takes two (prettier is non-idempotent on the shell but converges to the same fixed point, so the shells are `unformatted_ours` + `prettier_intermediate`, not an end-state divergence). Only the two value-position casts need this; every type position (`keyof`, mapped, `: T`, `is`, type-param, conditional `extends`, union/intersection member) keeps a trailing block inline and is already idempotent — [as_satisfies_value_trailing_block_comment](../tests/fixtures/typescript/expressions/as_satisfies_value_trailing_block_comment_prettier_divergence/)
- `as const` keyword to `const` → A block comment moves before the whole expression (`1 /* c */ as const`); a line comment floats past the statement (`1 as const; // c`). tsv keeps either between `as` and `const` (`1 as /* c */ const`; the line comment trails the keyword with `const` on the next line), matching the regular `as <type>` case that both formatters preserve. Unique to the `const` assertion — `as <type>`, `as <literal>`, and `satisfies const` all stay put in both — [as_const_keyword_comment](../tests/fixtures/typescript/expressions/as_const_keyword_comment_prettier_divergence/)
- **`as`/`satisfies` operand to keyword, and postfix `++`/`--` operand to operator (line / multiline block)** → Statement-trailing (after `;`); tsv **keeps the operand's grouping parens** and holds the comment inside them (`(⏎\t x // c⏎) as A`, `(⏎\t a // c⏎)++`). The mirror image of the keyword→value entry above, and the one place where a redundant paren shell is **not** stripped: both gaps are `[no LineTerminator here]`, so the keyword/operator may not start a line and a comment spanning lines can only ever have been authored *inside* a shell — a shell is redundant only when the stripped form can still express the comment's position. Inlining was **content loss**: the `//` swallowed the whole ` as T;` / `++;` tail, and the mangled output still parsed (as a bare operand statement), so it was a fixed point and F1 / round-trip were both blind. A multiline block was worse — it put a real line break in the gap, so the output did not re-parse at all, which prettier still does ([§Prettier bug index](./conformance_prettier.md#prettier-bug-index)). The retained shell **expands** — the operand takes its own indented line rather than gluing to the `(` — for every comment that makes the shell span lines, so the two authorings of one comment (`(⏎\t x // c⏎)` and `(x /* m1⏎m2 */)`) reach one form instead of disagreeing at the same gap; that is the rule a breaking paren already follows elsewhere (`build_expanded_parenthesized_union_opt`). The shell's **other** gap keeps it for a different reason — nothing else emits the `(`→operand gap, so a comment there that is neither glued to the operand (hence `owned_by_node`) nor inside its span belonged to no node and was **dropped** outright (`( // c⏎x) as A` → `x as A`); retaining on either gap is one rule for one shell. A `//` on the `(` line stays on it, an own-line block keeps its own line, a glued block leads the operand inline. One predicate (`asi_gap_needs_parens`) across every spelling: plain `as`, `satisfies`, `as const`, chained casts, an object operand, an operand that needed the parens anyway (`(a + b // c⏎) as A`, `(c as T // c⏎)++`), and a member operand. Stacked line comments show why the position is kept rather than floated — prettier merges *and reorders* them at the cast (`x as // c8 // c7`, so `// c7` becomes text). A single-line block forces nothing and stays inline in both (`x /* c */ as A`, `f /* c */++`) — [as_satisfies_operand_line_comment](../tests/fixtures/typescript/expressions/as_satisfies_operand_line_comment_prettier_divergence/), [update_postfix_paren_line_comment](../tests/fixtures/typescript/expressions/unary/update_postfix_paren_line_comment_prettier_divergence/)
- Angle-bracket type assertion, trailing `<` / trailing `>` (line) → trailing-`<` onto its own line; trailing-`>` into the cast, trailing the type (`<string> // c` → `<⏎string // c⏎>z`, tsv keeps it after `>` leading the expression one indent in). Own-line-after-`<` and trailing-the-type break the cast in both formatters and match — [type_assertion_line_comment](../tests/fixtures/typescript/types/type_assertion_line_comment_prettier_divergence/)
- Angle-bracket type assertion, own-line before `>` after a trailing-type comment (line) → No fixed point (prettier oscillates the comment across `>`); tsv keeps it on its own line before `>` inside the cast — [type_assertion_close_own_line_comment](../tests/fixtures/typescript/types/type_assertion_close_own_line_comment_prettier_divergence/)
- Angle-bracket type assertion, own-line after `>` before the expression (line) → Into the cast trailing the type, over two passes (pass 1 glues it onto `>`, pass 2 moves it inside — F4 audit signature); tsv keeps it on its own line leading the expression — [type_assertion_expr_own_line_comment](../tests/fixtures/typescript/types/type_assertion_expr_own_line_comment_prettier_divergence/)
- Angle-bracket type assertion robustness (object operand after `>`; block+line trailing `<`) → operand-after-`>` comment relocated into the cast, trailing-`<` block+line moved to their own line; tsv preserves each. A generic cast type with a nested `>` matches prettier (the close `>` is found past the type's own) — [type_assertion_line_comment_robustness](../tests/fixtures/typescript/types/type_assertion_line_comment_robustness_prettier_divergence/)
- Heritage keyword to type (line) → Up before the keyword — [extends_keyword_line_comment](../tests/fixtures/typescript/class/extends_keyword_line_comment_prettier_divergence/)
- Heritage list, line comments across elements → first element's leading comment Up before the keyword; tsv keeps each comment where written (leading first element after the keyword, per-element trailing/leading intact). When a keyword→first-element line comment forced the items onto their own lines, the list was joined with a plain `", "`, so a per-element line comment swallowed the next element (`// c1, B` — non-reparseable content loss); the list now joins comma-baking-aware. `extends` and `implements` alike — [heritage_element_line_comment](../tests/fixtures/typescript/class/heritage_element_line_comment_prettier_divergence/)
- Heritage keyword to type (own-line block) → tsv collapses inline (`class P extends /* c */ Q`); Prettier keeps the comment on its own line before the keyword (`class P⏎/* c */⏎extends Q`, stable — it does not inline it). See the keyword→value block-comment collapse rule below — [heritage_keyword_own_line_block_comment](../tests/fixtures/typescript/class/heritage_keyword_own_line_block_comment_prettier_divergence/)
- Conditional `extends` to check (line) → Trailing the extends-type — [check_extends_line_comment](../tests/fixtures/typescript/types/conditional/check_extends_line_comment_prettier_divergence/)
- Conditional `extends` redundant paren shell, mixed / trailing cmt → a redundant paren shell around the `extends`-type whose leading gap holds a **line** comment alongside a **leading block** (mixed, `(/* b */ // c⏎ B)`) or a **trailing** comment (`(// c⏎ B /* t */)`), and the double-nested forms. tsv strips the shell and **hangs** the run at the same fixed point the bare authoring settles on (`A extends /* b */ // c⏎ B` / `A extends // c⏎ B /* t */`) — the leading block trails `extends` inline, the line comment forces the extends-type onto its own line, a trailing block trails it inline. The **pure-line** shell keeps its own trail-on-inner canonical (`A extends B // c`, the non-divergence [extends_paren_leading_line_comment](../tests/fixtures/typescript/types/conditional/extends_paren_leading_line_comment/)); only mixed/trailing hang, because trailing a leading block would move it leading→trailing and the trailing case would reorder (a `//` must end its line). Prettier floats the block before `extends` (`A /* b */ extends B // c`) and reorders the trailing case (`A extends B /* t */ // c`) — [extends_paren_mixed_trailing_line_comment](../tests/fixtures/typescript/types/conditional/extends_paren_mixed_trailing_line_comment_prettier_divergence/)
- Conditional extends-type to `?` (own-line line) → Relocated across the `?` and indented with the true branch (`? // c⏎\tD`); tsv keeps an own-line comment on its own line above the `?`, which is what the value-level conditional already does for the identical authoring (`a instanceof B⏎// c⏎? x : y`) — the two conditional printers give one answer via the shared same-line-trails / later-line-breaks classification. The gap's **same-line** authoring is unchanged and non-divergent (`B extends C // c`, case H of [extends_question_block_comment](../tests/fixtures/typescript/types/conditional/extends_question_block_comment/)). Pulling an own-line run onto the extends-type's line merges it (`B extends C // c1 // c2`), the loss [consecutive_branch_comment](../tests/fixtures/typescript/types/conditional/consecutive_branch_comment/) states the rule against for the `?`→branch gap — [extends_question_own_line_line_comment](../tests/fixtures/typescript/types/conditional/extends_question_own_line_line_comment_prettier_divergence/)
- Mapped `:` to value (line) → Breaks the `[K in T]` brackets, trailing the comment after the key type — [mapped_value_line_comment](../tests/fixtures/typescript/types/mapped_value_line_comment_prettier_divergence/)
- Mapped key to `]` (line) → Both break the `[K in T]` brackets and keep the comment trailing the key with `]` on its own line; prettier also drops `K in T` to its own indented line (tsv keeps it on the `[` line) — [mapped_key_line_comment](../tests/fixtures/typescript/types/mapped_key_line_comment_prettier_divergence/)
- Mapped member without a value type, trailing block → Into the brackets, after the key constraint (`{ [K in B /* c */] }`); tsv keeps it trailing the member, after `]` or the optional modifier — [mapped/no_value_comment](../tests/fixtures/typescript/types/mapped/no_value_comment_prettier_divergence/)
- Mapped `]`→value-`:` (block) → Into the brackets, trailing the key constraint (`[K in keyof T /* c */]: V`), one pass from the inline authoring — so unlike the index-signature sibling, the inline authoring is itself a divergence. tsv keeps the comment in its authored gap before the `:` (`[K in keyof T] /* c */ : V` — the index-signature treatment, spaced parity), runs in order and each comment distinct. The own-line authoring collapses to the same inline form under tsv (own-line-ness is authoring signal for a leading position, not a trailing one); prettier walks it to the in-bracket relocation via an unstable pass across the `:` (`[K in keyof T]: /* c */⏎V` — pinned `prettier_intermediate_to_variant_own_line` → `variant_own_line`, one comment per pass, so the own-line run's 3-pass chain is README-recorded). The relocated landing is dual-stable, so the two authorings keep two fixed points — [mapped_bracket_colon_comment](../tests/fixtures/typescript/types/mapped_bracket_colon_comment_prettier_divergence/)
- Mapped optional-marker gaps, block (`]`→`?` and `?`/`+?`/`-?`→value-`:`) → Into the brackets, trailing the key constraint (`[K in keyof T /* c */]?: V`), erasing which side of the marker the author put the comment on; tsv keeps each comment on its authored side — before the marker (`[K in keyof T] /* c */?: V`) or trailing it before the `:` (`[K in keyof T]? /* c */ : V`), the property-signature parity. The relocated landing is dual-stable — [mapped_optional_marker_comment](../tests/fixtures/typescript/types/mapped_optional_marker_comment_prettier_divergence/)
- Mapped `]`→value-`:` (line) → Breaks the brackets and relocates the comment inside them, trailing the key constraint on its own indented line (`[⏎K in keyof T // c⏎]: V`); tsv keeps the comment after `]` and drops the value `:` to a continuation line indented one level (uniform forced-continuation indent — the index-signature layout). The own-line `//` authoring pulls up to the same continuation form under tsv, while prettier reaches its in-bracket landing via an unstable pass across the `:` (`[K in keyof T]: // c⏎V`); that landing is prettier-stable but not tsv-stable — tsv rewrites it to its own bracket-break form keeping `K in keyof T` on the `[` line (the mapped-key-to-`]` layout above), pinned `prettier_intermediate_to_divergent_variant_own_line` → `divergent_variant_own_line` — [mapped_bracket_colon_line_comment](../tests/fixtures/typescript/types/mapped_bracket_colon_line_comment_prettier_divergence/)
- Mapped `]`→value-`:` (≥2 line comments) → No fixed point (prettier oscillates the second comment between trailing the value `:` and inside the brackets); tsv keeps each on its own line, value `:` on a continuation line — [mapped_bracket_colon_multi_comment](../tests/fixtures/typescript/types/mapped_bracket_colon_multi_comment_prettier_divergence/)
- Mapped **before**-keyword gaps, key→`in` and constraint→`as` (line) → Breaks the `[…]` brackets and floats the comment *past* the keyword to the end of the whole binding (`[⏎K in T // c⏎]: V`), re-binding it from the name to the constraint; tsv keeps it trailing the head and drops the `in T` / `as U` tail to a continuation line indented one level — the mapped face of the type-parameter pre-keyword gaps (name→`extends`, pre-`=`), [§Uniform Forced-Continuation Indent](./conformance_prettier.md#uniform-forced-continuation-indent). Inlining is content loss, not a layout choice: the `//` swallows the keyword and its operand. Prettier's float is **information-destructive on a run** — the two comments merge onto one line **in reverse order** (`K in // c2 // c1⏎T`, the second `//` becoming text) and collapse to `K in T // c2 // c1` on the next pass (the chain `audit_signature.txt` pins); tsv keeps each distinct, in order. The own-line authoring pulls up to the same continuation form under tsv, while prettier takes two passes to reach its own landing — a chain no `prettier_intermediate*_*` marker can express (it converges on `output_prettier`, not `input`/`variant_*`/`divergent_variant_*`), so it is README-recorded rather than pinned. A **single-line block** stays inline in both (`[K /* c */ in T]`), and a **multiline** block the author broke after glues to the keyword in both — the broke-after continuation rule is scoped to value-separator gaps, and prettier does not distinguish the broke-after authoring here (the switch-case head→`:` precedent), so the gate is line-comments-only — [mapped/before_keyword_line_comment](../tests/fixtures/typescript/types/mapped/before_keyword_line_comment_prettier_divergence/)
- Named-tuple member label→`:` (line), incl. the optional `?`→`:` and rest-member gaps → Across the `:` **and past the element type**, to the end of the member (`[b: T // c]`) — the merge-prone destination: a run merges onto one line **in reverse order** (`b: // c2 // c1⏎T`) before collapsing to `b: T // c2 // c1` (the chain `audit_signature.txt` pins), the second `//` becoming text. tsv keeps the comment trailing the label and drops the whole `: element` tail to a continuation line indented one level, each comment distinct and in order — the named-tuple face of the [before-`:` key/binding gap](./conformance_prettier.md#uniform-forced-continuation-indent), which the member's own label→`?` gap already took. The own-line authoring pulls up to the same form under tsv in one pass; prettier needs two and lands on `output_prettier`, a chain no `prettier_intermediate*_*` marker can express, so it is README-recorded. A block stays inline in its authored gap (`[b /* c */: T]`, a match — [tuple_label_comment](../tests/fixtures/typescript/types/tuple_label_comment/)), and a broke-after **multiline** block glues in both formatters, so the gate is line-comments-only (as at the switch-case head→`:`) — [tuple/label_colon_line_comment](../tests/fixtures/typescript/types/tuple/label_colon_line_comment_prettier_divergence/)
- Type predicate `is` to type (line) → Trailing the body `{` — [predicate_is_line_comment](../tests/fixtures/typescript/types/predicate_is_line_comment_prettier_divergence/)
- Property signature `:` to union type (line) → Onto its own line after `:`; tsv trails it on `:` + indents the union — [annotation](../tests/fixtures/typescript/types/comments/annotation_prettier_divergence/)
- Destructuring binding `:` to union type (≥1 line) → Each onto its own line after `:`; tsv trails the first on `:` + indents — [annotation_multiple_line](../tests/fixtures/typescript/types/comments/annotation_multiple_line_prettier_divergence/)
- Type alias `=` to value type (line) → Onto its own line after `=`; tsv trails it on `=` + indents — [type_alias_equals](../tests/fixtures/typescript/types/comments/type_alias_equals_prettier_divergence/)
- Intersection first-member leading line cmt (after `=`) → Onto its own line after `=`; tsv trails it on `=` + indents — [intersection_leading_line_comment](../tests/fixtures/typescript/types/intersection_leading_line_comment_prettier_divergence/)
- Intersection member-gap **block** sharing the `&`'s line (`X⏎& /* a */⏎Y`) → Prettier re-binds it to the previous member and prints it **before** the `&` (`X /* a */ & Y`, dual-stable); tsv keeps it on the side of the operator the author chose and the intersection stays inline. The mirror authoring (`X⏎/* b */ &⏎Y`, the comment leading a line with the `&` glued behind it) is prettier's own mirror image — it re-binds *forward* to `X & /* b */ Y` — so the two authorings have one fixed point in tsv and two in prettier, each of prettier's stable in both formatters (`variant_comment_before_operator` / `variant_comment_after_operator`). A glued **run** the author gave its own line (`X &⏎/* c1 */ /* c2 */⏎Y`) collapses inline in **both**: neither comment is isolated, since each is adjacent to the other. An **isolated** block (a line of its own on both sides) still forces one member per line in both, as does any line comment — plain [intersection_member_own_line_comment](../tests/fixtures/typescript/types/intersection_member_own_line_comment/) — [intersection_member_operator_gap_comment](../tests/fixtures/typescript/types/intersection_member_operator_gap_comment_prettier_divergence/)
- Intersection member trailing line cmt, member glued to a following member → `lineSuffix`-relocated past the object-adjacency-glued members to the end of the visual line — across a member boundary (`{ c } // c⏎& D` → trails `D`), and past `;` (`B // c⏎& { c }` → trails the whole type, which then collapses inline); tsv keeps it on its member's line and breaks that boundary, gluing the object-adjacent members before it. Only a trailing comment on a member glued to a *following* member diverges — one landing at a neither-object boundary matches (regular [comments/intersection_object_adjacent_line](../tests/fixtures/typescript/types/comments/intersection_object_adjacent_line/)) — [intersection_glued_trailing_line_comment](../tests/fixtures/typescript/types/intersection_glued_trailing_line_comment_prettier_divergence/)
- Single generic type-argument `<` trailing (line) → Onto its own line; tsv keeps it trailing `<` and expands the list below it (`Foo< // c⏎\ta⏎>`) — the **N=1 form of the multi-argument entry below**, not a construct of its own: same delimiter-line placement, same indented body, same dangling `>`. It holds whatever the argument's shape, including from the paren-stripped spelling (`Foo<(// c⏎a | b)>`, the parens being redundant in type-argument position), and through the call/`new` instantiation builder. A comment on a *later* line leads its argument inside the list as usual, so only the `<`-line comment stays on the `<` line — [type_position_parens_leading_line_comment](../tests/fixtures/typescript/types/type_position_parens_leading_line_comment_prettier_divergence/), [type_argument_paren_union_leading_line_comment](../tests/fixtures/typescript/types/type_argument_paren_union_leading_line_comment_prettier_divergence/)
- Type-parameter `extends` constraint under a long trailing `=` default comment → Breaks after `extends`; tsv treats the trailing line comment as zero-width and keeps the constraint inline — [type_param_keyword_line_comment](../tests/fixtures/typescript/types/comments/type_param_keyword_line_comment_prettier_divergence/)
- Call arg after-comma block + same-line line comment → Block relocated before the comma (tsv keeps it on the comma line) — [plain](../tests/fixtures/typescript/expressions/calls/nonlast_arg_after_comma_block_then_line_prettier_divergence/), [new](../tests/fixtures/typescript/expressions/calls/new_nonlast_arg_after_comma_block_then_line_prettier_divergence/), [joined](../tests/fixtures/typescript/expressions/calls/multiline_arg_nonlast_after_comma_block_then_line_prettier_divergence/), [chain](../tests/fixtures/typescript/expressions/calls/chained/nonlast_arg_after_comma_block_then_line_prettier_divergence/), [import](../tests/fixtures/typescript/expressions/calls/import_inter_arg_block_then_line_prettier_divergence/)
- Element-list after-comma block + same-line line comment → Block relocated before the comma; tsv keeps it on the comma line, in front of the line comment, the call-argument rule above applied at the element-list sites. Source order is what forces it: the line comment defers through `line_suffix`, so a block left to lead the next element renders *after* it and the authored pair comes back reversed across two lines. A block with **no** line comment after it has nothing to defer behind and keeps leading the next element — the [end_of_line_block_comment](#comment-relocation) rule below, unchanged. Object literal, both destructuring patterns, the import/export specifier list and the enum-member loop share one element-comma emitter, so they answer this identically; the array literal reaches the same rule through its own printer's paired trailing/leading predicate — [object](../tests/fixtures/typescript/expressions/objects/after_comma_block_then_line_prettier_divergence/), [array](../tests/fixtures/typescript/expressions/arrays/after_comma_block_then_line_prettier_divergence/), [destructuring](../tests/fixtures/typescript/expressions/destructuring/after_comma_block_then_line_prettier_divergence/), [specifier](../tests/fixtures/typescript/modules/imports/specifier_after_comma_block_then_line_prettier_divergence/), [enum](../tests/fixtures/typescript/declarations/enum/member_after_comma_block_then_line_prettier_divergence/)
- Call arg after-comma block, **stranded** (newline before the next arg) → Block relocated before the comma (tsv respects the newline and keeps the stranded block on the comma line). A block instead **hugging** the next arg leads it (`C`) and both formatters agree (plain match) — [stranded](../tests/fixtures/typescript/expressions/calls/nonlast_arg_after_comma_block_stranded_prettier_divergence/), [import](../tests/fixtures/typescript/expressions/calls/import_inter_arg_stranded_prettier_divergence/) (dynamic `import()` source→options gap; hugging match at [import_inter_arg_comment](../tests/fixtures/typescript/expressions/calls/import_inter_arg_comment/))
- Comma-separated-item after-comma block, **stranded** (newline before the next item) → Block relocated before the comma; tsv keeps the stranded block on the comma line (`a, /* c */⏎ b`), a before-comma and a stranded after-comma block in the same gap each preserved independently. One rule (`is_stranded_after_comma_block`) across every site listed below: a block **hugging** the next item leads it (both formatters agree); a **stranded** block stays trailing the comma. The **element-comma family** — object literal, both destructuring patterns, array literal, import/export specifiers, enum members — is deliberately *not* on this rule; there a stranded block still leads the next element (the §Array element end-of-line block comment rule above), and only a following same-line `//` pulls it back to the comma line (the §Element-list after-comma block entry below). The stranded block is a **stable** form only when the items sit on separate lines: the multi-declarator statement always breaks its declarators, while the for-init, heritage (`extends`/`implements`), and function-parameter-list gaps collapse to one line when they fit (there the block hugs the next item and both agree) and diverge the same way only once the items wrap. The **type-side lists** — tuple types, type-parameter declarations, type-argument lists, and the parameter lists of function/constructor types and call/construct/method signatures — are on the rule for the same reason, through the one forced-multiline comma emitter they share (`emit_multiline_comma_with_comments`): a list that fits stays flat and hugs, and only a list forced multiline (a line comment in it, an own-line comment, an author blank line) puts the items on separate lines where the stranded form is stable. The regular heritage [interface](../tests/fixtures/typescript/declarations/interface/heritage_item_after_comma_block/) / [class](../tests/fixtures/typescript/declarations/class/heritage/heritage_item_after_comma_block/) fixtures pin the hugging case (leads the next item, matching prettier), as do the type-side [tuple](../tests/fixtures/typescript/types/tuple/element_after_comma_block/), [type-parameter](../tests/fixtures/typescript/types/type_params/param_after_comma_block/) (declaration + type-argument list) and [function/constructor-type](../tests/fixtures/typescript/types/function_type/param_after_comma_block/) fixtures. Function-parameter lists trail the comma like every other site (function declarations, methods, arrows, constructors, Svelte `{#snippet}` params, plus function/constructor **types** via their separate printer). Stranded fixtures: [declarator](../tests/fixtures/typescript/declarations/variable/multiple/after_comma_block_stranded_prettier_divergence/), [for-init](../tests/fixtures/typescript/statements/for/init_after_comma_block_stranded_prettier_divergence/), heritage [interface](../tests/fixtures/typescript/declarations/interface/heritage_item_after_comma_block_stranded_prettier_divergence/) / [class](../tests/fixtures/typescript/declarations/class/heritage/heritage_item_after_comma_block_stranded_prettier_divergence/), function-param [param](../tests/fixtures/typescript/syntax/comments/param_after_comma_block_stranded_prettier_divergence/), function/constructor-type [param](../tests/fixtures/typescript/types/function_type/param_after_comma_block_stranded_prettier_divergence/), type-member **signature** [param](../tests/fixtures/typescript/types/signature_param_after_comma_block_stranded_prettier_divergence/) (method / construct / bodyless `declare`, which share one printer and used to answer this gap a THIRD way — the whole gap as the previous param's trailing run, putting the comment *before* the comma; its hugging counterpart is [signature_param_after_comma_block](../tests/fixtures/typescript/types/signature_param_after_comma_block/)), [tuple](../tests/fixtures/typescript/types/tuple/element_after_comma_block_stranded_prettier_divergence/), [type-parameter](../tests/fixtures/typescript/types/type_params/param_after_comma_block_stranded_prettier_divergence/)
- **Last**-item trailing-comma line comment (`b: B⏎, // c⏎)`) → Hoisted onto the last item (`b: B // c`); tsv keeps the line the author gave it. A **normalization** divergence only: once written on its own line both formatters reproduce that form byte for byte (so the fixture carries no `output_prettier.svelte`), and only the authoring splits them. The rule is the after-comma **anchor** and where its argument stops. Everywhere else in a comma-separated list that anchor is the comma, because the printer pulls the comma back onto the previous item's line whatever the author did with it — so a comment written against it trails that item (the entry directly below). A last item's comma is **deleted** under `trailingComma: 'none'`, so it is not a position in the output at all, the argument does not reach it, and the comment stays where it was written — the same form both formatters already agree on when the author writes no trailing comma ([trailing_param_comment](../tests/fixtures/typescript/syntax/comments/trailing_param_comment/) `fn2`). One rule across every list that can carry a trailing comma: function and arrow parameters, function/constructor-type parameters, tuple elements, and type-parameter declarations — [last_item_line_after_comma](../tests/fixtures/typescript/syntax/comments/last_item_line_after_comma_prettier_divergence/)
- Comma-separated-item after-comma **line** comment (`a⏎, // c⏎ b`) → Both formatters trail it past the comma (`a, // c`), so the plain authorings match; the fixtures pin the anchor that gets them there and the run bound that keeps it lossless. The claim reads the **comma's** line, not the item's: an author who pushed the comma onto a line of its own wrote the comment against the comma, and an item-anchored reading hands it to the next item's leading run, re-binding it to an item it was never written against. At most **one** comment can trail — a `//` ends its output line, so a second welds onto it (`// c1 // c2`, the second delimiter becoming text) and the code behind it is swallowed — so the run ends at the first `//` the gap emits, from either side of the comma, and the rest leads the next item on its own line, where prettier puts it too — [param](../tests/fixtures/typescript/syntax/comments/param_line_before_comma/), [around-comma](../tests/fixtures/typescript/syntax/comments/line_comments_around_comma/)
- Function-definition open paren `(`, **block** comment with the first parameter below it → OUT of the parentheses, to between the name and the `(` (`function fn( /* c */⏎a)` → `function fn /* c */(a) {}`, dual-stable); tsv keeps it inside the list leading the first parameter — on the parameter's line when the list fits, above it when the list breaks, since the leading separator there is prettier's own soft `line`. The two settled forms (glued to the parameter, and alone on its own line) agree in both, so the divergence is only in how the `( /* c */⏎a)` authoring normalizes. A function **type** is the control — prettier does not hoist there — [open_paren_block_comment](../tests/fixtures/typescript/declarations/function/open_paren_block_comment_prettier_divergence/)
- Call open paren `(` trailing → Onto its own line — [open_paren_comment](../tests/fixtures/typescript/expressions/calls/open_paren_comment_prettier_divergence/), [chain](../tests/fixtures/typescript/expressions/calls/chain_open_paren_comment_prettier_divergence/), [new](../tests/fixtures/typescript/expressions/calls/new_open_paren_comment_prettier_divergence/)
- Callee→empty argument list line comment → Continuation kept **flush** (`call // c⏎()`); tsv indents it one level per [§Uniform Forced-Continuation Indent](./conformance_prettier.md#uniform-forced-continuation-indent). Prettier also **expands the enclosing member chain** (`a⏎ .b // c⏎ ()`, where tsv keeps the chain flat and breaks only where the `//` requires it). For an optional call **both formatters** normalize every authoring to the comment-**before**-`?.` form (`call // c⏎ ?.()`, `?.` fused into the opening `?.(`, for either callee kind): `?.` is pure structure in this gap and the comment trails the callee either way, so the side carries no authorship signal and the normalization is lossless — the pure-separator carve-out, so tsv matches rather than holding a side; the optional shapes then diverge only by the indent and chain-layout differences above. A block comment forces nothing and stays inline in both, glued before `?.` in the optional forms (`call /* c */?.()`, a byte-level match on a plain callee; a member callee still differs by the chain expansion). The complement of the open-paren entry above — that one is a comment *inside* `(`, this one is the gap *before* it. Inlining here was **content loss**: the `//` swallowed the call's own parens and the `;`, and the mangled output was a fixed point — [callee_line_comment_empty_args](../tests/fixtures/typescript/expressions/calls/callee_line_comment_empty_args_prettier_divergence/)
- Test-call (`it`/`test`/`describe`) argument-gap comment → Kept on the flat one-line layout, relocated onto the `(` line (leading gap) or past the previous argument (inter-argument gap), and prettier's own later passes then re-bind an inter-argument comment **into the callback body** (pass 2 `() => { // c`, pass 3 its own line inside the body — `audit_signature.txt` pins the chain). tsv gives up the flat layout for a call whose gaps hold a comment to emit: it expands like any other call and every comment keeps its line. Also a content-loss fix — the flat layout has no gap emitter at all, so those comments were dropped ([comments.md](./comments.md) hazard 4). A **glued** block comment is owned by its argument and rides inside that argument's doc, so it keeps the flat layout and matches prettier; so does a comment trailing the whole call — [test_call_arg_comment](../tests/fixtures/typescript/expressions/calls/test_call_arg_comment_prettier_divergence/)
- Object literal `{` trailing → Onto its own line — [open_brace_comment](../tests/fixtures/typescript/expressions/objects/open_brace_comment_prettier_divergence/)
- Array literal `[` trailing → Onto its own line — [open_bracket_comment](../tests/fixtures/typescript/expressions/arrays/open_bracket_comment_prettier_divergence/)
- Block body `{` trailing → Onto its own line — [block_open_brace_comment](../tests/fixtures/typescript/statements/block_open_brace_comment_prettier_divergence/)
- Type-parameter `<` trailing → Onto its own line — [open_angle_comment](../tests/fixtures/typescript/types/type_params/open_angle_comment_prettier_divergence/)
- Function/constructor-type `(` trailing → Onto its own line — [open_paren_comment](../tests/fixtures/typescript/types/function_type/open_paren_comment_prettier_divergence/)
- Fn/ctor-type pre-arrow `)` trailing (params) → Onto the last param (`a: T // c`) — [pre_arrow_param_line_comment](../tests/fixtures/typescript/types/function_type/pre_arrow_param_line_comment_prettier_divergence/)
- Fn/ctor-type `=>`→return-type line comment → Continuation kept **flush**; tsv indents it one level per [§Uniform Forced-Continuation Indent](./conformance_prettier.md#uniform-forced-continuation-indent), the layout this gap's own *frozen* arm already took (`member_gap_frozen` routes to the shared `append_keyword_value_line_comments`) and every sibling keyword→value gap takes. The comment does not move in either formatter — only the continuation's indent differs. A **union** return is unaffected: it breaks after the `=>` and hangs the comment with its members, its own layout. Both authorings of the comment reach one fixed point, the bare `=> // c⏎\tT` and the redundant-paren `=> (// c⏎T)` alike (`leading_paren_unwrapped` hands the gap its comments, so the shell's own hang can't answer differently) — [type_position_parens_leading_line_comment](../tests/fixtures/typescript/types/type_position_parens_leading_line_comment_prettier_divergence/), control case in [function_type_prettier_ignore_return](../tests/fixtures/typescript/types/function_type_prettier_ignore_return_prettier_divergence/)
- Fn/method/signature `)`→return-type-`:` line comment → Onto the last param (`a: T // c`), params broken; tsv keeps it trailing `)` with `:` forced onto the next line (the `)`→`=>` counterpart above — a `//` there would otherwise swallow the return type). The signature builders hug the single param (it stays flat); arrows, excluded from that hug, break it — [return_type_close_paren_line_comment](../tests/fixtures/typescript/declarations/function/return_type_close_paren_line_comment_prettier_divergence/)
- Fn/method/signature `)`→return-type-`:` **own-line block** comment → tsv collapses it inline (`f(a) /* c */ : T`), the same collapse every keyword→value gap and the `)`→`=>` gap apply; prettier relocates it into the parameter list over two passes (`f(a /* c */): T`, a dual-stable `variant_*`). The collapsed inline form is dual-stable too — [return_type_close_paren_own_line_block_comment](../tests/fixtures/typescript/declarations/function/return_type_close_paren_own_line_block_comment_prettier_divergence/)
- Call/construct signature `(` trailing → Onto its own line (method keeps) — [signature_params_leading_line_comment](../tests/fixtures/typescript/types/comments/signature_params_leading_line_comment_prettier_divergence/)
- Object destructuring `{` trailing → Onto its own line — [object_open_brace_comment](../tests/fixtures/typescript/expressions/destructuring/object_open_brace_comment_prettier_divergence/)
- Array destructuring `[` trailing → Onto its own line — [array_open_bracket_comment](../tests/fixtures/typescript/expressions/destructuring/array_open_bracket_comment_prettier_divergence/)
- Namespace/module body `{` trailing → Onto its own line — [open_brace_comment](../tests/fixtures/typescript/declarations/namespace/open_brace_comment_prettier_divergence/)
- Class/interface/enum body `{` trailing → Onto its own line — [class](../tests/fixtures/typescript/statements/class/open_brace_comment_prettier_divergence/), [interface](../tests/fixtures/typescript/statements/interface/open_brace_comment_prettier_divergence/), [enum](../tests/fixtures/typescript/declarations/enum/open_brace_comment_prettier_divergence/)
- Type literal body `{` trailing → Onto its own line — [type_literal_open_brace_comment](../tests/fixtures/typescript/types/type_literal_open_brace_comment_prettier_divergence/)
- Import/export specifier `{` trailing → Onto its own line — [imports](../tests/fixtures/typescript/modules/imports/open_brace_comment_prettier_divergence/), [exports](../tests/fixtures/typescript/modules/exports/open_brace_comment_prettier_divergence/)
- Tuple type `[` trailing → Onto its own line — [open_bracket_comment](../tests/fixtures/typescript/types/tuple/open_bracket_comment_prettier_divergence/)
- Index signature `[` trailing (line) → Onto its own line (key's leading comment) — [index_signature_open_bracket_line_comment](../tests/fixtures/typescript/types/type_members/index_signature_open_bracket_line_comment_prettier_divergence/)
- Index signature before `]` (own-line line) → After `]` (value `:` to next line) — [index_signature_close_bracket_line_comment](../tests/fixtures/typescript/types/type_members/index_signature_close_bracket_line_comment_prettier_divergence/)
- Index signature `]`→value-`:` (line) → Into brackets, trailing key type — [index_signature_bracket_colon_line_comment](../tests/fixtures/typescript/types/type_members/index_signature_bracket_colon_line_comment_prettier_divergence/). An own-line `//` authoring pulls up to the same form under tsv, while prettier reaches the in-bracket relocation via an unstable first pass (pinned `prettier_intermediate_to_variant_own_line` → `variant_own_line`)
- Index signature `]`→value-`:` **own-line block** comment → Into the brackets, trailing the key type (`[k: string /* c */]: number` — the same relocation as the line entry above), reached non-idempotently: pass 1 pulls the comment up to trail `]` keeping the break, pass 2 crosses the bracket, one comment per pass (3 passes for a run — two intermediates, past what a `prettier_intermediate_*` pin carries, so the chain is README-recorded). tsv collapses in one pass to the spaced inline form (`[k: string] /* c */ : number` — a plain match when authored inline, `index_signature_bracket_colon_comment`); the relocated landing is dual-stable, so the two authorings keep two fixed points — [index_signature_bracket_colon_own_line_block_comment](../tests/fixtures/typescript/types/type_members/index_signature_bracket_colon_own_line_block_comment_prettier_divergence/)
- Index signature `]`→value-`:` (≥2 line comments) → No fixed point (prettier oscillates); tsv keeps each on its own line — [index_signature_bracket_colon_multi_comment](../tests/fixtures/typescript/types/type_members/index_signature_bracket_colon_multi_comment_prettier_divergence/)
- Index signature value-`:`→type under a `]`→`:` comment (line) → Type kept flush (tsv indents continuation) — [index_signature_bracket_colon_value_line_comment](../tests/fixtures/typescript/types/type_members/index_signature_bracket_colon_value_line_comment_prettier_divergence/)
- Index signature in-bracket line comments, **class** context (four positions) → `[`/`:`→type/own-line-before-`]` relocate or stay flush, key→`:` matches — [index_signature_bracket_line_comment_positions](../tests/fixtures/typescript/declarations/class/index_signature_bracket_line_comment_positions_prettier_divergence/)
- Type-argument `<` trailing (multi) → Onto its own line — [type_argument_open_angle_comment](../tests/fixtures/typescript/types/type_argument_open_angle_comment_prettier_divergence/)
- Call/`new`-expr type-arg `<` trailing (multi) → Onto its own line — [type_args_open_angle_comment](../tests/fixtures/typescript/expressions/calls/type_args_open_angle_comment_prettier_divergence/)
- Computed-key `[` trailing (line) → Out of the brackets to the member's leading line — [computed_key_open_bracket_line_comment](../tests/fixtures/typescript/expressions/objects/computed_key_open_bracket_line_comment_prettier_divergence/)
- Computed-key `[` trailing (line, class) → Kept on `[` line, key glued flush — [computed_key_open_bracket_line_comment](../tests/fixtures/typescript/statements/class/computed_key_open_bracket_line_comment_prettier_divergence/)
- Computed-key key→`]` (line) → Out of the brackets (same-line → member's leading line, own-line → past `]:` onto the value) — [computed_key_close_bracket_line_comment](../tests/fixtures/typescript/expressions/objects/computed_key_close_bracket_line_comment_prettier_divergence/)

**Prefix type-operator operand hang** (`type A = keyof // c\n\t\tB`). A line comment in a prefix operator's (`keyof`/`typeof`) keyword→operand gap, the operand on a later line, has two authorings with two divergence characters. **Trailing** the operator (layout, *not* a relocation): both formatters keep the comment after the operator _and_ the operator on the `=` line, differing only on the operand's indent — tsv hangs it one level under the operator (the uniform keyword→value layout `append_keyword_value_line_comments`, shared with the type keyword→type sites in the table above), while Prettier leaves it flush. On its **own line** (relocation + indent): tsv keeps the comment where the author wrote it and hangs the operand below; Prettier pulls the comment *up* onto the operator line and leaves the operand flush — the same up-pull it applies to an own-line type-parameter keyword comment and to `infer`. A long _comment-free_ `keyof`/`typeof` still breaks after `=` in both formatters; the comment is what keeps the operator on the `=` line. Content-preserved and idempotent ([type_operator_keyword_line_comment](../tests/fixtures/typescript/types/type_operator_keyword_line_comment_prettier_divergence/)).

**How tsv treats keyword→value block comments** (the gap between a keyword/operator and its **single operand** — `as`/`satisfies`, heritage/conditional `extends`, `keyof`/`typeof`/`readonly`, `infer`, a type parameter's `extends`/`=`, predicate `is`, and indexed access `[`→index). These are **single-slot gaps**: one keyword/bracket, one operand, no delimited list. The governing principle across the whole comment story is *per construct, emit the least-divergent output that is lossless, idempotent, **and** position-preserving*; at a single-slot gap that lands on **collapse** — a block comment collapses to the inline form (`kw /* c */ operand`) whenever it *can* inline losslessly: a **single-line** block in any position (glued, trailing the keyword, or own-line), and a **glued** multiline block, one whose operand already shares the comment's closing line (`kw /* …⏎… */ operand`, the way Prettier keeps it — [keyword_value_glued_multiline_block_comment](../tests/fixtures/typescript/types/keyword_value_glued_multiline_block_comment/)). The own-line placement of such a block carries no signal the inline form drops, so tsv keeps the comment after the keyword and collapses. Only a **line** comment (a `//` would swallow the operand) or an **own-line** multiline block (`kw⏎/* …⏎… */⏎operand` — inlining it would reflow the author's break) still hangs the operand on its own line, and there tsv keeps the comment where the author wrote it rather than pulling it onto the keyword line ([type_operator_keyword_own_line_multiline_block_comment](../tests/fixtures/typescript/types/type_operator_keyword_own_line_multiline_block_comment_prettier_divergence/) for the operator family, [indexed_access_own_line_multiline_block_comment](../tests/fixtures/typescript/types/indexed_access_own_line_multiline_block_comment_prettier_divergence/) for the index slot; a template-literal interpolation hangs its type too, but keys the comment’s own placement on the authoring — see the template-literal note below). The gate is `comments_force_own_line_between` (hang iff a line comment, or a multiline block with a newline after it), shared with the type-alias `=` layout — whose `=` stays on its line for a glued multiline value too, rather than breaking on the comment's interior newline.

**One question, one predicate.** The hang rule is stated once, as `Printer::comment_hangs_next` (a line comment, or a multiline block with a newline after it), and *both* the gate above and the emitter that gate selects (`build_trailing_comments_hang_next`) ask it — so a gate and its emitter cannot answer differently. This matters more than it looks: an emitter keyed on `is_block` alone reads as plausible code, yet it collapses the own-line multiline case its own gate had just flagged as hanging. The layout then rests on an authored newline that the collapse itself destroys, so the format stops being idempotent on its own output — the `=` breaks on pass 1 and hugs on pass 2. The rule's two flush-form gaps (indexed access, template-literal interpolation) sat on exactly that split.

Collapse is not a universal law over "single-line block in a bracket" — it's the outcome of that principle *at a single-slot gap*. A delimited `<…>` list is a different construct and takes a different outcome (see the list contrast below). Within the gaps, the divergence lives entirely in the **own-line authoring** (`kw⏎/* c */⏎operand`), which the two formatters normalize to different fixed points — each fixture's `unformatted_ours_own_line` variant pins tsv's collapse. The two flavors of Prettier fixed point:

- **In-place-collapse gaps.** Prefix type operators `keyof`/`typeof`/`readonly` ([type_operator_keyword_own_line_block_comment](../tests/fixtures/typescript/types/type_operator_keyword_own_line_block_comment_prettier_divergence/)) and `infer` ([keyword_own_line_block_comment](../tests/fixtures/typescript/types/infer/keyword_own_line_block_comment_prettier_divergence/), line form [keyword_line_comment](../tests/fixtures/typescript/types/infer/keyword_line_comment_prettier_divergence/)): Prettier reaches the *same* inline form, but is **non-idempotent** — its first pass pulls the comment onto the keyword line yet leaves the operand on the next line, collapsing fully only on a second pass.
- **Relocation gaps.** `as`/`satisfies` ([as_satisfies_value_own_line_block_comment](../tests/fixtures/typescript/expressions/as_satisfies_value_own_line_block_comment_prettier_divergence/)), type predicate `is` ([predicate_is_own_line_block_comment](../tests/fixtures/typescript/types/predicate_is_own_line_block_comment_prettier_divergence/)), a type parameter's `extends`/`=` ([constraint_default_own_line_block_comment](../tests/fixtures/typescript/types/type_params/constraint_default_own_line_block_comment_prettier_divergence/)), a conditional type's `extends` ([extends_own_line_block_comment](../tests/fixtures/typescript/types/conditional/extends_own_line_block_comment_prettier_divergence/)), and indexed access `[`→index ([indexed_access_own_line_block_comment](../tests/fixtures/typescript/types/indexed_access_own_line_block_comment_prettier_divergence/)): Prettier collapses *and relocates* the comment across the keyword/bracket (`x /* c */ as A`, `A /* c */[K]`), and reaches that relocation **non-idempotently** in every case — its first pass leaves the comment after the keyword and hangs the operand (`x as /* c */⏎A`, `A[/* c */⏎K]`), only its second pass moves the comment across (`x /* c */ as A`, `A /* c */[K]`). tsv keeps it after the keyword and collapses in one pass — the pure [comment-position](./conformance_prettier.md#comment-position-philosophy) divergence. Each fixture pins Prettier's two-pass chain with a `prettier_intermediate_to_variant_own_line` (its unstable first pass) plus a `variant_own_line` (its relocated fixed point, dual-stable in both formatters).
- **Pass-count gaps (same fixed point).** `await`→operand and `new`→callee ([await_new_operand_own_line_block_comment](../tests/fixtures/typescript/expressions/await_new_operand_own_line_block_comment_prettier_divergence/)): unlike the relocation gaps above, prettier does **not** move the comment — it reaches the *same* fixed point tsv does (`await /* a */ foo`, `new /* b */ Foo()`), just **non-idempotently**. Its first pass pulls the comment up to the keyword while keeping the author's break (`await /* a */⏎foo;`), and only its second pass collapses that. tsv collapses in one pass, the same keyword→value rule (`comment_hangs_next`) its `as`/`satisfies`, `export =`, and module-header siblings follow. The intermediate form is the bug the fixture guards — it *is* the glued authoring with the break kept, so emitting it is not idempotent on its own output. Pinned by a `prettier_intermediate_own_line` (converging to input, not to a `variant_*`, since the fixed points agree).
- **Heritage `extends`/`implements`** ([heritage_keyword_own_line_block_comment](../tests/fixtures/typescript/class/heritage_keyword_own_line_block_comment_prettier_divergence/)): also a relocation gap, but Prettier does **not** collapse it. From the own-line authoring it settles — in one pass, stably — on the comment on its **own line before** the keyword (`class P⏎/* c */⏎extends Q`), never inlining it. tsv collapses to `class P extends /* c */ Q` (after the keyword, inline). So both move the comment relative to the author's break, but Prettier keeps the break while tsv removes it. Prettier's stable form is *not* dual-stable — tsv re-collapses it to a *third* form (`class P /* c */ extends Q`) — so it is pinned as a `divergent_variant_*` form rather than a `variant_*` (see the note below).

The own-line authoring of a *single-line* block is the only place these gaps diverge; the inline canonical (`kw /* c */ operand`) is dual-stable in both formatters. Only a **line** comment (a `//` can't be inlined — it would swallow the operand) or an **own-line multiline** block comment still hangs the operand on its own line; the collapse applies to single-line blocks (any authoring) and glued multiline blocks. The companion **line**-comment divergences (where the hang is forced) are cataloged alongside each gap above.

**List contrast — why `<…>` does not collapse.** A delimited type-argument list `<…>` is *not* a single-slot gap, so it takes the other outcome of the same principle: Prettier has a clean, idempotent list-break rule (an own-line comment on an argument breaks the whole `<…>`), and matching it is the least-divergent lossless choice — so tsv matches instead of collapsing. A single-argument own-line *block* comment therefore **expands** (`Foo<⏎/* c */⏎T⏎>`, dual-stable in both formatters — [single_arg_own_line_block_comment](../tests/fixtures/typescript/types/type_args/single_arg_own_line_block_comment/)) rather than collapsing to `Foo</* c */ T>`; the single-argument case is just the N=1 form of the list and follows the list, not the gap. (A single-argument leading *line* comment hugs `<`/`>` — a separate divergence, cataloged under the [open-delimiter trailing](#comment-relocation) entry.) The surface split — index `[K]` collapses, `<T>` expands — is not an inconsistency: it is the same "diverge only when Prettier is worse" rule landing on *collapse* where Prettier relocates non-idempotently (the index slot) and on *match* where Prettier is already fine (the list).

Two carve-outs keep the operand break instead of collapsing: **`export default`** still hangs (it's the one construct where Prettier *keeps* the operand break, so hanging — both broken, tsv indented — is the closer indent-only divergence) and **binary/logical operands** (the expression side, where tsv's own-line layout already matches Prettier). The **conditional `?`/`:` branches** (expression ternary and conditional type alike) collapse the single-line own-line form and keep the glued forms glued — including a **glued** multiline block (`? /* …⏎… */ A`) — and match Prettier there (no divergence fixture). A branch-gap comment the author broke *after* (`: /* c */⏎B`) keeps that break as a collapsible line: it holds while the conditional is broken and yields when the whole conditional fits, Prettier's own `printLeadingComment` separator and its fixed point ([ternary/operator_block_comment_break](../tests/fixtures/typescript/expressions/ternary/operator_block_comment_break/), [conditional/branch_block_comment_break](../tests/fixtures/typescript/types/conditional/branch_block_comment_break/)). One glued-multiline layout gap remains, unrelated to the block-comment gate: a class/interface **heritage** clause keeps a glued multiline inline (`class P extends /* …⏎… */ Q`) where Prettier breaks before the keyword. It lives in its own layout code (the heritage clause), not in `comments_force_own_line_between`.

**Pinning Prettier's fixed point (fixture mechanics).** Because each inline `input` is dual-stable, these fixtures have no `output_prettier` — the divergence lives only in the `unformatted_ours_own_line` variant (tsv normalizes it to `input`; Prettier does not). To assert Prettier's actual output on that variant, the in-place-collapse gaps carry a `prettier_intermediate_own_line` (Prettier's unstable first pass, which reconverges to `input`), and the relocation gaps carry a `prettier_intermediate_to_variant_own_line` + a `variant_own_line` (its relocated fixed point, genuinely dual-stable). **Heritage is the exception:** Prettier's fixed point (`class P⏎/* c */⏎extends Q`) is Prettier-stable but tsv re-collapses it to a *third* form (`class P /* c */ extends Q`) — neither `input` nor Prettier's form. A `variant_*` is meant to be dual-stable (both formatters keep it verbatim), and a `prettier_variant_*` is a Prettier-stable form tsv normalizes to `input`; heritage is neither, so it is pinned by a `divergent_variant_own_line` — a `divergent_variant_*` form.

**The `divergent_variant_*` type.** "Prettier keeps `V`, tsv rewrites `V` to a third stable form" is a small recurring category (three distinct stable forms: `input`, Prettier's `V`, and tsv's `ours(V)`). It is pinned by a `divergent_variant_*` fixture file, which asserts `prettier(V) == V`, `ours(V) != input`, `ours(V) != V`, and `ours(ours(V)) == ours(V)` (the third form is itself a fixed point). Besides heritage the category includes the non-block `else` body ([else_line_comment_nonblock](../tests/fixtures/typescript/statements/if/else_line_comment_nonblock_prettier_divergence/), `divergent_variant_nonblock`) and the blank-inside-relocated-condition form ([do_while/line_before_while_comment](../tests/fixtures/typescript/statements/do_while/line_before_while_comment_prettier_divergence/), `divergent_variant_spaces` — Prettier relocates a `}`→`while` comment into the condition parens and keeps a blank there; tsv drops it). This is distinct from `variant_*`, whose check requires true dual-stability (`ours(V) == V`), not merely that tsv reaches *a* fixed point from the form.

**No blank above a body block's `{`**: tsv drops an authored blank line before a body-leading own-line comment in the **header `)`→body** gap — `while (a)⏎⏎// c⏎{` → `while (a)⏎// c⏎{`, across `if`/`while`/`for` — so a body block's `{` never sits below a blank. This is consistent with tsv's own handling when `{` is on the header line (`while (a) {⏎⏎// c` also collapses), and Prettier drops it here too, so tsv matches. The residual divergence in this gap is a blank *after* the comment, before `{` (`while (a)⏎// c⏎⏎{`), which Prettier keeps and tsv normalizes, pinned as `prettier_variant_spaces` in [while/line_before_body_comment](../tests/fixtures/typescript/statements/while/line_before_body_comment_prettier_divergence/).

**The `}`→continuation-keyword gap keeps the blank**, and is a different question that merely shares an emitter. There is no body `{` to sit below the blank: it separates two branches of a chain (`}⏎⏎// c⏎else`, `}⏎⏎// c⏎catch (e) {`, `}⏎⏎// c⏎while (a);`), which is real authoring intent, so tsv preserves it. The gap covers `else`, `catch`, `finally`, and a do-while's `while` — every keyword that continues a construct across a `}`. Prettier agrees at `else` ([else_blank_before_comment](../tests/fixtures/typescript/statements/if/else_blank_before_comment/)). At `catch`/`finally` and at a do-while's `while` Prettier is no oracle — it relocates the comment away (into the following block's body, or into the condition parens; see the `divergent_variant_*` note above), discarding the blank with it — so tsv's own preservation stance governs there ([try/catch_between_comment](../tests/fixtures/typescript/statements/try/catch_between_comment_prettier_divergence/), [do_while/line_before_while_comment](../tests/fixtures/typescript/statements/do_while/line_before_while_comment_prettier_divergence/)). The comment's *position* is unchanged in every case — only the blank differs.

**A blank line BETWEEN two own-line comments is always preserved**, in every gap — this is a separate question from the blank *above* the first comment, which the two entries above split by gap. An authored blank between two comments separates two distinct remarks, exactly as a blank between two statements does, so tsv keeps it in statement lists, class bodies ([syntax/comments/consecutive_blank_lines](../tests/fixtures/typescript/syntax/comments/consecutive_blank_lines/)), object literals, array elements, parameter lists, the header→body gap for **both** block and non-block bodies across `if`/`while`/`for`/for-in/for-of ([syntax/comments/between_head_and_body](../tests/fixtures/typescript/syntax/comments/between_head_and_body/)), the `for` header's own clause gaps ([for/clause_leading_comment_run](../tests/fixtures/typescript/statements/for/clause_leading_comment_run/)) and `try`/`catch`/`finally` ([try/keyword_body_blank_comment](../tests/fixtures/typescript/statements/try/keyword_body_blank_comment_prettier_divergence/)), and the `}`→continuation-keyword gaps. Prettier agrees in all of them but **one**: it collapses the blank in the `}`→`else` / `}`→`else if` gap — for line comments, block comments, and every blank in a longer run alike — [if/else_blank_between_comments](../tests/fixtures/typescript/statements/if/else_blank_between_comments_prettier_divergence/). The divergence is Prettier's own inconsistency rather than a tsv stance: where it *relocates* these comments (into the `catch` body, into a do-while's condition parens) it carries the blank along with them, so it treats the blank as meaningful everywhere except the one gap it prints through a different path. Prettier's collapsed form is dual-stable under tsv (that fixture's `variant_collapsed`), so an already-prettier-formatted file does not churn.

**Type-construct gap line comments** (swallow prevention): a line comment in a type construct's delimiter/keyword/operator gap is kept where the author wrote it, the following token dropped to the next line so the `//` cannot swallow it (the shared `build_trailing_comments_hang_next`). Prettier relocates each differently — out to a statement-trailing position, into/after the enclosing brackets, or trailing the source. tsv preserves the authored position uniformly. Emitting the comment inline (the previous behavior at these sites) let the `//` run to end-of-line and absorb the following token — non-idempotent **content loss**. Covered: indexed access [indexed_access_line_comment](../tests/fixtures/typescript/types/indexed_access_line_comment_prettier_divergence/), mapped `in`/`as` [mapped/keyword_line_comment](../tests/fixtures/typescript/types/mapped/keyword_line_comment_prettier_divergence/), import-type qualifier [import_type_qualifier_line_comment](../tests/fixtures/typescript/types/import_type_qualifier_line_comment_prettier_divergence/), and tuple rest / named / optional members [tuple/member_line_comment](../tests/fixtures/typescript/types/tuple/member_line_comment_prettier_divergence/). Every one of those is an **after**-keyword / after-`:` gap — the comment leads the value. Their **before**-gap counterparts (a mapped type's key→`in` / constraint→`as`, a named tuple member's label→`:`) are the opposite position, the comment trailing the head, and take the indented continuation instead of this flush form — see the Mapped before-keyword and Named-tuple label→`:` entries above. (`infer` lands a break too but takes the indented keyword→value hang rather than this flush form, so it diverges — see the Infer keyword entry above.)

**A template-literal interpolation is authoring-dependent — the one gap that preserves *both* authorings.** The `${`→type gap takes neither of the forms above wholesale; it keys on where the author put the comment, and each form is a stable fixed point:

- authored **trailing** `${` → stays trailing, type below it (`` `a${// c⏎⇥B⏎}` ``). Prettier expands instead, moving the comment down — the divergence ([template_literal_interp_trailing_comment](../tests/fixtures/typescript/types/template_literal_interp_trailing_comment_prettier_divergence/), width case [template_literal_interp_trailing_comment_long](../tests/fixtures/typescript/types/template_literal_interp_trailing_comment_long_prettier_divergence/), where Prettier additionally emits a blank line after `${`). Prettier's expanded output is itself a tsv fixed point (pinned as that fixture's `variant_expanded`), so tsv never pulls the comment back up — the two authorings hold two stable forms rather than one collapsing into the other.
- authored **own-line** → the interpolation **expands**, the comment keeps its line (`` `a${⏎// c⏎B⏎}` ``). This is prettier's layout, so tsv matches — no divergence ([template_literal_interp_own_line_comment](../tests/fixtures/typescript/types/template_literal_interp_own_line_comment/)).

The two questions are independent and keyed on opposite sides of the comment: what *follows* it decides whether the type hangs at all (`comment_hangs_next`, the newline after), what *precedes* it decides whether the comment keeps its own line (the newline before). A glued single-line block still collapses inline (`` `a${/* c */ B}` ``). Both broken forms put `}` on its own line, as a width-driven break does — a hanging comment is the *only* break mechanism in play, so a too-wide type simply breaks *inside* the already-broken interpolation instead of a second break firing on top of it.

Why authoring-dependent here, when most gaps pick one canonical form: a single form **cannot** preserve both authorings — forcing the expansion moves a trailing-authored comment down (Prettier's choice), forcing the flush form pulls an own-line-authored one up. Both authorings render identically and each form is stable, so there is no lossless-or-idempotent reason to collapse them; the authored break is simply signal, and tsv keeps it. This is the same distinction the type-alias `=` gap already draws (`type A = /* c */ B` hugs, `type A =⏎/* c */⏎B` keeps its own line), and it makes the divergence *minimal* — only the trailing authoring differs from Prettier.

The `=` keeps the backtick on its line for both (`value_owns_its_comment_break`), since either layout already breaks the template itself.

**Else line comment with non-block body**: `} else // c\nexpr;` → Prettier keeps the comment after `else` but drops it onto its own line, indented with the body (`} else\n\t// c\n\texpr;`); tsv keeps it trailing `else` on the same line (`} else // c\n\texpr;`). Affects block consequent, comments-path, and non-block consequent cases. The comment-before-`else` form `} // c\nelse expr;` is also prettier-stable: dual-stable (`variant_comment_before_else`) for the block/comments-path cases, but a `divergent_variant_*` form (`divergent_variant_nonblock`) for the non-block consequent, where tsv re-collapses `if (a)⏎expr;` to `if (a) expr;`.

**Default / namespace import + export-all header comments**: the same preserve-in-place rule covers the remaining module-header shapes. In a **default** (`import /* c */ Foo from 'a'`) or **namespace** (`import /* c */ * as ns from 'a'`) import, tsv keeps each header comment where the user wrote it; Prettier keeps a comment already adjacent to the binding but relocates a comment between `import` and `type` to the binding side of `type` (`import type /* c */ Foo`). In an **export-all** (`export /* c */ * from 'a'`, including `export /* c */ type * from 'a'`) Prettier relocates _every_ header comment — around `export`, `type`, and `*` — to after `from`, before the source; tsv preserves them in place. A comment between `*` and `as` in a namespace binding (`import * /* c */ as ns`, `export * /* c */ as ns from 'a'`) is likewise preserved in place — Prettier relocates it to after `as`, before the binding. Both positions are dual-stable in our formatter. Per the uniform module-header rule below, a line comment in **every** one of these gaps indents the continuation one level — the keyword→default/namespace binding, `type`→default/namespace binding, `type`→namespace-`*`, export-all `export`/`type`→`*`, `*`→`from`, `*`→`as`, and `as`→binding gaps alike. Where Prettier relocates (export-all, `*`→`as`) tsv is free; where Prettier keeps the comment in place and flat (keyword/`type`→binding, `type`→namespace-`*`, `as`→binding) tsv's indent is a deliberate indent-only divergence.

**Import/export binding-or-specifiers to `from`**: A comment in the gap between an import's binding/specifiers (or a re-export's specifiers) and the `from` keyword is preserved where the user placed it; Prettier's relocation depends on the binding shape. For a **default** or **namespace** binding (no braces) a same-line block comment stays in place in both formatters (`import Foo /* c */ from 'a'` — dual-stable), but Prettier floats a line comment past the `;` to a statement-trailing position (`import Foo from 'a'; // c`, Prettier's statement-trailing float) while tsv keeps it before `from`, indenting the `from …` continuation one level onto its own line. For **named specifiers** (`import { a } /* c */ from 'a'`, `export { a } /* c */ from 'a'`) Prettier relocates the comment _into_ the braces as the last specifier's trailing comment — a block comment inline (`{ a /* c */ }`), a line comment expanding the braces multiline (`{\n\ta // c\n}`) — while tsv keeps it after `}` with the braces inline (indenting `from …` when a line comment forces the break). Both positions are dual-stable in our formatter. (Otherwise the comments are dropped entirely — content loss.)

**Import attributes header comments**: A comment in an import's attributes header — between the source and the `with` keyword (`import x from 'a' /* c */ with {…}`), or between `with` and the attributes `{` (`import x from 'a' with /* c */ {…}`) — is preserved where the user placed it. Prettier keeps a source→`with` block comment in place (dual-stable), relocates a `with`→`{` block comment to before `with` (`import x from 'a' /* c */ with {…}`), and floats a `with`→`{` line comment past the `;` (`import x from 'a' with {…}; // c`, Prettier's statement-trailing float); for a source→`with` _line_ comment, Prettier collapses `with {…}` back onto the source line and floats the line comment past the `;` (`import b from './b' with {…}; // c`), while tsv keeps it before `with` — see [with_keyword_comment_line](../tests/fixtures/typescript/modules/imports/with_keyword_comment_line_prettier_divergence/). tsv keeps each comment where the user wrote it; when a line comment forces the `with`/`{…}` onto a new line, tsv indents that continuation one level. The attributes `}`→`;` block comment trails after the `;` in both formatters (the **Import source to `;`** entry below). (Otherwise the header comments are dropped entirely — content loss.)

**Keyword → specifier brace comments**: A comment between the `import`/`export` keyword and the named-specifier `{` (`import /* c */ { a }`, `export /* c */ { a }`) is preserved before the brace; Prettier relocates it _into_ the braces as the first specifier's leading comment — a block comment inline (`import { /* c */ a }`), a line comment expanding the braces multiline. tsv keeps it where the user wrote it; a line comment forces `{` onto the next line, indenting the `{…}` continuation one level (the uniform module-header rule below). (The `import type … { a }` type→`{` gap and the empty-braces `import /* c */ {}` gap are preserved the same way; this is the non-type named-specifier case.) See [imports/keyword_brace_comment](../tests/fixtures/typescript/modules/imports/keyword_brace_comment_prettier_divergence/) and [exports/keyword_brace_comment](../tests/fixtures/typescript/modules/exports/keyword_brace_comment_prettier_divergence/).

**Declaration- and module-header line-comment continuation indent**: A _line_ comment in a declaration- or module-header gap forces the following token onto a new line, and tsv indents that continuation one level — a statement spanning lines reads as a continuation, not a second statement.

For **module headers** tsv applies this **uniformly to every gap, with no exceptions**: keyword→`type`, keyword/`type`→default-binding, keyword/`type`→namespace-`*`, keyword/`type`→`{`, keyword/`type`→empty-`{}` (re-export _and_ no-`from`), bare keyword→source, export-all `export`/`type`→`*`, `*`→`from`, `*`→`as`, `as`→binding, binding/specifiers→`from`, `from`→source, the `with`/attributes-`{` gaps, and `export`/`export default`→declaration. The two gaps a named specifier opens *inside* the braces are on the same rule through the same helper — its rename `as` gaps, and its per-specifier `type` modifier→name gap (the header `type`→`{` gap's shape one level in). Prettier's own handling varies per gap — it relocates the comment (into the braces, after `from`/`as`/`;`), floats it past `;`, or keeps it in place and flat — but tsv **always** indents. So where Prettier relocates, tsv's in-place indent is just one of two unrelated layouts; and where Prettier keeps the comment in place and flat (`as`→binding, keyword/`type`→default/namespace binding, `type`→namespace-`*`, bare/empty/export-all `from`→source, no-`from` empty `{}`, `export`→declaration) tsv's indent is a deliberate indent-only divergence, chosen so every continuation reads alike.

For **declaration headers** the same rule covers the keyword→name gap of `function` (incl. `async function`, `function*`, and `declare function`), `class` (incl. `abstract class`), and `enum` (incl. `const enum`), plus the keyword→declarator gap of `const`/`let`/`var`. Prettier keeps the comment in place and flat for the `function`/`class`/`enum` headers, so tsv's indent is an indent-only divergence there; for `const`/`let`/`var` Prettier **agrees** (it also indents the declarator), so there is no divergence — a regular fixture.

The deliberate exclusions are constructs where the following token isn't part of the same declaration: **ASI statement keywords** (`return`, `throw`, `break`, `continue`, `yield`) — a newline after them triggers automatic semicolon insertion, so `return // c⏎expr` is two statements, not a continuation; the **contextual declaration keywords** (`type`/`interface`/`namespace`/`module`/`declare`), whose line forms never form a single declaration — the keyword requires its following token on the **same line** (tsc's `isDeclaration`: `nextTokenIsIdentifierOnSameLine`, and the ASI modifier rule for `declare`), so a line break demotes the keyword to a plain identifier. `interface`/`namespace`/`module`→name then become unparseable (`interface // c⏎I {}` is rejected); `type`→name and `declare`→keyword **ASI-split** into two statements (`type;` then `T = …`; `declare;` then `const x = …`). tsv's parser conforms here — see [contextual_keywords/declaration_keyword_own_line](../tests/fixtures/typescript/syntax/contextual_keywords/declaration_keyword_own_line/); and **function/class expressions** (`const a = function // c⏎f() {}`), which are not declaration headers and stay flat in both formatters. Block comments and the no-comment case are byte-identical in every in-scope gap. (The `export`-prefixed forms — `export interface // c⏎I` etc. — are out of scope: after `export` the parser is committed to a declaration, where tsc does not apply the same-line gate, so tsv's acceptance there may be spec-correct; acorn-typescript rejects them, but that is a separate acorn-vs-tsc question.)

**Keyword-paren comments**: `if/* c */(a)` → Prettier absorbs comments between keywords and `(` into the condition parens. Applies to `if`, `while`, `for`, `switch`, `catch`, `do...while`. tsv preserves the comment between keyword and paren: `if /* c */ (a)`. For `for await`, comments in either gap are preserved: `for /* a */ await /* b */ (x of y)` → prettier absorbs both into parens. The only divergence is the comment position — the loop body layout still matches Prettier regardless: an empty body attaches as `for /* k */ (a; b; c);` (no space before `;`), a block body hugs `) {`, and a non-block body stays inline when the header fits ([keyword_comment](../tests/fixtures/typescript/statements/for/keyword_comment_prettier_divergence/)). Both positions are dual-stable in our formatter.

**Condition-absorbed comments**: `switch(x)/* c */{}` → Prettier absorbs comments between `)` and `{` into the condition parens: `switch (x /* c */) {}`. Similarly, `catch/* c */(e)` → `catch (/* c */ e)`. tsv preserves position: `switch (x) /* c */ {}`, `catch /* c */ (e)`. Both positions are dual-stable in our formatter.

**Type alias head to `=`**: `type A<X> // c\n= B | C` → tsv keeps a line comment between a type alias head (name + optional type parameters) and `=` trailing the head, and drops `= value` to a continuation line indented one level — the uniform forced-continuation indent, the same shape as every other before-`=` initializer site (declarators, enum members, class properties; the comment's association stays with the declaration head). Prettier relocates it across the `=` — to end-of-statement for an inline union value, to a hang leading the value otherwise. (With type parameters present, the comment was once dropped entirely — content loss.) A single-line block comment before `=` stays inline in both formatters (`type A<X> /* c */ = B | C`), so it is not a divergence — only a line comment forces the value off its line, and the two formatters disagree on which side of `=` the construct's tail lands. An own-line authoring pulls up to trail the head (own-line-ness is authoring signal for a leading position, not a trailing one). When the value is a **break-forced non-hugging union** (broken by print width or by interleaved member comments), tsv drops `=` onto its own line below the trailing comment and hangs the union members at the **`=`'s indent level** — the same level the author wrote and byte-identical to Prettier's union block, just with `=` lifted above it rather than the members one level deeper ([type_alias_line_pre_equals_break](../tests/fixtures/typescript/types/comments/type_alias_line_pre_equals_break_prettier_divergence/)). Conditional / intersection / object / short-union values hug `= ` and are unaffected; only the non-hugging union, whose broken form leads every member with `|` on its own line, drops `=` alone.

**Type parameter keyword to value (own-line)**: an own-line line comment between a type parameter's `extends`/`=` keyword and its constraint/default value (`U =\n// c\nV`, `T extends\n// c\nA`) is kept on its own line in the indented value block; Prettier pulls the first leading comment up onto the keyword line (`U = // c\n\tV`) and is **non-idempotent** doing so (its first pass leaves the value at the param indent, a second pass adds the extra indent). tsv stays idempotent and preserves the author's own-line placement. A comment that is **already** on the keyword line (`R extends A | B | void = // c\n…`): tsv emits it inline via `line_suffix` (zero width), so a long trailing comment never forces a preceding constraint union to break. Prettier instead counts the trailing comment toward the line width and breaks after `extends` (`R extends\n\tA | B | void = // c\n…`), so this is a divergence ([type_param_keyword_line_comment](../tests/fixtures/typescript/types/comments/type_param_keyword_line_comment_prettier_divergence/)); the own-line first-comment case diverges separately (below). (Emitting the same-line comment as plain text would force-break the constraint union by its width — content added — and merge two line comments onto one line — boundary loss; the `line_suffix` rendering avoids both.) See [type_param_keyword_own_line_comment_prettier_divergence](../tests/fixtures/typescript/types/comments/type_param_keyword_own_line_comment_prettier_divergence/).

**Function parameter default to value**: a line comment after a parameter's `=` default, before the value (`function fn(p = // c\n\tv) {}`), is kept after `=` with the value on a continuation line indented one level; Prettier floats it out to trail the whole parameter, after the value (`function fn(\n\tp = v // c\n) {}`). tsv keeps the comment associated with the default rather than floating it past the value. The parameter's type-annotation union stays inline in both (the trailing comment is zero-width). Applies to function, arrow, and method parameters; a same-line block comment (`p = /* c */ v`) stays inline in both formatters and is not a divergence. See [param_default_line_comment_prettier_divergence](../tests/fixtures/typescript/declarations/function/param_default_line_comment_prettier_divergence/).

**Name/key to default `=`** (the *before*-`=` counterpart): a line comment between a binding name/key and its `=` default (`p // c\n= v`), at a parameter default and every destructuring default — object shorthand (`{ a // c\n= 1 }`), array element (`[b // c\n= 1]`), and function-param destructure (`fn({ c // c\n= 1 })`) — is kept trailing the name with `= value` broken to the next line; the pattern expands to accommodate it. Prettier instead relocates the comment to trail the whole binding after the value (`{ a = 1 // c }`, expanded). tsv keeps the comment where the author wrote it (between name and `=`); emitting it inline would let the `//` swallow the `=` and default value (a non-idempotent content loss). A same-line block comment (`{ a /* c */ = 1 }`) stays inline in both formatters and is not a divergence (see the block-comment sibling [default_equals_comment](../tests/fixtures/typescript/expressions/destructuring/default_equals_comment/)). See [default_equals_line_comment_prettier_divergence](../tests/fixtures/typescript/expressions/destructuring/default_equals_line_comment_prettier_divergence/).

**`as`/`satisfies` cast keyword to type**: a line comment after the cast keyword, before the type (`x as // c\n\tA`), is kept after the keyword with the type on the next line; Prettier floats it out past the whole expression to a statement-trailing position (`x as A; // c`). tsv keeps the comment associated with the cast, on the keyword line via `line_suffix` with the type indented; emitting it inline instead would **swallow the cast type** (`x as // c A` — a non-idempotent content loss). A same-line block comment (`x as /* c */ A`) stays inline in both formatters and is not a divergence. See [as_satisfies_value_line_comment_prettier_divergence](../tests/fixtures/typescript/expressions/as_satisfies_value_line_comment_prettier_divergence/). A **block** comment in the same gap collapses to the inline form (`x as /* c */ A`) — whether authored glued, trailing the keyword, or on its own line (`x as⏎/* c */⏎A`). tsv keeps it after the keyword, associated with the cast type; Prettier collapses too but *relocates* the comment _before_ the keyword (`x /* c */ as A`). The block counterpart of the line case above — see the keyword→value block-comment collapse rule below and [as_satisfies_value_own_line_block_comment_prettier_divergence](../tests/fixtures/typescript/expressions/as_satisfies_value_own_line_block_comment_prettier_divergence/).

**Binary operand to operator line comment**: a line comment after a binary chain's left operand, before the operator (`1 // c\n+ 2`), is kept trailing the operand where the author wrote it, and the operator drops to the next line glued to its right operand (`1 // c\n+ 2`); Prettier relocates the comment _past_ the operator, keeping the operator trailing the left operand (`1 + // c\n2`). tsv preserves the comment's association with the left operand ([Comment Position Philosophy](./conformance_prettier.md#comment-position-philosophy) Principles 1 and 2). Emitting it inline instead would run to end-of-line and **swallow the operator and right operand** (`1 // c + 2` — `+ 2` absorbed into the comment, a non-idempotent content loss). The operator hugs its right operand rather than over-breaking onto a line of its own (`1 // c\n+\n2`); in a longer chain each boundary is independent, so an uncommented gap keeps the normal break-after-operator (`aa &&\nbb // c\n&& cc`). Covers arithmetic, logical, and the keyword-binary operators (`in`, `instanceof`) — all `BinaryExpression` through the one binary-chain printer (`build_binary_chain_parts`, `operators.rs`). A same-line block comment (`1 /* c */ + 2`) stays inline in both formatters and is not a divergence. See [operand_operator_line_comment](../tests/fixtures/typescript/expressions/binary/operand_operator_line_comment_prettier_divergence/).

**Call open paren `(` trailing**: `fn( // c` / `fn( /* c */` (a comment on the same line as a call's opening `(`) → Prettier relocates it to its own line as the first argument's leading comment (`fn(\n\t// c\n\t…)`). tsv keeps it trailing the `(` (`fn( // c\n\t…)`), treating the author's placement after `(` as a trailing comment on that line. This applies only when the call expands (a line comment after `(`, or own-line content among the args); a block comment that hugs the arg in a call that stays inline (`fn(/* c */ a)`) is unchanged and matches Prettier. When the author instead writes the comment on its own line, both formatters keep it there — the two positions are dual-stable. Applies to simple-callee calls (`call_formatting.rs`), member-chain calls (`chain_args.rs`), and `new` expressions (`new_expression.rs`). For chains, a block comment trailing `(` plus an own-line leading comment keep source order (the naive handling reverses them). For `new`, a line comment trailing `(` (`new Foo( // c\n\ta)`) is preserved rather than dropped entirely (content loss); prettier relocates it (line or block) to its own line inside the parens as the first argument's leading comment (`new Foo(\n\t// c\n\ta)`), the same as the simple-callee and member-chain forms.

**Object/array/block open-delimiter trailing**: the same position-preservation rule as the call `(` case, generalized to the other opening delimiters. A comment on the same line as an object literal's `{` (`const o = { // c`), an array literal's `[` (`const a = [ // c`), a block body's `{` (`function f() { // c`, plain `{ // c`, arrow `=> { // c`), a type-parameter list's `<` (`function f< // c`, also classes/interfaces/type aliases/arrows), a function/constructor-type parameter list's `(` (`type Fn = ( // c`, `new ( // c`), a **value-level function-definition** parameter list's `(` (`function fn( // c`, `function ( // c`, `( // c ) => …`, and class method / constructor / setter — [open_paren_line_comment](../tests/fixtures/typescript/declarations/function/open_paren_line_comment_prettier_divergence/), [method_open_paren_line_comment](../tests/fixtures/typescript/statements/class/method_open_paren_line_comment_prettier_divergence/)), an object/array destructuring pattern's `{`/`[` (`const { // c } = o`, `const [ // c ] = a`), a `namespace`/`module` body's `{` (`namespace N { // c`, `module M { // c`), a class/interface/enum body's `{` (`class C { // c`, `interface I { // c`, `enum E { // c`), a type literal's `{` (`type T = { // c`), an import/export specifier list's `{` (`import { // c`, `export { // c`), a tuple type's `[` (`type T = [ // c`), an index signature's `[` (`[ // c\n\tk: V]`), a computed property/member key's `[` (`{ [ // c\n\tfoo]: 1 }`, also class members, destructuring, and interface/type-literal members — [computed_key_glued_block_comment](../tests/fixtures/typescript/expressions/objects/computed_key_glued_block_comment_prettier_divergence/), which also pins prettier hoisting an in-bracket block comment out to the member's line), a mapped-type key's `[` (`{ [ // c\n\tK in keyof T]: V }` — prettier drops it to its own line — [mapped_key_glued_block_comment](../tests/fixtures/typescript/types/mapped_key_glued_block_comment_prettier_divergence/)), a computed member-access index's `[` (element access, `arr[ // c\n\ti]`, including optional `?.[` — prettier relocates it to trail the object), an angle-bracket type assertion's `<` (`const x = < // c\n\tT>y` — [angle_glued_block_comment](../tests/fixtures/typescript/expressions/type_assertion/angle_glued_block_comment_prettier_divergence/)), or a multi-argument type-argument list's `<` (`Map< // c`) is kept on the delimiter line; Prettier relocates it to its own line as the leading comment of the first element/property/statement/parameter/member/specifier/argument (for a computed key prettier instead relocates it out to the **member's** own leading line, or — class members — leaves it glued flush to the key). This applies only to a **line** comment (or a block comment that co-occurs with one) when the construct expands; an inline block comment that hugs content (`{ /* c */ a: 1 }`, `[/* c */ x]`, `<T /* c */>`, `(/* c */ p)`) and an own-line block comment (which both formatters keep on its own line) are unchanged and match Prettier. (An empty brace body whose sole content is that inline block comment, `{ /* c */ }`, likewise hugs rather than relocating — but tsv keeps its bracket spacing where Prettier 3.9.5 tightens it to `{/* c */}`, a separate divergence: see the §Empty-object comment bracket spacing entry above.) When the author instead writes the comment on its own line, both formatters keep it there — dual-stable. **An author blank line between the pulled comment and the first item does not survive** (`{ // c`, blank, `\ta`). That is a *consequence* of the position above rather than a second choice: both formatters discard a blank in the **leading gap** — an opening delimiter's line → the first item, verified byte-identical across eleven container kinds — and both preserve one *between* items. Keeping the comment on the delimiter line leaves the blank in the leading gap, so tsv's own leading-gap rule drops it; Prettier's relocation makes the comment the first body item, which moves the blank into an inter-item gap its own rule then keeps. The derivation runs both ways — hand tsv Prettier's relocated form and tsv preserves the blank itself — which is what the `unformatted_ours_blank_after_comment` (the authored shape, tsv normalizes it to `input`) and `variant_blank_after_comment` (dual-stable) siblings pin across the family. The one exception is the **import/export specifier list**, whose Prettier form is *not* dual-stable (`divergent_variant_blank_after_comment`): that builder also drops an author blank after a **leading own-line** comment (`import {`, `\t// c`, blank, `\ta`), where nothing is pulled and both formatters agree on the comment's position — a separate behavior of the specifier-list builder, not a consequence of this entry. Object literals are handled in `objects.rs`, arrays in `arrays.rs`, block bodies in the shared block-printing path (`expressions/blocks.rs`), type-parameter declarations in `types/type_params.rs` (covering function/class/interface/type-alias/arrow), function/constructor types in `types/function_types.rs`, value-level function-definition parameter lists (function declarations/expressions, arrows, class methods, constructors, setters, and Svelte `{#snippet}` params) in `build_params_doc_with_comments` (`expressions/functions.rs`, via the same `delimiter_line_comment_prefix` helper as the signature path), object/array destructuring patterns in `expressions/patterns.rs`, `namespace`/`module` bodies in the shared statement-list walk (`statements/type_declarations.rs`, reusing `build_statement_list_docs_into`), class/interface/enum bodies in their member loops (`build_class_body_doc` in `statements/class.rs`; `build_type_elements_doc` and `build_enum_declaration_doc` in `statements/type_declarations.rs`), type literals in the multiline member path (`build_type_literal_doc_inner` → `build_multiline_member_prefix_doc`, `types/type_literal.rs`), import/export specifier lists in the shared multiline comma-list builder (`build_hardline_comma_list`, `statements/modules/specifier_list.rs`; the `with {…}` import-attribute brace passes its first attribute's start through the same builder — [with_open_brace_line_comment](../tests/fixtures/typescript/modules/imports/with_open_brace_line_comment_prettier_divergence/)), tuple types in their multiline element path (`build_tuple_type_doc_with_line_comments`, `types/composite.rs`, using `build_list_leading_comments` for the first element), index signatures in `build_index_signature_member_doc` (`types/type_members.rs`), computed property/member keys in `build_computed_key_bracket_doc` (`expressions/objects.rs`, its breaking path — a computed key never breaks on width, so a line comment in either in-bracket gap, `[`→key (this case) or key→`]` (the [close-bracket sibling](#comment-relocation)), is the only trigger; block comments and the no-comment case keep the flat layout), mapped-type keys in `build_mapped_type_doc` (`types/composite.rs`, its bracket break path — same line-comment-only trigger, a same-line block stays inline), computed member-access (element access) in `build_computed_member_line_comment_bracket` (`chain/adapter.rs`, incl. `?.[`), angle-bracket type assertions in `build_assertion_broken_cast` (`expressions/mod.rs`, whose broken form is an already-broken group so a leading run's soft `line` is measured against the cast rather than the enclosing assignment), and multi-argument type-argument lists in their multiline path — **one shared emitter**, `build_type_arguments_doc_with_line_comments` (`types/type_arguments.rs`), reached from `build_type_arguments_doc` in _type_ position and from `build_type_parameter_instantiation_doc` (`types/type_params.rs`) in call/`new` _expression_ position, both gating on the same `type_arguments_force_expansion` predicate — a single-argument leading *line* comment hugs `<`/`>` (a divergence; prettier expands), while a single-argument own-line *block* comment expands to match prettier — all via the shared `Printer::delimiter_line_comment_prefix` helper (`comments/lists.rs`, wrapping `PartitionedComments` + `should_force_expansion_for_comments`). For the type-param `<` and function/constructor-type `(`, preserving the comment also prevents content loss — otherwise the `<` line comment is dropped and the `(` line comment swallows the following tokens. **Within-scope exceptions** (covered constructs are enumerated above): for type literals, the standard path (type aliases, annotations, function-param literals, intersection-trailing objects) is covered, but the union-member / parenthesized-intersection _alignment_ rendering (`type T = | { // c } | B`) still relocates; type-argument lists are covered in both _type_ position (`Map<A, B>`) and call/`new` _expression_ position (`foo<A, B>(x)`, `new Foo<A, B, C>(x)`), single-argument lists hug a leading *line* comment (a divergence — prettier expands) but expand an own-line *block* comment to match prettier. The value-level function-definition parameter `(` and the `with {…}` import-attribute brace are covered; the sole remaining relocation in this family is the union-member / parenthesized-intersection _alignment_ rendering above, whose fix is coupled to §Intersection Printer Convergence.

**Declaration keyword comments**: `abstract/* b */class B` → Prettier collects all comments between modifier keywords and emits them before the declaration name: `abstract class /* b */ B`. Similarly, `async /* c */ function* F()` → `async function* /* c */ F()`. tsv preserves comments between their original keywords.
**Anonymous function keyword comments**: `function /* c */ ()` → Prettier relocates comments between the keyword and `(` in anonymous function expressions, generators, and export default functions. No params: after `)` before `{`. With params: inside parens before first param. tsv preserves the comment between keyword and parens. Prettier's relocated forms are dual-stable (stable in both formatters); our keyword-adjacent form is only stable in our formatter.

**Anonymous function/class keyword line comments**: Same relocation as block comments above, but with line comments. `class // c\n{}` → Prettier moves into body: `class {\n\t// c\n}` (stable in one pass). `function // c\n()` → Prettier moves after parens: `function () // c\n{}` (pass 1), then into body: `function () {\n\t// c\n}` (pass 2, stable). Not idempotent for functions — takes 2 passes. With params: `function // c\n(x)` → Prettier moves into params: `function (\n\t// c\n\tx\n)` (stable in one pass). Also applies to generators (`function*`), async functions, and export default. tsv preserves the comment between keyword and next token. Both positions are dual-stable.

**Arrow body stripped parens**: `() => (x /* c */)` → Prettier strips the parens and trails the block comment on the body (`() => x /* c */`); a line comment is floated past `;` (`() =>\n\tx; // c`). For curried arrows, every level's parens are stripped and the comment trails (`(a) => (b) => z /* c */`). These are prettier's **first-pass** forms — prettier is non-idempotent on this input, and at its fixed point the comment floats *past the `;`* in every case but the call arg (`() => x; /* c */`, `(a) => (b) => z; /* c */`, `() => x; // c`), detaching it from the body entirely; the `audit_signature.txt` pins the full chain. That past-`;` relocation, not just the paren strip, is the strongest reason for the divergence. tsv preserves parens to keep comments in place: `() => (x /* c */)`, `(a) => ((b) => z /* c */)`. Same approach as unary expression fix. Each formatter normalizes to its own form (prettier strips parens, tsv preserves them). A **leading** comment in the same gap hugs `=>` in both formatters (`() => /* lead */ x /* trail */`) — except over a **ternary** body, where prettier prints its own `shouldAddParensIfNotBreak` layout paren and relocates the leading comment inside it (`() => (/* lead */ cond ? a : b /* trail */)`) while tsv keeps it outside. What tsv prints there are the **authored** parens, retained to hold the trailing comment, so outside them is a position the author chose. With no trailing comment to retain them the authored parens are stripped and only the ternary's layout paren returns — a delimiter that vanishes the moment the body breaks, so a position outside it exists in one rendering only — and there tsv converges with prettier, putting the comment inside ([body_stripped_paren_comment_long](../tests/fixtures/typescript/expressions/arrow/body_stripped_paren_comment_long/)).

**Own-line line comment before `;`, blank line after**: a same-line block comment after an import's source / `with {…}` attributes, and a same-line trailing line comment, both trail **after** the `;` in both formatters (`import { a } from './a'; /* c */`, `import { b } from './b'; // 1`) — tsv matches prettier here, no divergence. The divergence is narrower: an **own-line** line comment sitting between a statement's last token and its terminating `;`, with a **blank line after** it — prettier floats the blank *above* the comment, promoting `// 2` to lead the *following* statement, while tsv keeps the blank *below*, so `// 2` stays attached to the statement it was written under. The trigger is the blank line, **not** the preceding `; // 1` (it holds for a bare own-line comment), and it is **general**, not import-specific (a plain `const` whose own-line comment sits before its `;` with a blank line after diverges the same way) — this is the blank-line sibling of the non-divergence [line_before_semicolon](../tests/fixtures/typescript/syntax/comments/line_before_semicolon/) (own-line comment before `;`, *no* blank → both formatters agree). `input.svelte` is dual-stable; the `unformatted_ours_*` variants pin the split. (`import =` / `export =` keep a same-line trailing **block** before the `;` in both formatters — operand-attached — while a line comment floats after; those are regular fixtures, not divergences.)

**Member `;` that deferred a line comment, second comment on the `;`'s line**: the *run* extension of [line_before_semicolon](../tests/fixtures/typescript/syntax/comments/line_before_semicolon/) — a `//` written between a member's last token and its `;` trails **after** the `;` in both formatters (that fixture's non-divergence), and the divergence begins with whatever the author put on the `;`'s own line behind it. The member's doc now ends with the deferred comment, so nothing may share that output line: tsv hands the second comment to the next member's leading run, where it takes the line below, distinct and in source order. Prettier appends it to the same line instead — **merging** two line comments into one (`a = 1; // c1 // c2`, the second `//` becoming text of the first, so a comment ceases to exist and anything printed behind it would be swallowed) and **reordering** a block ahead of the comment it was written after (`a = 1; /* c2 */ // c1`). It is a **member-body** divergence only: at statement level prettier keeps the pair distinct, agreeing with the statement and block walks, and the rule is the same one those walks state ([comments.md](./comments.md) §The statement-gap seam — the previous item deferred, so it trailed *nothing* on that line and the next item's leading run is the only emitter left). `input.svelte` is dual-stable in both formatters, so the divergence rides the `unformatted_ours_*` variants, which carry the separator-on-its-own-line authoring — one spelling it `;`, the other a type member's `,`, which tsv normalizes to the same printed `;` and therefore defers past identically.

**Try/catch/finally before `{`**: Prettier absorbs line comments between keyword/paren and block into the block body. For catch, the comment gets absorbed into the parameter parens (`catch (\n\te // comment\n)`).

**While before `{}`**: A comment between `)` and the body `{}` stays after `)` in both formatters (`while (a) // c\n{}`). A blank line the author left *before* an own-line comment is dropped by both. The residual divergence is a blank *after* the comment, before `{`, which prettier keeps and tsv drops — see the `line_before_body_comment` fixture above and §"No blank above a body block's `{`".

**Between `}` and `else`**: Prettier keeps an **own-line** block comment between the closing brace and `else` where the author wrote it (`}\n/* c */\nelse` — a match with tsv, no oracle). A block comment leading the `else` keyword on the **same line** (`/* c */ else`) is cuddled there by prettier; tsv splits it onto its own line before `else`. With **multiple** inline block comments (`} /* c1 */ /* c2 */ else`), prettier drops every comment after the first onto its own line while tsv keeps them all inline. tsv preserves the comments before the `else` keyword on their own line.

**Trailing member chain**: When line comments appear before trailing member access in chains (`.length` after `.filter()`), Prettier relocates the comment to after the `=` and keeps the chain inline. For a plain member (`.length`) it puts the comment on its own line under `=`; for an **optional**-chain trailing member (`?.length`) it instead trails the comment on the `=` line itself (`const b = // comment`) and de-indents the value. tsv keeps the comment before the trailing member with the chain broken in both cases.

**Block comment in computed `[]`**: When a computed member access with a block comment exceeds print width, Prettier hoists the comment from inside the brackets to before the member chain. Not idempotent.

**Switch case colon comment**: Prettier relocates comments near the colon in switch cases: `case 1: /* c */` → `case 1 /* c */:` (moved before colon); `default /* c */:` → `default: /* c */ break;` (moved to body). tsv preserves comment placement. Three stable forms exist: our input form, prettier's `output_prettier` form, and a third body-only form.

**Class property modifier**: Both positions are dual-stable in both formatters. Our canonical form puts the comment after the modifier (`a? /* c */ = 1;`), Prettier's canonical form puts it before (`a /* c */? = 1;`). The user's chosen position is preserved. Same for `!`. Note: when a type annotation follows (`a /* c */?: T`), preserving the comment is a correctness matter, not a divergence — it is otherwise dropped entirely (content loss).

**Interface / type-literal member after `?`**: `a? /* c */ : number;` → Prettier moves before `?` (`a /* c */?: number;`), and `m? /* c */(x): void;` → inside the parens (`m?(/* c */ x): void;`). tsv preserves both after `?`, identically for interface members and type-literal members (the two type-element printers split around `?` consistently). Both positions are dual-stable. A method that has **type parameters** is not a divergence: there Prettier keeps the comment after `?` (`m?/* c */ <T>(…)`), so both formatters agree — a regular fixture pins it together with a comment *inside* `<>` (`m<T /* c */>`, also kept in place by both): `types/type_members/method_type_params_comment`.

**Optional `?` to `:` line comment**: a line comment in the gap between an optional `?` and the member's `:` annotation (`a? // c\n: number`) → Prettier relocates it to trail the member `;` (`a?: number; // c`). tsv preserves it after `?`; because a line comment must end its line, the annotation is forced onto the next line. Applies to interface members, type-literal members, and class properties. Preserving the comment is also a content-loss fix: emitting it inline would swallow the `: number` annotation as comment text (non-idempotent).
**Member key to `:` line comment (non-optional)**: the no-marker counterpart of the case above — a line comment between a (non-optional) property key and its `:` annotation (`a // c\n: number`) → Prettier relocates it to trail the member `;` (`a: number; // c`). tsv preserves it after the key, the annotation forced onto the next line. Applies to interface members, type-literal members, and class properties. Like the optional case this is also a content-loss fix (emitting the line comment inline would swallow the `: number` annotation — non-idempotent); a same-line block comment in the same gap stays inline in both formatters (`a /* c */: number`) and is not a divergence.
**Variable definite `!`**: `let a /* c */!: number;` → Prettier moves comment after `!` (`let a! /* c */ : number;`). tsv preserves before `!`.
**Function param optional `?`**: `function fn(a /* c */?: number) {}` → Prettier moves comment after `?` (`function fn(a? /* c */ : number) {}`). tsv preserves before `?`. Same pattern applies to arrow function params.

**Computed key after `]`**: `[x] /* c */ = 1` → Prettier moves inside brackets (`[x /* c */] = 1`). Applies to object literals, class members, interface members, and destructuring patterns. For interface `set` accessors, prettier moves into params instead (`set [x](/* c */ a)`). tsv preserves between `]` and the next token.

**Heritage last item before `{`**: `class A implements I, J // c {}` → Prettier relocates line comment from after the last heritage item into the class/interface body (`J {\n\t// c\n}`). tsv preserves the comment before `{` with a forced line break (`J // c\n{}`). When more than one comment precedes `{`, each is kept on its own line (`J // c1\n// c2\n{}`) — collapsing them onto the heritage line would absorb a following comment into the first line comment's text (`// c1 // c2` reparses as one comment), a content/boundary loss, not just a position change. Affects class `implements`, class `extends`, class expressions, and interface `extends`; the same preservation applies to the interface name/type-params→body gap when there is no `extends`. Consistent with tsv's handling of line comments before block bodies across all statement types — and now across the value-level function definitions too, which used to make prettier's relocation themselves (the Value-level function definition, signature→body `{` row above).

**Type params to `(`**: `<T> /* c */(x: T)` → Prettier moves inside parens as leading comment on first param (`<T>(/* c */ x: T)`). Prettier's behavior is context-dependent: for function declarations/expressions and class methods **with a body**, prettier preserves the comment between `>` and `(` (no divergence). For body-less declarations (overloads, abstract methods, interface/type literal method signatures, call/construct signatures, function/constructor types, declare functions) and arrow functions, prettier moves the comment inside parens. tsv preserves between `>` and `(` in all cases. Both positions are dual-stable.

**Union infix `|` line comment**: `A | // c\n B` (a line comment trailing the infix `|`) → Prettier relocates the comment to trail the previous member (`| A // c\n| B`). tsv keeps it on the separator/`B` side, on its own line so the pipe stays attached (`| A\n// c\n| B`) — the comment sits after the `|`, so tsv associates it with `B` rather than `A`. Both forms are dual-stable; the divergence is in how the infix-pipe input normalizes.

**Retained paren union member comment**: a block comment inside a parenthesized union member whose parens are **retained** — because the member nests in an outer union or intersection (`a | (b | c /* c */)`, `(a | b /* c */) | c`, `a & (b | c /* c */)`) → Prettier hoists a **trailing** comment out of the parens to after `)` (`a | (b | c) /* c */`, `(a | b) /* c */ | c`, `a & (b | c) /* c */`), but leaves a **leading** comment inside (`a | (/* c */ b | c)` — a match with tsv). tsv keeps every such comment inside the parens, associating it with the parenthesized member. (Otherwise the comment is dropped entirely — content loss.) When the parens are redundant and stripped — a top-level or single-member union — both formatters keep the comment in place (no divergence; see `union_intersection_parens_comment`). The parenthesized-_intersection_-in-union member (`a | (b & c /* c */)`) already preserves in place through a separate path. Both positions are dual-stable in our formatter.**Retained paren union member line comment**: the line-comment analog of the above — a line comment trailing the last inner member of a retained parenthesized union (`(a | b // c) | c`, `a | (b | c // c) | d`) → Prettier hoists it out to trail the whole member and keeps the inner union inline (`| (a | b) // c`). tsv keeps it inside the parens; because a line comment must end its line, the parenthesized union expands to its broken form (one member per line) with `)` on its own line. (Otherwise the comment is dropped entirely — content loss.) Unlike the block-comment case — which stays inline because a block comment can — the line comment forces the expanded layout. Our expanded form is stable in tsv; Prettier's inline-with-relocated-comment form is its own stable shape.

**Retained paren first-member leading line comment**: a **leading** line comment inside a retained parenthesized union member, when that member is the **first** member of the outer union (`(// c\n A | B) | C`) → Prettier moves the comment out of the parens to lead the member, keeping the inner union inline when it fits (`| // c\n (A | B)`). tsv keeps it inside the parens leading the inner union; because a line comment must end its line, the parenthesized union expands to its broken form with `)` on its own line. (Otherwise the comment is dropped entirely — content loss: the inner-leading line comment has no previous member to relocate onto.) This is the leading-comment counterpart of the trailing **Retained paren union member line comment** above, and mirrors its keep-inside behavior. A leading line comment inside a **later** member's parens is kept inside the parens the same way — tsv associates it with the member it documents rather than hoisting it out, for every member and not just the first (`A | (// c\n B | C) | D` → `| (\n // c\n B | C\n)`); Prettier hoists it onto its own line above the member. Both cases live in [union_intersection_retained_paren_leading_line_comment](../tests/fixtures/typescript/types/union_intersection_retained_paren_leading_line_comment_prettier_divergence/). An **authored-trailing** line comment (`| A // c`, written on the member's own line rather than inside a following member's parens) is a distinct case that stays trailing in both formatters (see [union_paren_member_long_line_comment](../tests/fixtures/typescript/types/comments/union_paren_member_long_line_comment/)).

**Retained paren intersection member comment**: the intersection counterpart of the retained-paren-union case — a block comment inside a parenthesized **intersection** member whose parens are retained because it nests in an outer union (`(a & b /* c */) | c`, `a | (/* c */ b & c)`, `a | (b & c /* c */)`) → Prettier hoists the comment out of the parens (trailing after `)`, leading before `(`: `(a & b) /* c */ | c`, `a | /* c */ (b & c)`). tsv keeps it inside the parens, associating it with the parenthesized member. For a **plain** intersection member this never dropped — it preserves through the paren-unwrapping path — so that half is pinned for completeness. The **trailing-object** member (`(a & { x: X } /* c */) | c`, and the same shell as an optional tuple element `[(a & { x: X } /* c */)?]`) is a different printer: its aligned layout is built from the already-unwrapped intersection and emits the shell's `(`…`)` itself, so *both* of the shell's gaps are invisible to every other emitter — the leading one is threaded in for the [leading line comment](#comment-relocation) case above, the trailing one here (otherwise the comment is dropped entirely — content loss, whether or not the object has members). A **line** comment in the trailing gap keeps its line with the `)` dropped to the next, mirroring the retained-paren union shell's expanded form; the comment's break lands on the enclosing union rather than forcing the object literal open, and the `)` takes the same `align(2)` sub-tab offset (under the `(`) that the object's own `})` closer takes when it breaks. Every position is dual-stable in our formatter.

**Retained paren shell trailing gap, comment run**: the ≥2-comment extension of the two retained-paren-shell entries above, for both shells that print their own `(`…`)` — the union one and the intersection-with-trailing-object one → Prettier hoists the whole run out of the parens, the first comment trailing the member and the rest leading the next (`| (a & { x: X }) // c1` then `// c2`); tsv keeps every comment inside the shell, which is the placement rule those entries already record. What the run adds is orthogonal to placement and is **not** a divergence: the comments must stay **distinct**. A run emitted with nothing between its comments welds them — the second `//` becomes text of the first (`// c1 // c2`), so the second comment stops existing — and a **block** comment following a line comment additionally **reorders** ahead of it, since a block is inline while a line comment rides `line_suffix`. Each comment following a line comment therefore takes a break before it (a `//` runs to end of line and cannot share it), while two **glued** block comments keep sharing their line: the separator is conditional on what precedes it, not blanket. The run continues at the column of the region its shell emits it into — the `)`'s `align(2)` sub-tab offset for the intersection shell, whose gap is emitted outside the object's group, and the interior indent for the union shell, whose gap is emitted inside it. This is the trailing-gap analog of the rule in [comments.md §Trailing and dangling runs](./comments.md); prettier keeps the comments distinct too, so it is the oracle for that half even though it is not the oracle for placement.

**A synthesized paren never lands between a comment and the token it is glued to** (`◆prettier_bug`, pinned by [pure_annotation_enclosing_parens](../tests/fixtures/typescript/syntax/comments/pure_annotation_enclosing_parens_prettier_divergence/)). Some comments are *bound to the token after them* — the whole point of `/* @__PURE__ */` is that it marks the call it precedes as side-effect-free, so rollup, esbuild and terser may drop the call when its result is unused. When the formatter adds a paren around an expression whose left edge is such a call, that paren must go **outside** the comment:

```js
const a = cond ? p : (/* @__PURE__ */ f() ?? y); // tsv — `f()` is still pure
const a = cond ? p : /* @__PURE__ */ (f() ?? y); // prettier — the mark is now inert
```

`f() ?? y` is not a call, so no bundler reads the relocated comment as an annotation: `f()` is silently retained. Nothing was added or dropped — the comment just crossed a boundary that changed what it binds to. This is [the JSDoc-cast bug](#jsdoc--paren-semantics) again, and it has the same cause: prettier's leading comments are applied by its *generic* printer on top of whatever the language printer returned, parens included (`src/main/ast-to-doc.js` wrapping `src/language-js/print/index.js`), so a comment is **structurally** outside the parens for every node. Its one escape hatch is `willPrintOwnComments`, an allowlist of node types. Two differences from the cast case make this one worse: prettier is **idempotent** on its own output here, so no second pass exposes the shift (the cast's non-idempotence is what surfaced *that* bug), and the annotation has no AST node at all, so there is nothing to hand ownership to.

tsv's rule is therefore about the **comment**, not the node: a comment glued to the token after it is printed by the innermost node that token begins (`Comment::owned_by_node`, set in the parser, skipped by every gap emitter). The comment travels inside its node's doc, so a paren synthesized around any enclosing expression is outside the pair by construction — at every paren site, present or future.

**Every glued block comment is owned, not just bundler annotations.** The position — a block comment glued to the token after it — is an authoring choice that binds the comment to the operand it leads, so there is no content sniff: `/* @__PURE__ */` and a plain `/* c */` bind their token identically, and no synthesized paren lands between them. An ordinary leading comment in a stripped-grouping-paren position is therefore preserved glued where prettier relocates it out — `(/* c */ x ?? y)` stays put, prettier hoists to `/* c */ (x ?? y)` — at every operand position: the ternary operand ([test_paren_leading_comment](../tests/fixtures/typescript/expressions/ternary/test_paren_leading_comment_prettier_divergence/)), the sequence operand ([operand_edge_comment](../tests/fixtures/typescript/expressions/sequence/operand_edge_comment_prettier_divergence/)), the unary assignment operand ([assignment_operand_leading_comment](../tests/fixtures/typescript/expressions/unary/assignment_operand_leading_comment_prettier_divergence/)), and the non-null / await / yield grouped operands ([§Comment relocation](#comment-relocation)). The annotation **convention** (`@`/`#` plus `__NAME__`) is retired as an ownership gate; the only remaining content sniff is `is_jsdoc_type_cast_comment`, and it governs *cast paren-retention* (the `JsdocCast` node below), never ownership — ownership flows to a cast the same as to any other glued comment.

**Ownership makes a comment invisible to emit-decisions, NOT to layout.** This is the rule for anyone extending the mechanism. An owned comment is skipped by the *to-emit* lookups (`comments_to_emit_in_range`) — the owning node prints it — so any gate deciding *who emits* must use them (`has_comments_to_emit_in_range`). But the comment is still on the page, still occupies width, and still means to prettier's rules exactly what an ordinary comment means. So any gate deciding *layout* must use the *on-page* lookups (`has_comments_on_page_in_range`), which count it. Get this backwards and the comment silently disappears from a layout decision: a unary operand loses the parens that keep the comment bound to the operand rather than the operator (`!(/* @__PURE__ */ f())` → `!/* @__PURE__ */ f()`), and a last argument loses the leading comment that refuses the expand-last hug. Both were real bugs. The unary wrap is decided **positionally** — the comment sits in the operator→operand gap, so `build_unary_doc` sees it there and adds the parens itself; `needs_parens` deliberately does *not* (it would double-wrap), and needs no left-spine walk, which is why an instantiation-expression operand (`!(/** @type {A} */ (x)<T>)`) gets the wrap for free.

### Comments inside a multi-word keyword

A comment can sit **inside** a keyword that spans two or more words — `await /* c */ using`,
`export /* c */ default`, `declare /* c */ const`. tsv preserves it there; Prettier relocates it
past the keyword's last word, onto the value (`await using /* c */ y`).

Preserving is the [Comment Position Philosophy](./conformance_prettier.md#comment-position-philosophy) default, and three
things confirm it rather than merely permit it:

1. **The carve-out doesn't reach.** tsv trails a comment past a *pure separator* — a list
   element's comma, which is structure. A keyword's words are not separators; `using`,
   `default`, and the kind keyword each carry meaning, so the gap before them is a position an
   author can mean.
2. **Prettier's own sibling gap preserves.** `export /* c */ const x = 1` and
   `export /* c */ function fn() {}` keep the comment after `export` in *both* formatters — only
   `export /* c */ default` relocates. Preserving makes the whole `export`→X family read one way;
   matching Prettier would split it on nothing but the follower.
3. **Relocation collapses a distinction.** With comments on both sides of the second word,
   Prettier lands them in one place (`await /* c2 */ using /* c3 */ b` →
   `await using /* c2 */ /* c3 */ b`), so before-`using` and after-`using` become
   indistinguishable. Text survives; the association does not.

`for await /* c */ (…)` — the gap *after* the same class of keyword — already preserves for the
same reason ([for_await_keyword_comment](../tests/fixtures/typescript/statements/for/for_await_keyword_comment_prettier_divergence/)),
so preserving the interior gap makes the treatment uniform.

A *line* comment in such a gap takes the [uniform forced-continuation
indent](./conformance_prettier.md#uniform-forced-continuation-indent) (`export // c⏎\tdefault 1;`); Prettier pulls the
value back flush. Both the authored and the relocated positions are stable under tsv.

"Uniform" is meant literally, and a multi-gap keyword is where it has to be said: a header whose
gaps break **more than once** continues at **one** level, not one per gap
([export_as_namespace_line_comment](../tests/fixtures/typescript/modules/exports/export_as_namespace_line_comment_prettier_divergence/)).
Each gap is emitted without its own indent and the whole tail is wrapped once. Indenting per gap
compounds, and the staircase is not merely deep — the keyword→value gap is emitted at the header's
own level, so the keyword's last word would sit *below* the value following it. Only a three-word
keyword (`export as namespace`, the import-equals header) can show the difference.

The tail has to be *inside* that one `indent`, not a sibling after it. The distinction is invisible
for a tail that can't break — a bare name renders the same either way, because the gap's hardline
has already written the indent the name lands on. It shows only when the tail breaks **internally**,
and the import-equals module reference is the one that can: as a sibling, `require(`'s own line
breaks resolve at the *outer* level, leaving its contents level with it and its `)` a level above
the `require(` it closes
([equals_header_line_comment](../tests/fixtures/typescript/modules/imports/equals_header_line_comment_prettier_divergence/)).

Not every multi-word keyword admits an interior comment: `declare // c⏎const x` ASI-splits into
two statements, and `await /* c⏎ */ using` demotes (the comment *is* a `LineTerminator`, and
`await [no LT] using` requires none) — see the exclusions under [Declaration- and module-header
line-comment continuation indent](#comment-relocation).

The same rule covers a keyword whose second word is a punctuator (`export /* c */ = value`) and
multi-gap headers (`export /* c1 */ as /* c2 */ namespace Foo`, the import-equals header).

It reaches past keywords, too. A **meta property** is the same shape — `new` `.` `target` — and both
gaps around its dot are positions an author can mean (`new /* c */.target`, `new./* c */ target`).
So is a **qualified name** (`ns` `.` `Type`), which is why one printer (`build_dotted_pair_doc`)
serves both: neither can regrow the hole independently, and through the qualified name it covers
type references, interface heritage, and import-equals module references at once. tsv keeps each
comment on its authored side, which is what prettier does for a block comment; a *line* comment
there is a divergence
([dot_gap_line_comment](../tests/fixtures/typescript/expressions/misc/meta_property/dot_gap_line_comment_prettier_divergence/),
[qualified_name_dot_gap_line_comment](../tests/fixtures/typescript/types/qualified_name_dot_gap_line_comment_prettier_divergence/)).

Those two are worth knowing for a different reason: the detector for this class — a `d.text` literal
with an *interior* space — **cannot see them**, because the joining literal is `"."`. The class is
"a header that concatenates fixed pieces without scanning the gaps between them"; the interior-space
grep is a proxy for it, not a definition of it, so a punctuator-joined header needs finding by hand.
Both were found that way, and both had been **dropping** every comment in both gaps.

**Preserving is not the same as diverging, and most of these gaps are neither.** Prettier's
treatment is not uniform: it relocates the `export as namespace` keyword-interior gaps, but
**preserves** the `namespace`→name gap and four of the five import-equals gaps. Where Prettier
preserves, tsv's old behavior was not a divergence at all — it **dropped** the comment, which is
content loss and strictly worse. Those gaps are pinned by *regular* fixtures
([export_as_namespace_name_comment](../tests/fixtures/typescript/modules/exports/export_as_namespace_name_comment/),
[equals_header_comment](../tests/fixtures/typescript/modules/imports/equals_header_comment/)), not
divergence ones; a divergence fixture states an opinion, and there is no opinion to state where the
two formatters already agree.

Catalogued in [§Comment relocation](#comment-relocation):
[default_keyword_comment](../tests/fixtures/typescript/modules/exports/default_keyword_comment_prettier_divergence/),
[default_keyword_line_comment](../tests/fixtures/typescript/modules/exports/default_keyword_line_comment_prettier_divergence/),
[default_keyword_decorator_comment](../tests/fixtures/typescript/modules/exports/default_keyword_decorator_comment_prettier_divergence/),
[decorator_first_default_keyword_comment](../tests/fixtures/typescript/modules/exports/decorator_first_default_keyword_comment_prettier_divergence/),
[declare_keyword_comment](../tests/fixtures/typescript/declarations/variable/declare_keyword_comment_prettier_divergence/),
[await_keyword_comment](../tests/fixtures/typescript/typescript_specific/using/await_keyword_comment_svelte_prettier_divergence/),
[export_equals_keyword_comment](../tests/fixtures/typescript/modules/exports/export_equals_keyword_comment_prettier_divergence/),
[export_as_namespace_keyword_comment](../tests/fixtures/typescript/modules/exports/export_as_namespace_keyword_comment_prettier_divergence/),
[export_as_namespace_line_comment](../tests/fixtures/typescript/modules/exports/export_as_namespace_line_comment_prettier_divergence/),
[equals_type_keyword_comment](../tests/fixtures/typescript/modules/imports/equals_type_keyword_comment_prettier_divergence/),
[equals_header_line_comment](../tests/fixtures/typescript/modules/imports/equals_header_line_comment_prettier_divergence/),
[qualified_name_dot_gap_line_comment](../tests/fixtures/typescript/types/qualified_name_dot_gap_line_comment_prettier_divergence/).

### JSDoc / paren semantics

- JSDoc type cast parens (standalone TS) — [jsdoc_type_cast_ts](../tests/fixtures/typescript/syntax/comments/jsdoc_type_cast_ts_prettier_divergence/)
- JSDoc type cast in Svelte template / directive positions — [jsdoc_cast_template](../tests/fixtures/svelte/syntax/comments/jsdoc_cast_template_svelte_prettier_divergence/)
- ◆prettier_bug — an enclosing paren emitted *between* the cast comment and its `(`, which re-binds the cast — [jsdoc_type_cast_enclosing_parens](../tests/fixtures/typescript/syntax/comments/jsdoc_type_cast_enclosing_parens_prettier_divergence/)

**JSDoc type cast parens.** `/** @type {T} */ (expr)` is a TypeScript type **assertion** (cast): the parentheses are required, and `/** @type {T} */ expr` (no parens) is *not* a cast. tsv therefore **preserves** the parens of a JSDoc cast everywhere — the parser records the cast in an internal `JsdocCast` wrapper node, keyed on the exact paren extent, so `(a.b)` (cast the member) and `(a).b` (cast the base) stay distinct and nested casts keep each level. The public AST stays paren-free, matching acorn/Svelte (which carry no `ParenthesizedExpression`). tsv never *adds* parens — only a `@type`/`@satisfies` block comment immediately before a `(` triggers preservation; a non-`@type` comment, or one not adjacent to a `(`, is stripped as ordinary grouping.

The divergence is **JS-vs-TS**, keyed on prettier's parser backend rather than on file type:

- **JS contexts** (`.js`, `.svelte` plain `<script>` — prettier's babel parser) **preserve** the cast parens too: prettier's `is-type-cast-comment.js` + the `postprocess` `shouldKeepParenthesizedExpression` check keep a `ParenthesizedExpression` whose immediately-preceding comment is a type cast. tsv **matches** here — see the match fixtures [jsdoc_type_cast_svelte](../tests/fixtures/typescript/syntax/comments/jsdoc_type_cast_svelte/), [jsdoc_type_cast_extent](../tests/fixtures/typescript/syntax/comments/jsdoc_type_cast_extent/), [jsdoc_type_cast_nested](../tests/fixtures/typescript/syntax/comments/jsdoc_type_cast_nested/), and [jsdoc_type_cast_keyword](../tests/fixtures/typescript/syntax/comments/jsdoc_type_cast_keyword/). Because tsv formats context-free, these JS matches also **oracle-anchor the preserved layout** that the TS divergence shares — the wide cases ([jsdoc_cast_call_arg_long](../tests/fixtures/typescript/expressions/calls/jsdoc_cast_call_arg_long/), [jsdoc_cast_multiarg_long](../tests/fixtures/typescript/expressions/calls/jsdoc_cast_multiarg_long/), [jsdoc_cast_arg_long](../tests/fixtures/typescript/expressions/calls/jsdoc_cast_arg_long/), [jsdoc_cast_paren_long](../tests/fixtures/typescript/calls/chained/jsdoc_cast_paren_long/), [jsdoc_cast_before_eq](../tests/fixtures/typescript/declarations/variable/jsdoc_cast_before_eq/), [jsdoc_cast_paren_span](../tests/fixtures/typescript/declarations/variable/jsdoc_cast_paren_span/), [arrow_jsdoc_cast_body_long](../tests/fixtures/typescript/calls/arrow_jsdoc_cast_body_long/)) pin how the preserved parens break against prettier-plugin-svelte rather than against tsv's own output.
- **TS contexts** (`.ts`, `.svelte` `<script lang="ts">` — prettier's oxc-ts parser) **strip** the parens: oxc-ts is cast-unaware (the preservation is gated behind `if (!isOxcTs)`), so prettier silently drops the assertion. tsv preserves → **divergence** (the fixtures listed above). When the inner expression needs parens anyway (e.g. an assignment in `return`), prettier keeps them in both backends, so that case stays a match.

tsv runs one context-free TypeScript formatter, so "preserve" is uniform: it matches prettier in JS contexts and diverges from prettier-TS's strip. This is the **more correct** behavior, not merely a taste call — the parens carry meaning, and prettier's own babel path special-cases preserving them (it strips meaningless parens aggressively everywhere else). The cast paren is **opaque to layout heuristics** (expand-last etc.), mirroring acorn's `ParenthesizedExpression`, so a long cast breaks via standard arg breaking and the inner expands inside the preserved parens. An object/array literal inner **hugs** the cast parens (`({…})` / `([…])`) — see [jsdoc_type_cast_object](../tests/fixtures/typescript/syntax/comments/jsdoc_type_cast_object/) — while a line comment between the `(` and the inner forces a hardline so it can't swallow the inner — see [jsdoc_type_cast_gap_comment](../tests/fixtures/typescript/syntax/comments/jsdoc_type_cast_gap_comment/). Negatives that pin the boundary: [jsdoc_type_cast_member](../tests/fixtures/typescript/syntax/comments/jsdoc_type_cast_member/) and [jsdoc_type_cast_nonadjacent](../tests/fixtures/typescript/syntax/comments/jsdoc_type_cast_nonadjacent/) (comments that are not casts), [jsdoc_type_cast_word_boundary](../tests/fixtures/typescript/syntax/comments/jsdoc_type_cast_word_boundary/) (the ASCII `\b` after `@type`/`@satisfies`, mirroring prettier's regex), and [jsdoc_type_cast_ts](../tests/fixtures/typescript/syntax/comments/jsdoc_type_cast_ts/) (never-add + no double-wrap in standalone TS).

**An enclosing paren goes OUTSIDE the cast comment** (`◆prettier_bug`, pinned by [jsdoc_type_cast_enclosing_parens](../tests/fixtures/typescript/syntax/comments/jsdoc_type_cast_enclosing_parens_prettier_divergence/)). A cast is the comment **plus** the `(` it is glued to. When the formatter adds a paren around an expression whose *left edge* is a cast — `??` clarity parens under a ternary, the required parens of a `??` operand under `||`, a member/call base, a `new` callee, a `**` operand, a value-position sequence or assignment — that paren must land **outside** the comment. Prettier emits it **between** the comment and the cast's own `(`:

```js
const a = cond ? x : /** @type {A} */ ((b).c ?? d); // prettier, pass 1
const a = cond ? x : /** @type {A} */ (b.c ?? d); // prettier, pass 2 (fixed point)
const a = cond ? x : (/** @type {A} */ (b).c ?? d); // tsv — cast still on `b`
```

Prettier's pass-1 output is **not a fixed point**: reparsed, the comment is now glued to the *enclosing* paren, so that paren becomes the cast and the original `(b)` decays to a redundant grouping paren, which pass 2 strips. The fixed point it lands on has **changed the program's meaning** — `@type` annotated `b`, and now annotates all of `b.c ?? d`, so `b` goes unchecked. The same bug is in oxfmt and biome; there is no formatter that gets it right.

tsv holds the pair together **structurally** rather than re-deriving it from text: the parser gives the `JsdocCast` node ownership of its comment (`Comment::owned_by_node`), so the comment is printed by the cast and skipped by every gap emitter — no synthesized paren can be placed between the two, at any of the ~29 paren sites, present or future. Prettier re-decides cast-hood from text adjacency on every parse, which is exactly why its own output doesn't survive a second pass.

Note that a cast re-binding is **invisible to every existing gate**: the cast is not a node in the public AST (the writer unwraps it) and grouping parens aren't either, so both forms serialize to byte-identical wire JSON — `ast_diff` calls them "semantically equivalent", `roundtrip_audit`'s skeleton compare cannot see it, and `corpus:compare:format`'s char-frequency SAFETY check cannot either (the characters only move). This one surfaced solely because prettier's version happens to be non-idempotent, which F1 catches. The fixture is therefore the only pin, and a dedicated audit (format → reparse → compare each cast's inner subtree, on the *internal* AST) is tracked as follow-up work.

A unary operand takes the cosmetic wrapping pair prettier also emits (`!(/** @type {A} */ (expr))`): bare, the comment reads as annotating the operator rather than the operand. Both forms are the same AST; the wrap lands outside the comment either way. The rule is **positional, not structural** — the comment sits in the operator→operand gap, so `build_unary_doc` sees it there and adds the parens, which means it applies to *any* operand shape (a member chain, a `**` operand, an instantiation expression) and to a bundler annotation as well as a cast, with no node list to keep in sync. An operand that already prints **its own value-position pair** is the exception: that pair encloses the comment, so the comment-holder wrap is not added again — `!(/* c */ x = y)` (one pair), never `!((/* c */ x = y))`. This covers every `needs_parens` operand — an assignment, a conditional, an arrow, await/yield, a type assertion — plus a sequence (whose own printer supplies the pair). Binary is the one operand that is *not* the exception: its plain path doesn't prepend the owned comment, so it keeps taking the comment-holder pair (a single pair — the redundant inner wrap is stripped). For the hoisting operands prettier instead relocates the comment out of the operand's parens (`!(/* c */ (x = y))`, `!(/* c */ (cond ? b : c))`) — a divergence, see [assignment_operand_leading_comment](../tests/fixtures/typescript/expressions/unary/assignment_operand_leading_comment_prettier_divergence/) and [paren_operand_leading_comment](../tests/fixtures/typescript/expressions/unary/paren_operand_leading_comment_prettier_divergence/); a **type-assertion** operand instead keeps the single pair in *both* formatters (a match — [type_assertion_operand_leading_comment](../tests/fixtures/typescript/expressions/unary/type_assertion_operand_leading_comment/)). A *trailing* comment on such an operand does keep both pairs (`!((x = y) /* c */)`, [operand_paren_comment](../tests/fixtures/typescript/expressions/unary/operand_paren_comment/)) — the comment-holder path fires on `has_trailing_comments`, so only the leading-comment case collapses to one pair.

**Svelte template / directive positions are not covered by the JS-vs-TS split.** The "JS preserves" rule above holds only inside `<script>` (prettier's babel/oxc path). In a Svelte **template** — attribute values (`title={…}`), block tests (`{#if …}`), and mustache tags (`{…}`, `{@html …}`) — prettier-plugin-svelte routes the expression through a path that **strips** the cast even in a plain (JS) component. tsv preserves uniformly → divergence, pinned by [jsdoc_cast_template](../tests/fixtures/svelte/syntax/comments/jsdoc_cast_template_svelte_prettier_divergence/) (also a `_svelte_divergence` — the cast comment's `leadingComments` attachment differs from Svelte; see [conformance_svelte.md](./conformance_svelte.md) §Comment Attachment Differences). The `{@const}` case is a prettier **bug**: `{@const y = /** @type {T} */ (z)}` makes prettier-plugin-svelte emit invalid output `(z}` (it drops the `)`) and then throw on its own output; tsv preserves it correctly, so it is documented here but cannot be pinned as an `output_prettier.*` oracle.

### Comment normalization (stable quirks)

Prettier has multiple stable forms for comment positioning. tsv normalizes to a single canonical form.

- Member access after-call comment — [trailing_member_after_call_comment](../tests/fixtures/typescript/expressions/calls/chained/trailing_member_after_call_comment_prettier_divergence/)
- Type-alias union value before-`;` line comment (2-pass: spurious union break, then collapse) — [type_alias_value_trailing_comment](../tests/fixtures/typescript/types/comments/type_alias_value_trailing_comment_prettier_divergence/)
- Value-position parenthesized-assignment trailing line comment → Floated out past `;` (`const y = (a = b); // c`); tsv keeps it inside the parens, consistent with the sequence/block cases (prettier itself keeps the *block* case inside) — [init_assignment_paren_line_comment](../tests/fixtures/typescript/statements/variable/init_assignment_paren_line_comment_prettier_divergence/)
- Block comment mid-chain — [block_comment_chain](../tests/fixtures/typescript/expressions/calls/chained/block_comment_chain_prettier_divergence/)
- Property signature leading line comment — [annotation_simple](../tests/fixtures/typescript/types/comments/annotation_simple_prettier_divergence/)
- Property signature leading block — [annotation_leading_block](../tests/fixtures/typescript/types/comments/annotation_leading_block_prettier_divergence/)
- Param/destructuring default block, own-line value → tsv collapses to after `=` (`a = /* c */ b`), prettier to before `=` (`a /* c */ = b`) — [param_default_own_line_block_comment](../tests/fixtures/typescript/declarations/function/param_default_own_line_block_comment_prettier_divergence/)

**Computed access comment**: Prettier requires 2 passes to stabilize line comments before computed access (`[0]`). The intermediate form places the comment inside brackets (`[// comment\n0]`), which then normalizes to end-of-line (`[0]; // comment`). tsv reaches the stable form in one pass.

**Block comment mid-chain**: When nested grouping parens with block comments are stripped on a member chain (e.g., `/* outer */ (/* inner */ (a).b).c(fn)`), Prettier repositions the inner comment mid-chain (`a /* inner */.b`) and breaks the chain. Prettier requires 2 passes to stabilize the spacing: pass 1 produces `a/* inner */ .b`, pass 2 produces `a /* inner */.b`. Both passes break the chain at the same point. Not JSDoc-specific — any block comment before stripped grouping parens triggers this. tsv normalizes directly to the stable form in one pass.

**Property signature leading block**: A block comment between `:` and the type in a property signature has two intentional stable positions (`a: /* block */ X;` after `:`, and `a /* block */: X;` before `:`); both formatters preserve each when given as input. The divergence is in normalizing **unstable** layouts — when the user breaks the line around the comment (`a: /* block */\n X;` or `a:\n /* block */\n X;`), tsv compacts to the inline form after `:`, while prettier eventually relocates the block before the `:` (sometimes via a multi-pass convergence). Neither choice is information-destructive; this is purely about which canonical target to favor for ambiguous inputs.

**Param/destructuring default block, own-line value**: The exact analog of the property-signature case at a parameter (or destructuring binding) `=` default. A block comment in the `=`→value gap has two intentional inline positions — `a = /* block */ b` after `=` (on the value) and `a /* block */ = b` before `=` (on the name) — and both formatters preserve each when given as input. When the value is authored on its own line (`a = /* block */\n b`), tsv collapses to the after-`=` form while prettier collapses to the before-`=` form. Block comments inline losslessly, so neither wraps; the only difference is which side of `=` the comment lands on for the ambiguous own-line input.

**Property signature leading line comment**: For a line comment between `:` and an inline-renderable type in a property signature (`{ prop: // c\n X }` — covers identifiers, optional `?:`, readonly, computed keys, generics like `Array<X>`, tuples, function types, `typeof`, etc.), Prettier moves the comment past the implicit `;` to end-of-line (`prop: X; // c`); tsv keeps the comment after `:` and drops the type to a continuation line indented one level (`prop: // c\n\t X;`, the [Uniform Forced-Continuation Indent](./conformance_prettier.md#uniform-forced-continuation-indent)). Both forms are stable under their own formatter. A multi-member **union** in the same position **diverges** — prettier drops the comment onto its own line after `:` while tsv keeps it trailing the `:` + indents the union (the [annotation](../tests/fixtures/typescript/types/comments/annotation_prettier_divergence/) fixture); a multi-member **intersection** likewise **diverges** (prettier keeps it flush, tsv indents — see [annotation_continuation_indent](../tests/fixtures/typescript/types/comments/annotation_continuation_indent_prettier_divergence/)). Notably, prettier's end-of-line motion is information-destructive when more than one comment touches the property: leading line + trailing line collapses to `f: X; // leading // trailing` (second `//` becomes text inside the first comment); two leading lines merge **and reverse** order (`g: // c1\n // c2\n X;` → `g: // c2 // c1\n X;`); leading line + trailing block reorders to `h: X; /* trailing */ // leading`. tsv preserves each comment at its authored position as a separate comment node. The end-of-line **relocation** is property-signature-only — prettier keeps variable declarations (`const e: // c\n X = ...`) and class properties (`class C { prop: // c\n X }`) in place — but tsv's continuation **indent** is universal across all these contexts, so those keep-in-place cases become an indent-only divergence too (the same [annotation_continuation_indent](../tests/fixtures/typescript/types/comments/annotation_continuation_indent_prettier_divergence/) fixture).

