<script>
	// Own-line comment before the body block
	for (;;)
	// c
	{
		fn();
	}

	// Multiple own-line comments: a blank between them is kept; a blank above the
	// first (the header gap) is dropped
	for (;;)
	// c1

	// c2
	{
		fn();
	}
</script>
