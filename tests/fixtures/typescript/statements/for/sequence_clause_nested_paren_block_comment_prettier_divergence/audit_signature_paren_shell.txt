# Auto-generated prettier chain signature. Do not edit manually.
# Regenerate: deno task fixtures:update:formatted <pattern>
#
# Each %%PASS=N%% section is exactly prettier^N(unformatted_ours_<suffix>.<ext>),
# the same-suffix source beside this file. It exists when prettier's output from
# that source is claimed by no single-form marker — a chain with two or more
# distinct intermediates, or a fixed point no documented stable form holds.
# See docs/fixture_overview.md (rule N12) and crates/tsv_debug/src/fixtures/audit_signature.rs.

%%PASS=1%%
<script lang="ts">
	// A block comment inside the grouping shell the parser erased from a `for` header
	// sequence clause's LAST operand, where that operand is ITSELF a sequence. The one
	// pair the operand prints stands in for every shell that collapsed into it, so the
	// comment stays inside that pair.
	for (a, (b, c /* c1 */); ;) {
		fn();
	}

	// The shell may be the one the author wrote around the operand's own pair — the same
	// collapsed run, so the same answer.
	for (a, (b, c) /* c2 */; ;) {
		fn();
	}

	// Two shells with a comment each collapse in source order.
	for (a, (b, c /* c3 */) /* c4 */; ;) {
		fn();
	}
</script>

%%PASS=2 (fixed point)%%
<script lang="ts">
	// A block comment inside the grouping shell the parser erased from a `for` header
	// sequence clause's LAST operand, where that operand is ITSELF a sequence. The one
	// pair the operand prints stands in for every shell that collapsed into it, so the
	// comment stays inside that pair.
	for (a, (b, c) /* c1 */; ;) {
		fn();
	}

	// The shell may be the one the author wrote around the operand's own pair — the same
	// collapsed run, so the same answer.
	for (a, (b, c) /* c2 */; ;) {
		fn();
	}

	// Two shells with a comment each collapse in source order.
	for (a, (b, c) /* c3 */ /* c4 */; ;) {
		fn();
	}
</script>
