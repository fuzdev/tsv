# Auto-generated prettier chain signature. Do not edit manually.
# Regenerate: deno task fixtures:update:formatted <pattern>
#
# Each %%PASS=N%% section is exactly prettier^N(output_prettier.<ext>).
# This file exists when prettier is non-idempotent on output_prettier.* — it pins
# the full chain so the audit recognizes the case and the validator catches drift.
# See docs/fixture_overview.md (rule F4) and crates/tsv_debug/src/fixtures/audit_signature.rs.

%%PASS=2%%
<script>
	// A run in the binding→keyword gap keeps each comment on its own line; the
	// keyword still takes its continuation indent.
	for (a /* c2 */ of arr) // c1
		expr;

	// A run the author glued onto one line keeps that line.
	for (b /* c1 */ /* c2 */ of arr)
		// c3
		expr;

	// A blank line between two comments in the gap is preserved.
	for (c /* c2 */ of arr) // c1
		expr;

	// So is one the author left between the binding and the run's first comment.
	for (m of arr) // c1
		expr;

	// A multiline block after a line comment takes its own line.
	for (d /* c2 // c1
		c3 */ of arr)
		expr;

	// The keyword→iterable gap answers the same way (for-in shares the path).
	for (e /* c2 */ in obj) // c1
		expr;

	// So does the iterable→`)` gap.
	for (f of arr)
		// c1
		/* c2 */
		expr;

	// An own-line comment in the binding→keyword gap keeps its line once another
	// gap's line comment breaks the header.
	for (g /* c1 */ of arr) // c2
		expr;

	// The `(`→binding gap is the fourth: a run the author glued to the binding
	// leads it on one line.
	for (/* c1 */ /* c2 */ h of arr)
		// c3
		expr;

	// A blank line between two own-line comments there is preserved too.
	for (// c1

	// c2
	k of arr)
		// c3
		expr;
</script>

%%PASS=3 (fixed point)%%
<script>
	// A run in the binding→keyword gap keeps each comment on its own line; the
	// keyword still takes its continuation indent.
	for (a /* c2 */ of arr)
		// c1
		expr;

	// A run the author glued onto one line keeps that line.
	for (b /* c1 */ /* c2 */ of arr)
		// c3
		expr;

	// A blank line between two comments in the gap is preserved.
	for (c /* c2 */ of arr)
		// c1
		expr;

	// So is one the author left between the binding and the run's first comment.
	for (m of arr)
		// c1
		expr;

	// A multiline block after a line comment takes its own line.
	for (d /* c2 // c1
		c3 */ of arr)
		expr;

	// The keyword→iterable gap answers the same way (for-in shares the path).
	for (e /* c2 */ in obj)
		// c1
		expr;

	// So does the iterable→`)` gap.
	for (f of arr)
		// c1
		/* c2 */
		expr;

	// An own-line comment in the binding→keyword gap keeps its line once another
	// gap's line comment breaks the header.
	for (g /* c1 */ of arr)
		// c2
		expr;

	// The `(`→binding gap is the fourth: a run the author glued to the binding
	// leads it on one line.
	for (/* c1 */ /* c2 */ h of arr)
		// c3
		expr;

	// A blank line between two own-line comments there is preserved too.
	for (// c1

	// c2
	k of arr)
		// c3
		expr;
</script>
