# Auto-generated prettier chain signature. Do not edit manually.
# Regenerate: deno task fixtures:update:formatted <pattern>
#
# Each %%PASS=N%% section is exactly prettier^N(output_prettier.<ext>).
# This file exists when prettier is non-idempotent on output_prettier.* — it pins
# the full chain so the audit recognizes the case and the validator catches drift.
# See docs/fixture_overview.md (rule F4) and crates/tsv_debug/src/fixtures/audit_signature.rs.

%%PASS=2 (fixed point)%%
<script lang="ts">
	// The leftmost node of a `return` / `throw` / `yield` argument sits in a pair the printer
	// REQUIRES (`(a as any).b`, `(a++).b`), so the multi-line block glued inside it can never
	// reach the keyword's line: the pair holds it. The left-side walk that adds a hanging pair
	// for a STRIPPED shell
	// (operand_paren_leading_multiline_block_left_side_prettier_divergence) stops here —
	// descending would wrap a second pair around the one that already holds the run.
	function fn1() {
		return;
		/* a
b */ (a as any).b;
	}
	function fn2() {
		return;
		/* a
b */ (a++).b;
	}
	function* fn3() {
		yield;
		/* a
b */ (a as any).b;
	}
</script>
