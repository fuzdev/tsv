<script>
	// Each statement below is authored with a redundant paren pair around its
	// expression (see the variant); the paren is dropped and the comment inside
	// it leads the statement.
	(
	// c1
	a);

	(
	/* c2 */
	b);

	(
	// c3
	c, d);

	((
	// c4
	e));
</script>
