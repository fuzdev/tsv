<script>
	if (
		// before condition
		x // inline with condition
		// trailing after condition
	) {
		a();
	}

	// Block comment variant
	if (
		/* before */ x /* inline */
		/* trailing */
	) {
		b();
	}
</script>
