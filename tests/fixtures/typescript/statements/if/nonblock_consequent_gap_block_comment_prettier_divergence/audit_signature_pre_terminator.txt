# Auto-generated prettier chain signature. Do not edit manually.
# Regenerate: deno task fixtures:update:formatted <pattern>
#
# Each %%PASS=N%% section is exactly prettier^N(unformatted_ours_<suffix>.<ext>),
# the same-suffix source beside this file. It exists when prettier's output from
# that source is claimed by no single-form marker — a chain with two or more
# distinct intermediates, or a fixed point no documented stable form holds.
# See docs/fixture_overview.md (rule N12) and crates/tsv_debug/src/fixtures/audit_signature.rs.

%%PASS=1%%
<script>
	// own-line tail run, then a block in the `;`→`else` gap: source order, block on its own line
	if (a) expr1;
	// c1
	/* c2 */ else fn1();
	// a same-line trailer in the tail, then the gap block: the consequent stays collapsed
	if (b)
		expr2; // c3
	 /* c4 */ else fn2();
	// a lone gap block with no deferred tail stays on the `;` line
	if (c) expr3;
	 /* c5 */ else fn3();
	// an own-line block tail: the pair splits, in source order
	if (d) expr4;
	/* c6 */
	/* c7 */ else fn4();
	// two gap blocks split too — the gap's own one-comment-per-line policy
	if (e) expr5;
	// c8
	/* c9 */
	/* c10 */ else fn5();
</script>

%%PASS=2 (fixed point)%%
<script>
	// own-line tail run, then a block in the `;`→`else` gap: source order, block on its own line
	if (a) expr1;
	// c1
	/* c2 */ else fn1();
	// a same-line trailer in the tail, then the gap block: the consequent stays collapsed
	if (b)
		expr2; // c3
	/* c4 */ else fn2();
	// a lone gap block with no deferred tail stays on the `;` line
	if (c) expr3;
	/* c5 */ else fn3();
	// an own-line block tail: the pair splits, in source order
	if (d) expr4;
	/* c6 */
	/* c7 */ else fn4();
	// two gap blocks split too — the gap's own one-comment-per-line policy
	if (e) expr5;
	// c8
	/* c9 */
	/* c10 */ else fn5();
</script>
