<script lang="ts">
	// A line comment between a parameter name and its `:` keeps the comment after
	// the name; the `: type` continuation indents one level.
	function fn(
		a // c
			: string,
	) {}
</script>
