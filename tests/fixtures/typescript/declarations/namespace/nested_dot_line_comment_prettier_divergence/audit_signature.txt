# Auto-generated prettier chain signature. Do not edit manually.
# Regenerate: deno task fixtures:update:formatted <pattern>
#
# Each %%PASS=N%% section is exactly prettier^N(output_prettier.<ext>).
# This file exists when prettier is non-idempotent on output_prettier.* — it pins
# the full chain so the audit recognizes the case and the validator catches drift.
# See docs/fixture_overview.md (rule F4) and crates/tsv_debug/src/fixtures/audit_signature.rs.

%%PASS=2%%
<script lang="ts">
	// A line comment before the `.` — stays there, and the rest of the name continues
	// one level down
	namespace A.B {
		// c1
		export const a = 1;
	}

	// The gap on the other side of the dot
	namespace C.D {
		// c2
		export const a = 1;
	}

	// The `module` spelling and a deeper chain share the printer
	module E.F.G {
		// c3
		export const a = 1;
	}

	// A run stays in order, each comment on its own line
	namespace H.I { // c5 // c4
		export const a = 1;
	}
</script>

%%PASS=3 (fixed point)%%
<script lang="ts">
	// A line comment before the `.` — stays there, and the rest of the name continues
	// one level down
	namespace A.B {
		// c1
		export const a = 1;
	}

	// The gap on the other side of the dot
	namespace C.D {
		// c2
		export const a = 1;
	}

	// The `module` spelling and a deeper chain share the printer
	module E.F.G {
		// c3
		export const a = 1;
	}

	// A run stays in order, each comment on its own line
	namespace H.I {
		// c5 // c4
		export const a = 1;
	}
</script>
