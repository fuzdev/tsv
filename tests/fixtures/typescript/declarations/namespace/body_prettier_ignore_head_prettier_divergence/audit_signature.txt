# Auto-generated prettier chain signature. Do not edit manually.
# Regenerate: deno task fixtures:update:formatted <pattern>
#
# Each %%PASS=N%% section is exactly prettier^N(output_prettier.<ext>).
# This file exists when prettier is non-idempotent on output_prettier.* — it pins
# the full chain so the audit recognizes the case and the validator catches drift.
# See docs/fixture_overview.md (rule F4) and crates/tsv_debug/src/fixtures/audit_signature.rs.

%%PASS=2%%
<script lang="ts">
	// an own-line directive in the header→`{` gap freezes the whole namespace body
	namespace Aaa { // prettier-ignore
		const mmm = 1;
	}

	// `module` shares the head, so it shares the freeze
	module Bbb { // prettier-ignore
		const nnn = 2;
	}

	// `declare global` — `global` is both keyword and name, and the gap follows it
	declare global { // prettier-ignore
		const ooo = 3;
	}

	// an own-line block comment behaves identically — placement keys the freeze
	namespace Ccc /* prettier-ignore */ {
		const ppp = 4;
	}
</script>

%%PASS=3 (fixed point)%%
<script lang="ts">
	// an own-line directive in the header→`{` gap freezes the whole namespace body
	namespace Aaa {
		// prettier-ignore
		const mmm = 1;
	}

	// `module` shares the head, so it shares the freeze
	module Bbb {
		// prettier-ignore
		const nnn = 2;
	}

	// `declare global` — `global` is both keyword and name, and the gap follows it
	declare global {
		// prettier-ignore
		const ooo = 3;
	}

	// an own-line block comment behaves identically — placement keys the freeze
	namespace Ccc /* prettier-ignore */ {
		const ppp = 4;
	}
</script>
