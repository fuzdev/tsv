<script lang="ts">
	// Contextual type keywords are valid enum names (they are not reserved words)
	enum any {}
	enum string {}
	enum number {}
	enum object {}
	enum unknown {}

	// const / declare / export enum variants
	const enum boolean {}
	declare enum symbol {}
	export enum never {}
</script>
