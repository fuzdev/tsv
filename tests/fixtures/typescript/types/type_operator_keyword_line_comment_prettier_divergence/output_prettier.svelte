<script lang="ts">
	// keyof operator: comment after the keyword, operand on the next line
	type A = keyof // c
	B;

	// typeof operator: comment after the keyword, operand on the next line
	type B = typeof // c
	x;
</script>
