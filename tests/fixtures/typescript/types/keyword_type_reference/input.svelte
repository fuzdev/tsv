<script lang="ts">
	let break_ref: break;
	let default_ref: default;
	let static_ref: static;
	let function_ref: function;
	let case_ref: case;
	let yield_ref: yield;
	let await_ref: await;
	let while_ref: while;
	let do_ref: do;
	let continue_ref: continue;
	let debugger_ref: debugger;
	let delete_ref: delete;
	let in_ref: in;
	let instanceof_ref: instanceof;
	let switch_ref: switch;
	let try_ref: try;
	let catch_ref: catch;
	let finally_ref: finally;
	let throw_ref: throw;
	let extends_ref: extends;
	let enum_ref: enum;
	let class_ref: class;
	let else_ref: else;
	let for_ref: for;
	let if_ref: if;
	let return_ref: return;
	let var_ref: var;
	let let_ref: let;
	let super_ref: super;
	let export_ref: export;

	let qualified: break.foo;
	let withArgs: yield<T>;
	type U = A | default;
</script>
