# Auto-generated prettier chain signature. Do not edit manually.
# Regenerate: deno task fixtures:update:formatted <pattern>
#
# Each %%PASS=N%% section is exactly prettier^N(output_prettier.<ext>).
# This file exists when prettier is non-idempotent on output_prettier.* — it pins
# the full chain so the audit recognizes the case and the validator catches drift.
# See docs/fixture_overview.md (rule F4) and crates/tsv_debug/src/fixtures/audit_signature.rs.

%%PASS=2%%
<script lang="ts">
	// Cast keyword→value: the shell holds a leading run and a trailing one, and a third
	// comment follows it in the declarator's own gap
	const a = b as C; // c1 // c2 // c3

	// The same position with the trailing run alone — the trailing `//` retains the
	// shell on its own, with no leading run to hang
	const d = e as F; // c4 // c5

	// Type annotation
	let g: // c6
	H; // c7 // c8

	// Type-alias `=` RHS
	type I =
		// c9
		J; // c10 // c11

	// Type-parameter `=` default
	type K<
		L = // c12
			M // c13 // c14
	> = L;

	// Mapped-type value
	type N = {
		[
			O in string // c15
		]: P; // c16
		// c17
	};

	// Type-predicate `is`
	function fn(q: unknown): q is R { // c18
		// c19
		// c20
		return true;
	}

	// Conditional-type `extends`
	type S = T extends (U extends V ? W : X) // c21 // c22
		? // c23
			Y
		: Z;

	// Control: a leading run with no trailing one still hangs, the shell stripping and
	// the comment relocating into the keyword→value gap — the retention is keyed on the
	// TRAILING gap
	const a1 = b1 as C1; // c24
</script>

%%PASS=3 (fixed point)%%
<script lang="ts">
	// Cast keyword→value: the shell holds a leading run and a trailing one, and a third
	// comment follows it in the declarator's own gap
	const a = b as C; // c1 // c2 // c3

	// The same position with the trailing run alone — the trailing `//` retains the
	// shell on its own, with no leading run to hang
	const d = e as F; // c4 // c5

	// Type annotation
	let g: // c6
	H; // c7 // c8

	// Type-alias `=` RHS
	type I =
		// c9
		J; // c10 // c11

	// Type-parameter `=` default
	type K<
		L = // c12
			M // c13 // c14
	> = L;

	// Mapped-type value
	type N = {
		[
			O in string // c15
		]: P; // c16
		// c17
	};

	// Type-predicate `is`
	function fn(q: unknown): q is R {
		// c18
		// c19
		// c20
		return true;
	}

	// Conditional-type `extends`
	type S = T extends (U extends V ? W : X) // c21 // c22
		? // c23
			Y
		: Z;

	// Control: a leading run with no trailing one still hangs, the shell stripping and
	// the comment relocating into the keyword→value gap — the retention is keyed on the
	// TRAILING gap
	const a1 = b1 as C1; // c24
</script>
