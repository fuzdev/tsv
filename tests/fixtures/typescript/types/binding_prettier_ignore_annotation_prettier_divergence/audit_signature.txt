# Auto-generated prettier chain signature. Do not edit manually.
# Regenerate: deno task fixtures:update:formatted <pattern>
#
# Each %%PASS=N%% section is exactly prettier^N(output_prettier.<ext>).
# This file exists when prettier is non-idempotent on output_prettier.* — it pins
# the full chain so the audit recognizes the case and the validator catches drift.
# See docs/fixture_overview.md (rule F4) and crates/tsv_debug/src/fixtures/audit_signature.rs.

%%PASS=2 (fixed point)%%
<script lang="ts">
	// an own-line directive before a binding's `:` stays own-line and freezes the whole
	// `: type` annotation — class property, parameter, variable, index-signature key
	class A {
		a: { x: 1 } = // prettier-ignore
			{ x: 1 };

		// before the `?` marker: the freeze starts at the marker, the text the directive
		// precedes
		b?: { w: 4 }; // prettier-ignore
	}

	function fn(
		b // prettier-ignore
		: Record<string,   number>
	) {
		return b;
	}

	const c // prettier-ignore
	: {z:   3} = { z: 3 };

	interface B {
		[
			k // prettier-ignore
			: 'a'  |  'b'
		]: number;
	}
</script>
