# Auto-generated prettier chain signature. Do not edit manually.
# Regenerate: deno task fixtures:update:formatted <pattern>
#
# Each %%PASS=N%% section is exactly prettier^N(output_prettier.<ext>).
# This file exists when prettier is non-idempotent on output_prettier.* — it pins
# the full chain so the audit recognizes the case and the validator catches drift.
# See docs/fixture_overview.md (rule F4) and crates/tsv_debug/src/fixtures/audit_signature.rs.

%%PASS=2 (fixed point)%%
<script lang="ts">
	// a directive between a parameter's decorator and its binding freezes the binding —
	// the node it precedes — and the decorators stay outside the frozen slice
	class A {
		constructor(
			@dec
			// prettier-ignore
			private a: { x: 1 },
			b: U
		) {}

		m(
			@dec1
			@dec2 // prettier-ignore
			c: { x: 1 },
			d: U
		) {}

		// the frozen slice prints the binding's source verbatim, redundant parens and the
		// comment inside them included, so the parameter list's own trailing run claims
		// nothing there and the comment is printed once
		n(@dec
			// prettier-ignore
			e = (1   /* c */), f: U) {}
	}
</script>
