<script>
	// Own-line comment after `export default`, before the value: kept on its own
	// line with the value below (tsv preserves the author's placement). A blank
	// line after the comment is preserved. Prettier pulls the comment up onto the
	// `export default` line.
	export default /* c */

	x;
</script>
