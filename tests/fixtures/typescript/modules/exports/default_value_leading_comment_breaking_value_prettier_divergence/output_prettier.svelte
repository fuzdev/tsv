<script lang="ts">
	// a block comment the author broke after, before a value that breaks on its own: the break
	// is forced, so the value hangs below the keyword
	export default /* x */
	{
		aaa: 1
	};
</script>
