<script lang="ts">
	// An assignment as the exported value takes clarity parens, like any other value
	// position — the leftmost-token rule is a separate reason for a separate pair.
	export default (x = y);
</script>
