<script lang="ts">
	// a line comment on the keyword's line, then a multi-line comment glued to the value: the
	// value keeps its own layout below the comment
	export default // x
	/* y
	 */ (
	aaa ? bbb : ccc);
</script>
