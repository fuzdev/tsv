<script lang="ts">
	// Block comment between `import` and the `type` keyword — stays where the author
	// wrote it; prettier relocates it to the binding side of `type`
	import /* c */ type C = require('./c');
</script>
