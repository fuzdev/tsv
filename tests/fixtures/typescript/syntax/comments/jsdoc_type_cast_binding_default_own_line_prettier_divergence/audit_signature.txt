# Auto-generated prettier chain signature. Do not edit manually.
# Regenerate: deno task fixtures:update:formatted <pattern>
#
# Each %%PASS=N%% section is exactly prettier^N(output_prettier.<ext>).
# This file exists when prettier is non-idempotent on output_prettier.* — it pins
# the full chain so the audit recognizes the case and the validator catches drift.
# See docs/fixture_overview.md (rule F4) and crates/tsv_debug/src/fixtures/audit_signature.rs.

%%PASS=2 (fixed point)%%
<script>
	// A cast comment the author gave a line of its own hangs the value: the `=` ends the
	// line, and the comment and its `(` sit indented under it — the layout every other
	// value gap gives an own-line cast.
	const {
		/** @type {A} */
		objDefault = someObject.somePropertyName.someOtherProperty.andAnotherOne.more
	} = obj;

	const [
		/** @type {A} */
		arrDefault = someObject.somePropertyName.someOtherProperty.andAnotherOne.mor
	] = arr;

	function fn1(
		/** @type {A} */
		paramDefault = someObject.somePropertyName.someOtherProperty.andAnother.m
	) {}
</script>
