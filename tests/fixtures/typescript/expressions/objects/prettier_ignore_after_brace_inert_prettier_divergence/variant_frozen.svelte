<script lang="ts">
	// a directive trailing the opening brace is inert — the comment stays on the
	// brace line and the members format normally
	const o = {
		// prettier-ignore
		a:   1,
		b: 2
	};
</script>
