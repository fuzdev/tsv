# Auto-generated prettier chain signature. Do not edit manually.
# Regenerate: deno task fixtures:update:formatted <pattern>
#
# Each %%PASS=N%% section is exactly prettier^N(output_prettier.<ext>).
# This file exists when prettier is non-idempotent on output_prettier.* — it pins
# the full chain so the audit recognizes the case and the validator catches drift.
# See docs/fixture_overview.md (rule F4) and crates/tsv_debug/src/fixtures/audit_signature.rs.

%%PASS=2 (fixed point)%%
<script lang="ts">
	async function fn1() {
		// A block comment the author wrote inside a frozen operand's parens stays inside
		// them — the same answer the unfrozen `await` operand gives that gap.
		const aaa =
			await // prettier-ignore
			fn2(  bbb  ); /* c */

		// A line comment in the same gap opens the pair — inline it would swallow the `)`.
		const ccc =
			await // prettier-ignore
			fn2(  ddd  ); // c

		// An operand whose parens are REQUIRED keeps the comment inside them too.
		const eee = await // prettier-ignore
		(fff  ??  ggg); /* c */
	}
</script>
