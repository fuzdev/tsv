<script>
	// Single-line block glued to `=` with the value on the next line stays inline.
	x = /* c */ a;

	// Own-line comment (a newline precedes it) keeps the break after `=`.
	y =
		/* c */
		b;
</script>
