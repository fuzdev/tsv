# Auto-generated prettier chain signature. Do not edit manually.
# Regenerate: deno task fixtures:update:formatted <pattern>
#
# Each %%PASS=N%% section is exactly prettier^N(unformatted_ours_<suffix>.<ext>),
# the same-suffix source beside this file. It exists when prettier's output from
# that source is claimed by no single-form marker — a chain with two or more
# distinct intermediates, or a fixed point no documented stable form holds.
# See docs/fixture_overview.md (rule N12) and crates/tsv_debug/src/fixtures/audit_signature.rs.

%%PASS=1%%
<script lang="ts">
	// A block comment glued to a cast's operand stays glued to it when the cast takes a
	// pair of its own: the pair goes outside the comment.

	// member base
	const a = (/* c1 */ x as A).b;

	// callee
	const b = (/* c2 */ x as A)();

	// logical operand
	const c = y && (/* c3 */ x as A);

	// ternary test
	const d = (/* c4 */ x satisfies A) ? 1 : 2;

	// non-null operand
	const e = (/* c5 */ x as A)!;

	// null control: no pair to add, both formatters agree
	const f = /* c6 */ x as A;

	// a second member base, which the shell variant leaves bare
	const g = /* c7 */ (x as A).c;
</script>

%%PASS=2 (fixed point)%%
<script lang="ts">
	// A block comment glued to a cast's operand stays glued to it when the cast takes a
	// pair of its own: the pair goes outside the comment.

	// member base
	const a = /* c1 */ (x as A).b;

	// callee
	const b = /* c2 */ (x as A)();

	// logical operand
	const c = y && /* c3 */ (x as A);

	// ternary test
	const d = /* c4 */ (x satisfies A) ? 1 : 2;

	// non-null operand
	const e = /* c5 */ (x as A)!;

	// null control: no pair to add, both formatters agree
	const f = /* c6 */ x as A;

	// a second member base, which the shell variant leaves bare
	const g = /* c7 */ (x as A).c;
</script>
