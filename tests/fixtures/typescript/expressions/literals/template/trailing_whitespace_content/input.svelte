<script lang="ts">
	const a = `export { 
	a, 
	b,
}`;

	const b = `line with trailing space 
next line`;

	const c = `no trailing space
next line`;

	const d = `mixed trailing spaces   
	indented trailing   
regular`;
</script>
