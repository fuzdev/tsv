<script>
	/* Short call - stays inline (under 100 chars) */
	const short = fn(arg1, arg2, arg3);

	/* Long call - exceeds 100 chars, wraps to one arg per line */
	const long = fn(
		argument1,
		argument2,
		argument3,
		argument4,
		argument5,
		argument6,
		argument7,
		argument8,
	);
</script>
