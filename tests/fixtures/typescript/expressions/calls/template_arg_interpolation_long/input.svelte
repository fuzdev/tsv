<script lang="ts">
	// Template with interpolations at 100 chars - no break
	console.error(`Invalid indices: from ${from_index} to ${to_index} in array of length ${lenxxxx}`);

	// Template with interpolations at 101 chars - breaks
	console.error(
		`Invalid indices: from ${from_index} to ${to_index} in array of length ${lenxxxxx}`
	);
</script>
