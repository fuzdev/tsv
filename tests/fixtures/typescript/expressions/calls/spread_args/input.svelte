<script lang="ts">
	const a = fn(...args);
	const b = fn(x, ...rest);
	const c = fn(...first, y);
	const d = fn(x, ...middle, y);
	const e = fn(...arr1, ...arr2);
	const f = fn<T>(...args);
	const g = fn<T, U>(x, ...rest, y);
</script>
