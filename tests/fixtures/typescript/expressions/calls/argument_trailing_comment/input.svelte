<script lang="ts">
	// Trailing comment after argument (forces multiline)
	fn(
		a && b // trailing
	);

	// Trailing comment with multiple arguments
	fn(
		x, // comment1
		y // comment2
	);
</script>
