<!--
	A non-breaking space (U+00A0) body is content, not boundary whitespace, so the
	block stays inline and the NBSP is preserved (an ASCII-space body expands instead)
-->
{#if a} {/if}
{#each items as i} {/each}
{#key k} {/key}
