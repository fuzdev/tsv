<!-- Parity guard: a wide inline HTML element (`<a>`) at the same boundary as the
     wide-component fixtures. It drops to its own line with the preceding word hugged,
     identical in shape to `inline_component_wide_long`. Inline elements already do
     this correctly; this pins that they keep doing so (and match components). -->
<p>
	text1 text2 text3 text4 text5 text6 text7 text8 text9 text10 text11 text12 aaaaaaaaaaaaaa and <a href="aaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaa">content</a> tail1 tail2 tail3 tail4 tail5 tail6
</p>
