<!--
	A closing tag name ends at any JS `\s` character, so a Unicode space may sit
	between the name and `>` (see the unformatted_* variants). The raw-text and
	RCDATA closes tolerate the same run.
-->
<div>text</div>
<textarea>text</textarea>

<style>
	div {
		color: red;
	}
</style>
