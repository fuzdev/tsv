<!-- 100 cols: fits, both keep the siblings on one line -->
<span>a fairly long inline label used as filler to reach the width boundary</span> <button></button>

<!-- 101 cols: exceeds print width, so the siblings break onto their own lines -->
<span>a fairly long inline label used as filler to reach the width boundarya</span> <button></button>
