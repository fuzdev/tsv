# Auto-generated prettier chain signature. Do not edit manually.
# Regenerate: deno task fixtures:update:formatted <pattern>
#
# Each %%PASS=N%% section is exactly prettier^N(output_prettier.<ext>).
# This file exists when prettier is non-idempotent on output_prettier.* — it pins
# the full chain so the audit recognizes the case and the validator catches drift.
# See docs/fixture_overview.md (rule F4) and crates/tsv_debug/src/fixtures/audit_signature.rs.

%%PASS=2 (fixed point)%%
<!-- A `//` in a whitespace-sensitive element's attribute list runs to end of line, so the `>`
	takes its own line instead of hugging the last attribute. The break lands inside the tag,
	where no character is content, so the literal text is untouched. -->

<!-- With content, the attributes keep the tag-name line and the `>` hugs the content one
	level in, the layout an over-width `<textarea>` already takes -->
<pre>text</pre>

<pre data-attr="value">text</pre>
// c

<!-- With no content there is nothing for the `>` to protect, so it sits at base indent -->
<pre></pre>

<pre data-attr="value"></pre>
// c

<!-- `<textarea>` is the same element class and takes the same two shapes -->
<textarea>text</textarea>

<textarea data-attr="value"></textarea> // c

<!-- An own-line `//` before the `>` carries its line break with it: the attributes wrap
	one per line and the `>` keeps the next line, one level in -->
<pre data-attr="value">text</pre>
// c
