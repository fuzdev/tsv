<style>
	/* `from`/`to` are case-insensitive keyframe-selector keywords; prettier (and
	   tsv) lowercase them. Percentage selectors are unaffected. */
	@keyframes slide {
		from {
			left: 0;
		}
		50% {
			left: 50%;
		}
		to {
			left: 100%;
		}
	}
</style>
