<style>
	/* A keyframe selector glued to a no-break space (U+00A0) is not the `from`/`to`
	   keyword (the space is part of the name), so it is kept verbatim: not lowercased,
	   the space intact */
	@keyframes slide {
		FROM  {
			left: 0;
		}
		to  {
			left: 100%;
		}
	}
</style>
