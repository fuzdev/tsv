<style>
	/* an at-rule whose prelude is empty writes no name→prelude separator, so its `;`
	closes flush — the block form is already flush because `{` absorbs the separator */
	@media;
	@supports;
	@container;
	@custom-media;
	@layer;
	@scope;
</style>
