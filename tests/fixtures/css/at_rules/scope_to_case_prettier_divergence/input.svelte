<style>
	/* the `to` scope-limit keyword is a case-insensitive grammar keyword; tsv
	   canonicalizes it to lowercase, prettier preserves the author's case. */
	@scope (.card) to (.footer) {
		a {
			color: red;
		}
	}
</style>
