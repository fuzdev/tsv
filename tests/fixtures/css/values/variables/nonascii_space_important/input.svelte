<style>
	a {
		/* A custom-property value that is one no-break space (U+00A0) is not empty (the
		   space is not CSS whitespace), so `!important` follows it after the usual space */
		--a:   !important;
		--b:   !important;
	}
</style>
