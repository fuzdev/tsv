<style>
	div {
		cursor: pointer;
		cursor: default;
		cursor: grab;
		cursor: grabbing;
		cursor: not-allowed;
		cursor: wait;
		cursor: crosshair;
		cursor: url(cursor.png), auto;
		pointer-events: auto;
		pointer-events: none;
		user-select: none;
		user-select: text;
		user-select: all;
		touch-action: none;
		touch-action: pan-x pan-y;
	}
</style>
